<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>CodeSaw</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://haotianmcihael.github.io/"/>
  <updated>2022-01-14T09:29:43.333Z</updated>
  <id>http://haotianmcihael.github.io/</id>
  
  <author>
    <name>HaotianMichael</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>MIT6.824(Spring 2021)-MapReduce</title>
    <link href="http://haotianmcihael.github.io/2022/01/03/MIT6-824-Spring-2021-MapReduce/"/>
    <id>http://haotianmcihael.github.io/2022/01/03/MIT6-824-Spring-2021-MapReduce/</id>
    <published>2022-01-03T14:29:59.000Z</published>
    <updated>2022-01-14T09:29:43.333Z</updated>
    
    <content type="html"><![CDATA[<h3 id="abstract">Abstract</h3><blockquote><p>最近终于有时间研究一下MIT的<a href="https://pdos.csail.mit.edu/6.824/schedule.html" target="_blank" rel="noopener">神课</a><code>6.824</code>。</p><p>这门课教<code>Distributed System</code>，即多台物理隔离的计算机通过网络来协调，共同完成一致性任务，可以是计算任务，也可以是存储任务。</p><p><strong>分布式系统</strong>和单机相比，除了有很多高并发的<code>Partial Failures</code>，更多的是硬件老化，断电，网卡失灵等这种现实问题。因此设计低成本高性能系统以及处理并发和网络问题——<strong><code>Performance</code>性能和<code>Fault Tolerance</code>容错</strong>是这门课的核心。</p><p>课程共有20个<code>Lectures</code>和4个<code>labs</code>。和<code>6.828</code>一样重视实践——<strong>看论文，查资料，做<code>lab</code></strong>~简单粗暴。所以学习过程基本上就按照如下推进：</p><ul><li>提前看<code>Lecture</code>要求的<code>papers</code></li><li>听<a href="https://www.bilibili.com/video/BV1R7411t71W?spm_id_from=333.999.0.0" target="_blank" rel="noopener">对应</a>的<code>Lectures</code></li><li>查找<code>lab</code>相关的学习资料</li><li>做<code>lab</code></li></ul><p>之前做<code>6.828</code>的时候就发现我在做这种<strong>学习过程很长，思考内容又很深</strong>的课程训练时很容易钻牛角尖而让自己乱了节奏，思维如若不能放松下来效果肯定不会太好——希望这次能尽量调整过来。</p></blockquote><a id="more"></a><h3 id="paper">Paper</h3><blockquote><p>论文<a href="https://pdos.csail.mit.edu/6.824/papers/mapreduce.pdf" target="_blank" rel="noopener">MapReduce:Simplified Data Processing on Large Clusters</a>是关于Google已经淘汰掉的<strong>大规模数据集处理编程架构<code>MapReduce</code></strong>。当时的Google面临在多台机器上的大规模数据集处理比如计算<code>PageRank</code>或者对所有文件进行<code>grep</code>等。</p><p>而统一化编程模型可以很好的抽象掉因为大规模集群导致的<strong>并行，网络，容错</strong>等问题——将这些细节隐藏起来，只留一个供程序员使用的编程接口可以大大提高开发效率。而工业级<code>MapReduce</code>的本质就是一个<strong>利用普通机器组成的大规模计算集群进行并行，高容错，高性能的大数据处理函数框架。</strong></p></blockquote><h4 id="理论模型">理论模型</h4><blockquote><p>理论模型借鉴于<code>Lisp</code>语言中的<code>map/reduce</code>函数。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">&gt; <span class="function"><span class="keyword">void</span>  <span class="title">map</span><span class="params">(<span class="built_in">string</span> key, <span class="built_in">string</span> value)</span> </span>&#123;</span><br><span class="line">&gt;   <span class="comment">//key:  文档名</span></span><br><span class="line">&gt;   <span class="comment">//value: 文档内容</span></span><br><span class="line">&gt;   <span class="keyword">for</span> each word w in value:</span><br><span class="line">&gt;   EmitIntermediate(w, <span class="string">"1"</span>);  </span><br><span class="line">&gt; &#125;</span><br><span class="line">&gt; </span><br><span class="line">&gt; <span class="function"><span class="keyword">void</span> <span class="title">reduce</span> <span class="params">(<span class="built_in">string</span> key, Iterator values)</span> </span>&#123;</span><br><span class="line">&gt;   <span class="comment">//key: 一个单词</span></span><br><span class="line">&gt;  <span class="comment">//value: 计数值列表</span></span><br><span class="line">&gt;   <span class="keyword">int</span> result = <span class="number">0</span>;</span><br><span class="line">&gt;   <span class="keyword">for</span> each v in value:</span><br><span class="line">&gt;   result  += ParseInt(v);</span><br><span class="line">&gt;   Emit(toString(result));  </span><br><span class="line">&gt; &#125;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>这里两个函数模型的类型分别是:</p><ul><li>map: (k1, v1) —&gt; list(k2, v2)</li><li>reduce: (k2, list(v)) —&gt; list(v2)</li></ul><p>对于一个<code>map</code>操作，将<code>key</code>文档中的内容按照一定方式读取，并统计其中的<code>w</code>项的频率<code>value</code>并输出到一个中间文件上。而<code>reduce</code>从中间文件中读取并迭代其<code>lists</code>，将对应的<code>key</code>项统计。</p></blockquote><p><img src="1.png" width="50%" height="50%" alt=""></p><blockquote><p>这一模型可以很好的处理并行化操作——<strong>因为<code>map</code>的过程可以在多台机器上并行完成而没有依赖。<code>reduce</code>过程也是。但是整体<code>reduce</code>和<code>map</code>之间存在着顺序依赖关系即必须是相应的<code>map</code>操作结束才可以唤醒<code>reduce</code>操作。</strong></p><p>这样的话所有的依赖和协调过程都被隐藏在<code>map</code>和<code>reduce</code>函数的<strong>数据分发</strong>之间，而开发者可以完全不用考虑分布式细节而单独编写这两个函数以达到自己的目的。</p></blockquote><h4 id="实现">实现</h4><blockquote><p>从实现角度看系统的很多行为是由<code>MapReduce</code>内置的库完成的。这里有几个容易混淆的概念：</p><ul><li><code>user program</code>：用户程序，会被多次<code>fork</code>成为<code>master</code>和<code>worker</code><br></li><li><p><code>master</code>: 主节点，用来将<code>task</code>分配给相应的<code>worker</code>，记录工作节点的当前状态和声明周期</p></li><li><code>task</code>：<code>map</code>任务或者<code>reduce</code>任务，由相应的<code>worker</code>完成</li><li><code>worker</code>: 工作节点，用来完成相应的<code>task</code></li><li><p><code>user-defined map/reduce</code>: 开发者自定义的<code>map/reduce</code>函数</p></li></ul></blockquote><p><img src="2.png" width="50%" height="50%" alt=""></p><h5 id="输入预处理">输入预处理</h5><blockquote><p>首先库函数会将输入文件切分成<code>M</code>份，并在一个集群的若干台机器上启动程序的多个副本。副本中除了有一个作为主节点，其他都是工作节点。</p><p>主节点将M个map任务R个reduce任务分配给空闲的工作节点（理解<code>task</code>在被分配给<code>worker</code>后两者是一个意思）。</p></blockquote><h5 id="map工作节点区分工作节点和用户函数">map工作节点（区分工作节点和用户函数）</h5><blockquote><p>每一个被分配map的工作节点需要读取相应内容并解析出<code>key/value</code>对，<strong>并将这些作为参数提供给用户自定义的map函数，</strong>而函数输出的中间结果缓存在内存中。</p><p>缓存的中间<code>key/value</code>对会被划分为R块并写入本地磁盘，而这些数据对在磁盘中的相应位置会被传回主节点——由主节点再传给相应的reduce工作节点。</p></blockquote><h5 id="reduce工作节点区分工作节点和用户函数">reduce工作节点（区分工作节点和用户函数）</h5><blockquote><p>保证map操作结束后，主节点唤醒reduce工作节点并告知其中间输出的磁盘位置。reduce工作节点使用RPC调用获取这些中间数据。<strong>当reduce读取完所有的数据，需要按照key对其进行排序，是为了最后迭代器统计计数提供前置条件。</strong>最后reduce工作节点迭代排好序的中间数据，并将每一个中间key和它关联一组中间value传递给用户定义的reduce函数。</p></blockquote><h5 id="shuffle过程核心状态机">shuffle过程（核心状态机）</h5><blockquote><p>整个map和reduce之间的数据shuffle是系统分布式的核心，关键就是map生成的中间文件及其命名。</p></blockquote><p><img src="3.png" width="50%" height="50%" alt=""></p><blockquote><p>如上图，假设有5个map工作节点和3个reduce工作节点。</p><ul><li>map工作节点的输出分为3类，分别由<code>R0</code>，<code>R1</code>和<code>R2</code>处理，<strong>当map的解析函数一致时，相同的key一定被分到了同一个中间文件中。</strong>即<code>0-0.txt</code>、<code>1-0.txt</code>和<code>0-2.txt</code>三个文件中<code>key</code>类型的交集是空集。</li><li><code>md5(key)%R</code>计算map最终输出落入到哪个中间文件</li><li>中间文件命名：map任务编号-reduce任务编号</li></ul><p>因此5个map操作结束后会生成5*3的中间文件矩阵。</p><ul><li>map结束后，3个reduce工作节点通过RPC分别获取矩阵的每一列文件数据</li></ul><p>比如R0调用<code>0-0.txt、1-0.txt、2-0.txt、3-0.txt、4-0.txt</code>。因为同一列中类型相同，所以只需要内部排序。</p><p>所谓<code>shuffle</code>就是map/reduce对中间文件的行列操作，其结果是将中间文件进一步处理成为<code>&lt;value, list&lt;key&gt;&gt;</code>作为用户定义reduce函数的输入进行计数统计。</p></blockquote><h5 id="最终输出">最终输出</h5><blockquote><p>reduce工节点处理完计数后，将结果输出到磁盘上作为下一次<code>MapReduce</code>的输入。</p></blockquote><h4 id="工业级处理">工业级处理</h4><blockquote><p>在框架实现的过程中出现了很多的细节。Google在将<code>MapReduce</code>工业化落地的过程中也将这些影响系统<strong>性能</strong>和<strong>可靠性</strong>的主要因素进行了优化——而一个优秀的系统本身就是在实践中不断迭代出来的，这里简单概括一下，不做详细讨论。</p></blockquote><h5 id="容错">容错</h5><blockquote><p><strong>分布式容错的本质就是如何在不可靠的硬件上构建可靠的软件。</strong>因为是大规模集群，所以很难避免<code>Partial Failure</code>——从实现角度看就是各个节点的容错。</p><ul><li>主节点：主节点奔溃之后会coredump到一个恢复文件中，重启后可以从最近的恢复点重新执行MapReduce</li><li>工作节点：奔溃后一般都要将当前的task重新分配到一个节点完成<ul><li>map：输出是一个中间文件所以符合原子提交即后面的任务会将文件覆盖</li><li>reduce：其输出是一个持久性的文件所以需要重命名一个新的文件防止冲突</li></ul></li></ul></blockquote><h5 id="加速">加速</h5><blockquote><p>当出现大规模的机器节点时，很多性能问题会成为优化的方向。比如进一步优化网络和磁盘性能。</p><ul><li>局部性：在分布式系统中，输入文件传到map节点，map中间文件传到reduce节点都需要网络带宽。可以考虑将map直接分配到本地磁盘上等操作增加并行性。</li><li>任务粒度：M个map节点，R个reduce节点。分配太多节点导致网络带宽不够，分配太少导致并行度不够。</li><li>负载均衡：不同的工作节点性能也不同，如果系统卡在最后几个节点导致负载过高花费时间过长，就会让整个系统处于不平衡的状态。</li></ul></blockquote><h3 id="lecture12">Lecture1~2</h3><h4 id="go语言环境配置">go语言环境配置</h4><blockquote><p>作为一个<code>VIM</code>重度用户我已经不用IDE很多年了。就自行配置了<code>Vim-Go</code>开发环境。</p></blockquote><h5 id="go安装配置">Go安装配置</h5><blockquote><p>我用<code>MacOs Catalina</code>环境，下载安装就不说了。主要关注<code>Go</code>环境变量，需要写入相应的<code>SHELL</code>配置文件中，我的在<code>.zshrc</code>中：</p><ul><li><code>GOROOT</code>: Go语言的安装目录，一般是<code>/usr/local/go</code></li><li><code>GOPATH</code>: 工作区即写代码的目录，一般分为三个子目录：<ul><li><code>src</code><br></li><li><code>bin</code></li><li><code>pkg</code></li></ul></li><li><code>GOBIN</code>: 存放GO语言可执行文件的目录（这里的可执行文件主要指通过<code>go get</code>下载并编译好的库，供直接调用的）</li><li><code>PATH</code>: 需要将当前的可执行文件目录放到PATH下面</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;</span> # 注意""的使用</span><br><span class="line"><span class="meta">&gt;</span> export GOROOT=/usr/local/go</span><br><span class="line"><span class="meta">&gt;</span> export GOPATH=~/go/golib:~/go/goproject    #这里设置了两个工作区</span><br><span class="line"><span class="meta">&gt;</span> export GOBIN=~/go/gobin</span><br><span class="line"><span class="meta">&gt;</span> export PATH="$GOROOT/bin:$GOBIN:$PATH"</span><br><span class="line"><span class="meta">&gt;</span> </span><br><span class="line"><span class="meta">&gt;</span></span><br></pre></td></tr></table></figure></blockquote><h5 id="镜像">镜像</h5><blockquote><p>因为Github和Go官网不太好用，所以使用镜像来下载需要的库比较方便。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;</span> # Mac/Linux环境下</span><br><span class="line"><span class="meta">&gt;</span> export GO111MODULE=on</span><br><span class="line"><span class="meta">&gt;</span> export GOPROXY=https://goproxy.cn</span><br><span class="line"><span class="meta">&gt;</span></span><br></pre></td></tr></table></figure></blockquote><h5 id="二进制工具">二进制工具</h5><blockquote><p><code>vim-go</code>是一个开发<code>go</code>的<code>vim</code>插件。我使用<code>vundle</code>包管理工具来管理<code>vim</code>插件。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;</span> # 如何使用vundle自行Google</span><br><span class="line"><span class="meta">&gt;</span> Plugin 'fatih/vim-go'</span><br><span class="line"><span class="meta">&gt;</span></span><br></pre></td></tr></table></figure></blockquote><blockquote><p>因为这个插件启动还需要一些和二进制工具，具体的列表可打开<code>&quot;~/.vim/bundle/vim-go/plugin/go.vim&quot;</code>如下：</p></blockquote><p><img src="4.png" width="50%" height="50%" alt=""></p><blockquote><p>常规做法是在<code>vim</code>命令行模式下面直接执行<code>:GoInstallBinaries</code>来自动安装。如果没有安装代理的话尽管有镜像还是很容易出错，所以我的做法是一个一个从官网上<code>go get</code>然后<code>go insatll</code>手动安装。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;</span> $ go get github.com/go-delve/delve/cmmd/dlv     #从官网上直接下载二进制工具</span><br><span class="line"><span class="meta">&gt;</span> $ go install github.com/go-delve/delve/cmmd/dlv@laatest   #使用go install手动安装</span><br><span class="line"><span class="meta">&gt;</span></span><br></pre></td></tr></table></figure></blockquote><blockquote><p>如果按照前面的方式配置好了环境变量，那这些二进制工具就会被安装在<code>~/go/gobin</code>目录下。验证方式就是打开<code>vim</code>之后不会有插件报错信息。</p></blockquote><h3 id="lab1">Lab1</h3><h3 id="to-conclude">To conclude</h3><blockquote><p>在学习过程用到的资料列举如下:</p><p><a href="https://mit-public-courses-cn-translatio.gitbook.io/mit6-824/" target="_blank" rel="noopener">MIT6.824中文翻译</a></p><p><a href="https://go-zh.org/doc/" target="_blank" rel="noopener">Go文档</a></p><p><a href="https://book.itsfun.top/gopl-zh/" target="_blank" rel="noopener">Go语言圣经</a></p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;最近终于有时间研究一下MIT的&lt;a href=&quot;https://pdos.csail.mit.edu/6.824/schedule.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;神课&lt;/a&gt;&lt;code&gt;6.824&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;这门课教&lt;code&gt;Distributed System&lt;/code&gt;，即多台物理隔离的计算机通过网络来协调，共同完成一致性任务，可以是计算任务，也可以是存储任务。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;分布式系统&lt;/strong&gt;和单机相比，除了有很多高并发的&lt;code&gt;Partial Failures&lt;/code&gt;，更多的是硬件老化，断电，网卡失灵等这种现实问题。因此设计低成本高性能系统以及处理并发和网络问题——&lt;strong&gt;&lt;code&gt;Performance&lt;/code&gt;性能和&lt;code&gt;Fault Tolerance&lt;/code&gt;容错&lt;/strong&gt;是这门课的核心。&lt;/p&gt;
&lt;p&gt;课程共有20个&lt;code&gt;Lectures&lt;/code&gt;和4个&lt;code&gt;labs&lt;/code&gt;。和&lt;code&gt;6.828&lt;/code&gt;一样重视实践——&lt;strong&gt;看论文，查资料，做&lt;code&gt;lab&lt;/code&gt;&lt;/strong&gt;~简单粗暴。所以学习过程基本上就按照如下推进：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;提前看&lt;code&gt;Lecture&lt;/code&gt;要求的&lt;code&gt;papers&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;听&lt;a href=&quot;https://www.bilibili.com/video/BV1R7411t71W?spm_id_from=333.999.0.0&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;对应&lt;/a&gt;的&lt;code&gt;Lectures&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;查找&lt;code&gt;lab&lt;/code&gt;相关的学习资料&lt;/li&gt;
&lt;li&gt;做&lt;code&gt;lab&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;之前做&lt;code&gt;6.828&lt;/code&gt;的时候就发现我在做这种&lt;strong&gt;学习过程很长，思考内容又很深&lt;/strong&gt;的课程训练时很容易钻牛角尖而让自己乱了节奏，思维如若不能放松下来效果肯定不会太好——希望这次能尽量调整过来。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="Golang" scheme="http://haotianmcihael.github.io/categories/Golang/"/>
    
    
      <category term="Distributed System" scheme="http://haotianmcihael.github.io/tags/Distributed-System/"/>
    
      <category term="MapReduce" scheme="http://haotianmcihael.github.io/tags/MapReduce/"/>
    
      <category term="MIT6.824" scheme="http://haotianmcihael.github.io/tags/MIT6-824/"/>
    
  </entry>
  
  <entry>
    <title>NJU静态程序分析(2-Interprocedural-Analysis)</title>
    <link href="http://haotianmcihael.github.io/2021/05/10/NJU%E9%9D%99%E6%80%81%E7%A8%8B%E5%BA%8F%E5%88%86%E6%9E%90%EF%BC%882-Interprocedural-Analysis%EF%BC%89/"/>
    <id>http://haotianmcihael.github.io/2021/05/10/NJU静态程序分析（2-Interprocedural-Analysis）/</id>
    <published>2021-05-10T10:23:58.000Z</published>
    <updated>2022-01-29T02:56:47.588Z</updated>
    
    <content type="html"><![CDATA[<h3 id="abstract">Abstract</h3><blockquote><p>根据<code>safe-approximation</code>，过程内数据流分析<code>Intraprocedural</code>对所有的函数调用<code>method call</code>做出保守假设——即所有分析结果必须<code>safe</code>，而根据<code>Lattice</code>理论这种处理会使<code>must</code>和<code>may</code>分析都变的<code>less precise</code>。所以引入过程间数据流分析<code>Interprocedural Analysis</code>，通过<code>return/call edges</code>来实现过程间的数据流传递，从而进一步提高分析的精度。</p><p>For Better precision, we need Interprocedural Analysis：propagate data-flow information along <strong>interprocedural control-flow edges</strong> i.e., call and return edges</p><p>而<code>constant Propagation</code>常量传播优化在LLVM中也是通过<code>Interprocedural Analysis</code>实现的。</p></blockquote><a id="more"></a>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;根据&lt;code&gt;safe-approximation&lt;/code&gt;，过程内数据流分析&lt;code&gt;Intraprocedural&lt;/code&gt;对所有的函数调用&lt;code&gt;method call&lt;/code&gt;做出保守假设——即所有分析结果必须&lt;code&gt;safe&lt;/code&gt;，而根据&lt;code&gt;Lattice&lt;/code&gt;理论这种处理会使&lt;code&gt;must&lt;/code&gt;和&lt;code&gt;may&lt;/code&gt;分析都变的&lt;code&gt;less precise&lt;/code&gt;。所以引入过程间数据流分析&lt;code&gt;Interprocedural Analysis&lt;/code&gt;，通过&lt;code&gt;return/call edges&lt;/code&gt;来实现过程间的数据流传递，从而进一步提高分析的精度。&lt;/p&gt;
&lt;p&gt;For Better precision, we need Interprocedural Analysis：propagate data-flow information along &lt;strong&gt;interprocedural control-flow edges&lt;/strong&gt; i.e., call and return edges&lt;/p&gt;
&lt;p&gt;而&lt;code&gt;constant Propagation&lt;/code&gt;常量传播优化在LLVM中也是通过&lt;code&gt;Interprocedural Analysis&lt;/code&gt;实现的。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="Java" scheme="http://haotianmcihael.github.io/categories/Java/"/>
    
    
      <category term="PL" scheme="http://haotianmcihael.github.io/tags/PL/"/>
    
      <category term="Software-Analysis" scheme="http://haotianmcihael.github.io/tags/Software-Analysis/"/>
    
  </entry>
  
  <entry>
    <title>NJU静态程序分析(1-Data Flow Analysis)</title>
    <link href="http://haotianmcihael.github.io/2021/05/04/NJU%E9%9D%99%E6%80%81%E7%A8%8B%E5%BA%8F%E5%88%86%E6%9E%90-1-Data-Flow-Analysis/"/>
    <id>http://haotianmcihael.github.io/2021/05/04/NJU静态程序分析-1-Data-Flow-Analysis/</id>
    <published>2021-05-03T16:12:53.000Z</published>
    <updated>2022-02-10T03:37:11.339Z</updated>
    
    <content type="html"><![CDATA[<h3 id="abstract">Abstract</h3><blockquote><p>编译器中大部分机器无关的全局优化是基于<code>Data-flow Analysis</code>实现的。数据流分析技术从代码中收集程序语义相关的信息，是程序分析的基础。</p><p><strong>In each data-flow analysis application, we associate with every program point a data-flow value that represents an abstraction of the set of all possible program states that can be observed for that point.</strong></p><p>在工业界的<a href="https://www.zhihu.com/question/41959902" target="_blank" rel="noopener">主流编译器</a>中，<code>C</code>系的<code>LLVM</code>，<code>Java</code>系的<code>HotSpot</code>都是基于<code>SSA</code>做数据流分析而不是传统的<code>lattice</code>。南大这节课中是基于<code>Lattice</code>来讲解过程内<code>Intra-procedural</code>，无别名<code>no aliases</code>的经典数据流分析框架。</p></blockquote><a id="more"></a><h3 id="applications">Applications</h3><h4 id="overview">Overview</h4><blockquote><h5 id="cfg">CFG</h5><p>分析框架都是建立在<code>control-flow-graph</code>上的，这是一种<code>IR</code>——由<code>basic-blocks</code>通过<code>control-flows</code>连接而成，可以分别看成是<code>CFG</code>的<code>nodes</code>和<code>edges</code>。 如何通过源码（单个函数）生成<code>CFG</code>见课件。</p><p><img src="1.png" width="50%" height="50%" alt=""></p><h5 id="abstract-1">Abstract</h5><p>数据流分析用来描述所谓<strong>数据</strong>如何在<strong>CFG</strong>中流动。这里的<code>data</code>指的是程序语义信息的一种<code>abstraction</code>，<code>CFG</code>中的<code>nodes</code>和<code>edges</code>分别映射<code>transfer-function</code>和<code>control-flow handling</code>。所以整个分析是建立在一套成熟的理论框架之上的。</p><ul><li><code>data(application-specific)</code>: 不同的数据流分析应用会有不同的抽象值<code>value</code>，比如分析变量数值时：<code>+;-;0;UND;UNKNOW;</code><ul><li><code>domain</code>: 抽象数据流的值域即所有<code>value</code>定义的集合</li></ul></li><li><code>transfer-functions</code>:不同的程序会有不同的<code>CFG</code>表示，进一步需要深刻<code>CFG</code>中<code>statements</code>，<code>basic-blocks</code>在被映射成<code>transfer-functions</code>时的区别和联系</li><li><code>control-flows</code>:控制流基本上只有三个——顺序，汇聚以及分流</li></ul><h5 id="safety-analysis">safety analysis</h5><p>大部分的分析框架要求分析结果是<code>Sound</code>的即<code>over-approximation</code>。而实际上并非所有如此——这里正式将所有分析分成两类：<code>may analysis</code>和<code>must analysis</code>。</p><ul><li><code>may analysis</code>: 要求分析结果中必须全部包含所有正确答案，换个角度理解就是<strong>不放过任何一个动态运行时的可能出错的行为</strong>——一旦发现就要报错，即<code>over</code>的概念</li><li><code>must analysis</code>: 要求分析结果一定是正确的，即不能有误报，即<code>under</code>的概念</li></ul><p>而<code>over</code>和<code>under</code>在不同的应用中都代表了各自最<code>safe</code>的结果，因此所有的静态程序分析可以总结成：<code>abstraction</code>和<code>safe-approximation</code>两个阶段，不同的数据流分析有不同的<code>abstraction</code>和<code>safe-approximation strategies</code>即不同的<code>transfer-functions</code>和<code>control-flow handlings</code>。</p></blockquote><h4 id="preliminaries">Preliminaries</h4><blockquote><h5 id="input-and-output-states">Input and Output States</h5><p>所有的操作对象都是在一个<code>CFG</code>中。</p><p><code>statements</code> 是<code>CFG</code>中的执行的最小单位——每一条<code>statements</code>的执行都会改变程序的状态。</p><p><strong>每一个<code>statements</code>前后都有自己的<code>program point</code>分别代表<code>input/output state</code>，记作<code>IN[S_i]/OUT[S_i]</code>。又因为一个<code>basic-blocks</code>中的所有<code>statements</code>可以结合在一起（因为BB内部只能顺序执行）故每个<code>basic-blocks</code>开始和结束也有自己的<code>program point</code>，记作<code>IN[B_i]/OUT[B_i]</code>。</strong></p><p><code>control flow</code>表示了各个<code>basic-blocks</code>的执行顺序关系。分为三种：顺序，汇聚和分流</p><p><img src="2.png" width="50%" height="50%" alt=""></p><ul><li>顺序：<code>IN[s2] = OUT[s1]</code></li><li>汇聚: <code>IN[s2] = OUT[s1] ^ OUT[s3]</code><ul><li>操作符<code>^</code>指的是<code>meet operator</code>，不同的算法有不同操作比如并集，交集等</li></ul></li><li>分流: <code>IN[s2] = IN[s3] = OUT[s1]</code></li></ul><p><strong>需要关注的是每个BB或者Statement的<code>program point</code>，数据流分析会给每个<code>program point</code>一个<code>data-flow value</code>来抽象出当前位置程序的部分语义信息，再进一步利用各种<code>safe-approximation-directed constraints</code>以达到分析目的，这里的<code>constraints</code>具体指的是不同的<code>transfer functions</code>和<code>control flow handlings</code>。</strong></p><h5 id="notations-for-transfer-functions-constraints">Notations for Transfer Functions' Constraints</h5><ul><li>Forward Analysis： OUT[s] = F_s(IN[s]),其中F_s就是针对具体输入的<code>Transfer Function</code>，前向分析是从输入推导输出</li></ul><p><img src="3.png" width="50%" height="50%" alt=""></p><ul><li>Backward Analysis：IN[s] = F_s(OUT[s]),后向分析是从输出推导输入</li></ul><p><img src="4.png" width="50%" height="50%" alt=""></p><h5 id="notations-for-control-flows-constraints">Notations for Control Flows' Constraints</h5><h6 id="control-flow-within-a-bb">control flow within a BB</h6><p>在一个BB内部，<code>IN[S_(i+1)] = OUT[S_(i)], for all i = 1, 2,...n-1</code>——这个很好理解，一个BB中的<code>statements</code>只能顺序执行故只会有一个输入输出。</p><h6 id="control-flow-among-bbs">control flow among BBs</h6><p>对于一个<code>basic block</code>来说：<code>IN[B] = IN[S_1];   OUT[B] = OUT[S_n]</code>。 而BB之间的执行不仅仅是顺序关系。还有汇聚和分流的执行顺序。</p><p>首先是汇聚：<code>OUT[B] = F_b(IN[B])</code>，其中<code>F_b = F_sn * ...* F_s2 * F_s1</code>也就是一个BB的的<code>Transfer Function</code>由内部<code>statements</code>各自的<code>transfer function</code>组成，<code>IN[B] = ^ OUT[P]</code>，表示所有汇聚的输出的并集都成为下一个的输入，这里的<code>^</code>就是表示并集的<code>meet operator</code>而<code>P</code>表示<code>B</code>的所有前驱<code>basic blocks</code>。</p><p><img src="5.png" width="50%" height="50%" alt=""></p><p>其次是分流：<code>IN[B] = F_b(OUT[B])</code>;<code>OUT[B] = ^IN[S]</code>这里表示<code>B</code>的输出都成为所有<code>S</code>的输入，<code>S</code>是<code>B</code>的所有后继。</p><p><img src="6.png" width="50%" height="50%" alt=""></p><p>有了前面的基础，下面先介绍三种经典的数据流分析算法：</p></blockquote><h4 id="reaching-definitions-analysis">Reaching Definitions Analysis</h4><blockquote><h5 id="定义">定义</h5><p>A definition d at <strong>program point</strong> p reaches a point q if <strong>there is</strong> a path from p to q such that d is not &quot;killed&quot; along that path.(A definition of a variable is a statement that assigns a value to v).</p><p><img src="7.png" width="50%" height="50%" alt=""></p><p>首先<strong><code>definition</code>-定义</strong>指的是对变量<code>v</code>进行赋值的语句。一般来讲<strong>定义</strong>都是从一个<code>program point</code>上完成的。</p><p>在p点上的，一个对变量<code>v</code>的<code>definition</code>能<code>reach</code>到某一个新的点q，取决于在控制流上是否<strong>存在</strong>一条从p到q的路径，同时在这条路径上该变量<code>v</code>不会被重新定义，就说是这个<code>definition</code>可以reach到q点。</p><h5 id="分析类型">分析类型</h5><p>从定义的<code>there is</code>可以看出，这是一个<code>may analysis</code>——即控制流上从p到q的所有路径而言，只需要有一条路径没有被重新定义，那这两个<code>program point</code>之间就是<code>Reached</code>的。并且还知道这是一个<code>forward analysis</code>——因为是前面的<code>program point</code>上的<code>definition</code>可以<code>reach</code>到后面的<code>program point</code>。</p><h5 id="应用">应用</h5><p>该分析可以用来<strong>检查未定义的变量</strong>。对于所有需要检测的变量，在<code>CFG</code>的<code>entry</code>入口处都赋值为<code>dummy definition</code>——实际上就是一个<code>undefined</code>的标签，如果变量<code>v</code>的可以<strong>到达</strong>某一个使用点，那这个变量就有可能是未定义前使用。</p><h5 id="细节">细节</h5><h6 id="data-flow-valuesfact">Data Flow Values/Fact</h6><p>首先数据流分析需要对分析的对象进行抽象，<strong>这里使用<code>BitVector</code>来表示一个<code>CFG</code>（即一段过程内程序中）上所有变量的<code>definition</code>。</strong>（注意关注对象<code>definition</code>不是变量本身，而是定义这个变量的所有<code>program point</code>。）</p><h6 id="transfer-function">Transfer Function</h6><p>根据<code>IN</code>求出每一个<code>statemetns/basic blocks</code>的<code>OUT</code>的。由该分析的目的可以得出：<code>OUT[B] = gen_B ∪ (IN[B] - kill_B)</code>，即对于每一个<code>program point</code>来说，其<code>OUT</code>就是在该点处新增加的对变量的<code>definitions</code>除掉别处这些变量的<code>definitions</code>。</p><h6 id="control-flow">Control Flow</h6><p>根据上一个<code>statements/basic blocks</code>的<code>OUT</code>求出下一个的<code>IN</code>。因为作为<code>may</code>分析，其不放过任何一个运行时的行为，所以针对所有留到该<code>IN</code>的<code>OUT</code>都要接收。即<code>IN[B] = ∪_p(p是B所有的前驱结点)OUT[P]</code>。</p><h5 id="算法">算法</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">&gt; <span class="comment">/*</span></span><br><span class="line"><span class="comment">&gt; INPUT: CFG(kill_B and gen_B computed for each basic blocks B)</span></span><br><span class="line"><span class="comment">&gt; OUTOUT: IN[B] and OUT[B] for each basic block B</span></span><br><span class="line"><span class="comment">&gt; */</span></span><br><span class="line">&gt; OUT[entry] = <span class="literal">nullptr</span>;</span><br><span class="line">&gt; <span class="keyword">for</span> (each basic block B\entry)</span><br><span class="line">&gt;   OUT[B] = <span class="literal">nullptr</span>;</span><br><span class="line">&gt; </span><br><span class="line">&gt; <span class="keyword">while</span>(changes to ant OUT occur) </span><br><span class="line">&gt;   <span class="keyword">for</span>(each basic blocks B\entry) &#123;</span><br><span class="line">&gt;       IN[B] = ∪_OUT[P];</span><br><span class="line">&gt;       OUT[B] = gen_B ∪ (IN[B] - kill_B);      </span><br><span class="line">&gt;     &#125;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>这是非常典型的<strong>不动点算法</strong>，如果我们简化模型仅仅只是关注每一个<code>basic blocks</code>的<code>IN</code>和<code>OUT</code>,会发现对于有限的<code>definition</code>集合，在经过多次迭代之后，正向的增加<code>gen/survivors</code>一定会导致<code>OUT</code>达到一个定值。</p></blockquote><h4 id="live-variables-analysis">Live Variables Analysis</h4><blockquote><h5 id="定义-1">定义</h5><p>Live variables analysis tells whether the value of variable v at program point p <strong>could be used </strong>along some path in CFG starting at p. If so, v is live at p; otherwise, v is dead at p.</p><p><img src="8.png" width="50%" height="50%" alt=""></p><p>分析<strong>变量<code>v</code>在一个<code>program point</code>-p上是否是活跃的</strong>，取决于是否存在一条从p点开始一直到程序执行结束的路径，该路径中有使用过<code>v</code>的地方，<strong>并且从p点到使用点期间<code>v</code>不可以被重新定义</strong>——则说明该变量<code>v</code>在p点是活跃的。</p><h5 id="分析类型-1">分析类型</h5><p>根据定义可以知道这是一个<code>may</code>分析，该变量只要有使用点则分析结果就是活跃的。又因为分析的过程是从后往前的故该分析也是一个<code>backward analysis</code>。</p><h5 id="应用-1">应用</h5><p>该分析可以用来完成<strong>寄存器分配</strong>。比如在执行到程序的某一点需要将操作数加载到其中一个寄存器中——而这时候所有的<code>registers</code>都满了。如何选择合适的寄存器呢？如果选择的是一个<code>dead</code>不活跃的寄存器值（未来不会被用到），那就可以直接替换掉。而如果这个寄存器中的变量值是活跃的，那后面再次使用到的时候就需要把它再次从内存读到寄存器，浪费了资源。</p><h5 id="细节-1">细节</h5><h6 id="data-flow-valuefact">Data Flow Value/Fact</h6><p>该分析关注的对象，<strong>就是变量本身</strong>，使用<code>BitVector</code>表示。</p><h6 id="transfer-function-1">Transfer Function</h6><p><code>IN[B] = use_B ∪ (OUT[B] - def_B)</code>。这里的<code>use_B</code>指的是<strong>所有在重新定义之前变量的使用点，</strong>通过<code>backward</code>的思路来分析，对于一个<code>basic block</code>，给出其<code>OUT</code>来得到其<code>IN</code>需要关注在BB中变量是否是满足活跃的前提要求的。</p><p><img src="9.png" width="50%" height="50%" alt=""></p><h6 id="control-flow-1">Control Flow</h6><p>因为作为<code>may</code>分析，其不放过任何一个运行时的行为，所以针对所有留到该<code>IN</code>的<code>OUT</code>都要接收。即<code>IN[B] = ∪_s(s是B所有的前驱结点)OUT[s]</code>。</p><h5 id="算法-1">算法</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">&gt; <span class="comment">/*</span></span><br><span class="line"><span class="comment">&gt; INPUT: CFG(def_B and use_B computed for each basic blocks B)</span></span><br><span class="line"><span class="comment">&gt; OUTOUT: IN[B] and OUT[B] for each basic block B</span></span><br><span class="line"><span class="comment">&gt; */</span></span><br><span class="line">&gt; IN[<span class="built_in">exit</span>] = <span class="literal">nullptr</span>;</span><br><span class="line">&gt; <span class="keyword">for</span> (each basic block B\<span class="built_in">exit</span>)</span><br><span class="line">&gt; IN[B] = <span class="literal">nullptr</span>;</span><br><span class="line">&gt; </span><br><span class="line">&gt; <span class="keyword">while</span>(changes to ant IN occur) </span><br><span class="line">&gt; <span class="keyword">for</span>(each basic blocks B\<span class="built_in">exit</span>) &#123;</span><br><span class="line">&gt;    OUT[B] = ∪_IN[S];</span><br><span class="line">&gt;    OUT[B] = use_B ∪ (OUT[B] - def_B);      </span><br><span class="line">&gt;  &#125;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>将两个算法的<code>Transfer Func</code>放到一起对比：</p><ul><li>RDA: <code>OUT[B] = gen_B ∪ (IN[B] - kill_B)</code></li><li>LVA: <code>IN[B] = use_B ∪ (OUT[B] - def_B)</code></li></ul><p>会发现很多的数据流分析算法都是<code>GEN/KILL Problem</code>，统计一下公式就是<code>OUT = gen ∪ (IN - kill)</code>。</p></blockquote><h4 id="available-expressions-analysis">Available Expressions Analysis</h4><blockquote><h5 id="定义-2">定义</h5><p>An expression x op y is available at program point p if (1) <strong>all paths </strong>from the entry to p must pass through the evaluation of x op y, and (2) <strong>after the last evaluation of x op y, there is no redefinition of x or y</strong>.</p><p><img src="10.png" width="50%" height="50%" alt=""></p><p>在<code>CFG</code>上的所有的复合型表达式都会被拆成三地址码。<strong>判断一个<code>expression</code>-<code>x op y</code>在p点是<code>available</code>的</strong>，取决于从<code>entry</code>到<code>p</code>点所有的路径都必须有该表达式的<code>evaluation</code>求值，并且在最后一个求值之后不能对<code>x/y</code>重新赋值，即就说这个表达式在p点事<code>available</code>的。</p><h5 id="分析类型-2">分析类型</h5><p>必须保证所有的<code>path</code>都有<code>evaluation of expression</code>分析结果才是<code>available</code>。因此这是一个<code>must</code>分析，即不能有误报但可以有漏报，比如下图中的情况，假设<code>x = 3</code>前后都成立那么实际上这个表达式也是可以被优化的，但是根据该<code>must</code>分析的定义这个分析结果是<code>not avaliable</code>的。</p><p><img src="11.png" width="50%" height="50%" alt=""></p><h5 id="应用-2">应用</h5><p>该分析可以用来进行<strong>公共子表达式删除</strong>，即用<code>the last evaluation of available expression</code>直接替换该表达式，从而优化代码。</p><h5 id="细节-2">细节</h5><h6 id="data-flow-valuefact-1">Data Flow Value/Fact</h6><p>顾名思义就是程序中所有的<code>expression</code>。</p><h6 id="transfer-function-2">Transfer Function</h6><p>根据定义<code>OUT[B] = gen_B ∪ (IN[B] - kill_B)</code>，对于所有的输入的表达式，产生的新表达式并且删除掉在其变量被重新赋值的表达式。</p><h6 id="control-flow-2">Control Flow</h6><p>因为是<code>must</code>分析对于所有的情况都要考虑，所以<code>IN[B] = ∩(p:predecessors of B)OUT[p]</code>。</p><h5 id="算法-2">算法</h5><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">&gt; /*</span><br><span class="line">&gt; <span class="type">INPUT</span>: <span class="type">CFG</span>(kill_B <span class="keyword">and</span> gen_B computed <span class="keyword">for</span> each basic blocks <span class="type">B</span>)</span><br><span class="line">&gt; <span class="type">OUTOUT</span>: <span class="type">IN</span>[<span class="type">B</span>] <span class="keyword">and</span> <span class="type">OUT</span>[<span class="type">B</span>] <span class="keyword">for</span> each basic block <span class="type">B</span></span><br><span class="line">&gt; */</span><br><span class="line">&gt; out[entry] = nullptr;</span><br><span class="line">&gt; <span class="keyword">for</span> (each basic block <span class="type">B</span>\entry)</span><br><span class="line">&gt; <span class="type">OUT</span>[<span class="type">B</span>] = nullptr;</span><br><span class="line">&gt; </span><br><span class="line">&gt; <span class="keyword">while</span>(changes <span class="keyword">to</span> ant <span class="type">OUT</span> occur) </span><br><span class="line">&gt; <span class="keyword">for</span>(each basic blocks <span class="type">B</span>\entry) &#123;</span><br><span class="line">&gt;    <span class="type">IN</span>[<span class="type">B</span>] = ∪_IN[<span class="type">P</span>];</span><br><span class="line">&gt;    <span class="type">OUT</span>[<span class="type">B</span>] = gen_B ∪ (<span class="type">OUT</span>[<span class="type">B</span>] - kill_B);      </span><br><span class="line">&gt;  &#125;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p><strong>对于这三种分析来说，如何理解<code>may/must</code>，<code>false positive/false negative</code>和在所有<code>CFG</code>上程序的运行时行为之间的关系是很关键的问题。</strong></p></blockquote><h3 id="foundations">Foundations</h3><h4 id="mathematics-prerequisite">Mathematics Prerequisite</h4><blockquote><h5 id="partial-order">Partial Order</h5><p>定义<code>poset</code>偏序集<code>(P, ≼)</code>表示一个集合<code>p</code>，符号<code>≼</code>定义在该集合的<code>partial ordering</code>有如下性质：</p><ul><li><em>Reflexivity</em>. ∀ x∊P, x≼x<br></li><li><em>Antisymmetry.</em> ∀ x,y∊P, x≼y ∧ y≼x ⟹ x=y.</li><li><em>Transitivity</em>. ∀ x,y,z∊P, x≼y ∧ y≼z ⟹ x≼z.</li></ul><p>如何理解偏序-<code>partial ordering</code>？<strong>在集合<code>p</code>中定义了一种<code>order</code>，但并不是所有两个元素之间都必须满足这种<code>order</code>——即集合中两个元素之间可以是<code>incomparable</code>的</strong>。比如<code>power set</code>是非常典型的<code>poset</code>。</p><p><img src="12.png" width="50%" height="50%" alt=""></p><h5 id="upper-and-lower-bounds">Upper and Lower Bounds</h5><p>给定偏序集<code>(P, ≼)</code>以及该集合的一个子集<code>S ⊆ P</code>。我们使用集合<code>P</code>中的<strong>一个元素<code>u ∊ P</code></strong>来定义<strong>集合<code>S</code>的<code>upper bound</code>和<code>lower bound</code>:</strong></p><ul><li>∀x∊S, x≼u ⟹ u∊P is an <em>upper bounds</em> of S</li><li>∀x∊S, l≼x ⟹ l∊P is an <em>lower bound</em> of S</li></ul><p>在此基础上，我们进一步定义<strong>集合<code>S</code>的<code>least upper bound(lub or join)</code>简写为<code>⊔S</code>，以及<code>greatest lower bound(glb or meet)</code>简写为<code>⊓S</code>:</strong></p><ul><li>for every <em>upper bound</em> of S, say u: <code>⊔S ≼ u</code></li><li>for every <em>lower bound</em> of S, say l:<code>l ≼ ⊓S</code></li></ul><p>根据<code>⊔S/⊓S</code>的定义，如果集合<code>S</code>中只有两个元素<code>a and b</code>。那么：</p><ul><li><code>⊔S</code> ≡ <code>a ⊔ b</code>(a和b的并集)</li><li><code>⊓S</code> ≡ <code>a ⊓ b</code>（a和b的交集）</li></ul><p><strong>不是所有的偏序集都有<code>⊓S/⊔S</code>，但是如果一个偏序集同时有<code>⊔S/⊓S</code>的话，那该偏序集就是唯一的。</strong></p><h5 id="lattic">Lattic</h5><p>对于一个偏序集<code>P</code>，如果集合中<strong>任意两个元素（构成的子集S）</strong><code>a</code>和<code>b</code>，它们的<code>a⊔b</code>和<code>a⊓b</code>都存在，则这个偏序集就是一个<code>lattice</code>。</p><ul><li>Given poset (P, ≼), ∀a,b∊P, if ∃ a⊔b, ∃ a⊓b ∃ then P is calles a lattice</li></ul><p>在此基础上，如果这个偏序集P中的任意两个元素只有<code>a⊔b</code>或者<code>a⊓b</code>中的一个，则这个偏序集分别称为<code>join semilattice</code>和<code>meet semilattice</code>。</p><h5 id="complete-lattice">Complete Lattice</h5><p>对于一个偏序集<code>P</code>，如果集合中<strong>任意一个子集S</strong>都存在<code>⊔S</code>和<code>⊓S</code>，则这个偏序集便称为<code>complete lattice</code>。<strong>全格和格的区别主要在于，格对其子集的要求是只需要有两个元素而全格对子集元素的数量没有限制。</strong></p><p>在此基础上，对于一个<code>complete lattice</code>，都有存在：</p><ul><li>a greatest element ⊤ = ⊔P called <font color="red">top</font></li><li>a least element ⊥ = ⊓P called <font color="blue">bottom</font></li></ul><p><strong>每一个<code>finite lattice</code>都是一个<code>complete lattice</code>。但是一个<code>complete lattice</code>却不一定是<code>finite lattice</code></strong>。</p><h5 id="product-lattice">Product Lattice</h5><p>给定多个<code>lattice</code>-<code>Li = (Pi, ≼i)</code>的笛卡尔积构成的一个新的<code>lattice，</code>如果这些<code>lattice</code>每一个都有<code>lub</code>和<code>glb</code>的话<strong>(这里没有说明子集S的个数主要是因为它可以是只有两个元素的普通<code>lattice</code>也可以是包括任意元素的<code>complete lattice</code>，换句话说如果一个<code>product lattice</code>是<code>complete lattice</code>的笛卡尔积，那么它本身也是一个<code>complete lattice</code>)</strong>则称其积为<code>product lattice</code>并满足下面性质:</p><ul><li>P = P1 x P2...x Pn</li><li>(x1, ..., xn) ≼ (y1, ..., yn) ≡ (x1 ≼ y1)∧...∧(xn≼yn)</li><li>(x1, ..., xn) ⊔ (y1, ..., yn) ≡ (x1 ⊔ y1)∧...∧(xn ⊔ yn)</li><li>(X1, ..., xn) ⊓ (y1, ..., yn) ≡ (x1 ⊓ y1)∧...∧(xn ⊓ yn)</li></ul><p>即一个<code>product lattice</code>中的每一个元素<code>lattice</code>满足的性质它都满足。</p></blockquote><h4 id="data-flow-analysis-framework-via-lattice">Data Flow Analysis Framework via Lattice</h4><blockquote><h5 id="iterative-algorithm">Iterative Algorithm</h5><p>回顾前面三个具体的数据流分析算法，即相应的<code>may/must</code>分析从各自的初始状态出发，<strong>经过不断迭代（Transfer Function/Control Flow）</strong>最终到达<strong>不动点</strong>——整个程序的数据流不会发生变化，算法停止。</p><p>这里从更具体的角度来理解这个算法：</p><p>给定一个<code>CFG</code>，其中有<code>k</code>个节点(这里简化成为只带一个<code>statement</code>的<code>basic block</code>)。<strong>而迭代算法在每次迭代中更新这k个节点的输出<code>OUT</code></strong>，引入一个<code>k-tuple</code>:</p><ul><li>(OUT[n_1], OUT[n_2], ..., OUT[n_k]) ≡ (V_1 x V_2 ... x V_k)</li></ul><p><strong><code>k-tuple</code>就是每一次迭代后所有节点输出值的<code>product</code>记为<code>V-k</code>。</strong>而更具体的解释就是<strong>不同的数据流分析算法在每次迭代中通过应用各种<code>Transfer Function/Control Flow</code>来更新<code>k-tuple</code>的值——可以将每一次迭代抽象出成为一个函数:<code>F: V-k  ⟶  V-k</code></strong>。而当最后一次迭代的输出没有发生变化便到达了不动点。</p><p><img src="13.png" width="50%" height="50%" alt=""></p><p>This general <em>iterative algorithm</em> produces a solution to data flow analysis.</p><h5 id="data-flow-analysis-framework-via-lattice-1">Data Flow Analysis Framework via Lattice</h5><p>定义一个数据流分析框架主要有三个部分(<font color="red">D</font>, <font color="green">L</font>, <font color="blue">F</font>):</p><ul><li><font color="red">D</font>: 指的是分析的方向是<code>backwwards/forwards</code></li><li><font color="green">L</font>: 核心部分是<code>lattice</code>，代表所分析的<strong>数据流值域</strong>，和<strong>一个<code>meet⊓</code>或者<code>join⊔</code>运算符</strong>（一般一个数据流分析算法只会用到其中一个运算符——故其实是<code>semilattice</code>）</li><li><font color="blue">F</font>: 指的是分析中的<code>Transfer Functions</code></li></ul><p><strong>分析框架就可以看成是在一个<code>lattice</code>的值上不停的迭代<code>transfer functions</code>和<code>join/meet operations</code>。</strong></p><p>##### Fixed Point Theorem</p><p>在学完<strong><code>Iterative Algorithm</code></strong>以及<strong><code>Data FLow Analysis Framework</code></strong>之后，我们需要进一步理解这两者之间的关系，并更深层次的理解<strong>在<code>lattice</code>上定义的函数的不动点原理-<code>Fixed Point Theorem</code></strong>，首先对<code>Iterative Algorithm</code>提出一些核心的问题:</p><ul><li>迭代算法能保证<strong>一定会到达不定点么？</strong></li><li>假设可以到达不动点，那这个<strong>不动点是唯一的么</strong>，或者<strong>通过迭代算法到达的不动点是最精确的么？</strong></li><li>假设可以到达不动点，那<strong>什么时候可以到达？</strong></li></ul><h5 id="section"></h5></blockquote><h4 id="maymust-analysis-on-lattice">May/Must Analysis On Lattice</h4><h4 id="mop-and-distributivity">MOP and Distributivity</h4><h4 id="constant-propagation">Constant Propagation</h4><h4 id="worklist-algorithm">Worklist Algorithm</h4>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;编译器中大部分机器无关的全局优化是基于&lt;code&gt;Data-flow Analysis&lt;/code&gt;实现的。数据流分析技术从代码中收集程序语义相关的信息，是程序分析的基础。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;In each data-flow analysis application, we associate with every program point a data-flow value that represents an abstraction of the set of all possible program states that can be observed for that point.&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在工业界的&lt;a href=&quot;https://www.zhihu.com/question/41959902&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;主流编译器&lt;/a&gt;中，&lt;code&gt;C&lt;/code&gt;系的&lt;code&gt;LLVM&lt;/code&gt;，&lt;code&gt;Java&lt;/code&gt;系的&lt;code&gt;HotSpot&lt;/code&gt;都是基于&lt;code&gt;SSA&lt;/code&gt;做数据流分析而不是传统的&lt;code&gt;lattice&lt;/code&gt;。南大这节课中是基于&lt;code&gt;Lattice&lt;/code&gt;来讲解过程内&lt;code&gt;Intra-procedural&lt;/code&gt;，无别名&lt;code&gt;no aliases&lt;/code&gt;的经典数据流分析框架。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="Java" scheme="http://haotianmcihael.github.io/categories/Java/"/>
    
    
      <category term="PL" scheme="http://haotianmcihael.github.io/tags/PL/"/>
    
      <category term="Software-Analysis" scheme="http://haotianmcihael.github.io/tags/Software-Analysis/"/>
    
  </entry>
  
  <entry>
    <title>NJU静态程序分析(0-Introduction)</title>
    <link href="http://haotianmcihael.github.io/2021/05/02/NJU%E9%9D%99%E6%80%81%E7%A8%8B%E5%BA%8F%E5%88%86%E6%9E%90-0-Introduction/"/>
    <id>http://haotianmcihael.github.io/2021/05/02/NJU静态程序分析-0-Introduction/</id>
    <published>2021-05-01T19:00:36.000Z</published>
    <updated>2022-01-03T07:25:59.274Z</updated>
    
    <content type="html"><![CDATA[<h3 id="abstract">Abstract</h3><blockquote><p>编译器作为工业界对PL理论的部分实现——和编程语言，运行时环境一样只是机器解释世界的一个角度。而当我解锁了从更加PL的方式看待<code>计算</code>这件事情之后，又打开了一扇通往新世界的大门。过去的几十年里<code>Programming Language</code>的内核并没有变化多少——但是程序本身却随着应用需求变的更加庞大和复杂。而静态程序分析作为PL的重要应用，其目的就在于在编译期确保程序的可靠性，安全性等各种性质。</p><p>​ <strong>Static Analysis analyzes a program P to reasono about its behaviors and determines whether it satisfies some properties before running P.</strong></p><p>​ 主要的学习资源是<a href="https://pascal-group.bitbucket.io/teaching.html" target="_blank" rel="noopener">NJU-PASCAL团队的静态分析课程</a>以及<a href="https://book.douban.com/subject/3774682/" target="_blank" rel="noopener">龙书</a>和<a href="https://book.douban.com/subject/1400374/" target="_blank" rel="noopener">鲸书</a>中的部分章节。</p></blockquote><a id="more"></a><h3 id="static-analysis">Static-Analysis</h3><blockquote><p>​ PL的大框架大体上可以分成三个部分:</p><ul><li>Theory</li></ul><p>包括如何设计一个语言内核，一个类型系统，如何完成语义分析，如何形式化等等不同的理论，更多是作为学术研究的课题</p><ul><li>Environment</li></ul><p>针对特定场景设计的编译器和运行时环境等</p><ul><li>Application</li></ul><p>程序分析，程序合成，程序验证</p><p>​ 而静态程序分析主要关注在程序的<strong>Reliability</strong>比如在程序运行前检测到空指针异常，内存泄漏等问题防止程序在运行过程中报运行时错误；<strong>Security</strong>比如隐私数据泄露，注入攻击<code>injection attack</code>等；<strong>Optimization</strong>编译优化等方面。</p></blockquote><h3 id="rices-theorem">Rice‘s Theorem</h3><blockquote><p>​ Any non-trivial property of the behavior of programs in a <strong>recursively enumerable</strong> language is undecidable.</p><p>​ 这个定理有点哥德尔不完备定理的感觉——不存在一个完美的分析算能准确预测出程序的各种关键行为，即No Silver Bullet。这也引出了在实践中最重要的一个评判标准Sound和Complete。</p><p>​ 这里的<code>recursively enumerable</code>递归可枚举是<strong>图灵完备</strong>的另一种说法，简写为<code>r.e.</code>。</p></blockquote><h3 id="sound-and-complete">Sound and Complete</h3><blockquote><p>​ 假设一个程序中存在10个<code>unknown error</code>，一套分析框架不可能精确定位到这十个。而是在其值域范围内波动——不仅仅包含这10的集合就是<code>Sound</code>，只包含这10个的子集就是<code>Complete</code>。</p><p><img src="1.png" width="50%" height="50%" alt=""></p><p>​ 伴随着这一对概念还有一对——<code>false negatives</code>和<code>false positives</code>。</p><ul><li>False negatives</li></ul><p>就是<code>Compromise soundness</code>也就是缩小原来分析的量，这样会更加的<code>complete</code>——便会产生<strong>漏报</strong></p><ul><li>False positives</li></ul><p>就是<code>Compromise completeness</code>也就是增大原来分析的量，这样会更加的<code>sound</code>——便会产生<strong>误报</strong></p><p>其实这里为什么要引入这一对概念我没有太理解……有点往回绕的意思。 这个图得好好理解一下。</p><p><img src="2.png" width="50%" height="50%" alt=""></p><p>在实践中，用的更多的标准是<code>compromising completeness</code>也就是要让结果更加的<code>sound</code>但是损失了精确度。<strong>换句话讲，分析的结果一定要全面，但不一定精确。也就是说宁可误报一些分析行为，也不允许程序在运行时报异常——这样整个系统付出代价会更高。</strong></p><h4 id="例子">例子</h4><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">&gt; <span class="class"><span class="keyword">class</span> <span class="title">A</span>&#123;</span>&#125;;</span><br><span class="line">&gt; <span class="class"><span class="keyword">class</span> <span class="title">Base</span>&#123;</span></span><br><span class="line">&gt;   A fld;</span><br><span class="line">&gt; &#125;;</span><br><span class="line">&gt; <span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> A&#123;&#125;;</span><br><span class="line">&gt; <span class="class"><span class="keyword">class</span> <span class="title">C</span> :</span> A&#123;&#125;;</span><br><span class="line">&gt; </span><br><span class="line">&gt; Base a = <span class="keyword">new</span> Base();</span><br><span class="line">&gt; <span class="keyword">if</span>(condition) &#123;</span><br><span class="line">&gt;   B b = <span class="keyword">new</span> B();</span><br><span class="line">&gt; a.fld = b;</span><br><span class="line">&gt; &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">&gt;   C c = <span class="keyword">new</span> D();</span><br><span class="line">&gt; a.fld = c;</span><br><span class="line">&gt; &#125;</span><br><span class="line">&gt; </span><br><span class="line">&gt; <span class="comment">// case</span></span><br><span class="line">&gt; B bTmp = (B)a.fld;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>可以看到上例中的case中进行了一次强制转换，如果控制流走的是<code>!condition</code>那么就会报错。如果分析器是<code>sound</code>的这里就应该检测出该程序的控制流错误——从而减少了BUG。</p><h4 id="tradeoff">tradeoff</h4><p>​ 因为<code>sound</code>会分析出比理论更多的结果，所以导致分析的速度会变慢。</p><p>​ Static Analysis ensure soundness, while making good trade-offs between analysis precision and analysis speed——分析过程的本质上也是一种速度和精度之间的权衡。</p></blockquote><h3 id="to-conclude">To Conclude</h3><blockquote><p>​ 大部分分析方式可以总结为下面的两个部分：</p><ul><li>Abstraction</li></ul><p>就是将具体的程序特征和行为进一步抽象成分析器的元符号，完成一定意义的所谓<strong>形式化</strong></p><ul><li><p>Over-approximation</p><ul><li>transfer functions</li></ul><p>In static analysis, transfer functions define how to evaluate different program statements on abstract values. Its defined according to &quot;analysis problem&quot; and the &quot;semantics&quot; of different program statements.</p><ul><li>control flows</li></ul></li></ul><p>程序的本质其实就是语句及语句之间的关联，映射到static analysis中就是（语句）<code>transfer functions</code>和（关联）<code>control flows</code>。在抽象的基础上，进一步颗粒化程序运行行为。比如<code>transfer functions</code>定义为程序中<code>statements</code>和程序异常行为的语义比如声明语句，除零异常等。而<code>control flows</code>定义了程序的控制流比如一些if-else分支。<strong>这样就能更加细粒度的完成程序行为的建模，然后在模型基础上完成分析器预定的sound的分析结果。</strong></p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;编译器作为工业界对PL理论的部分实现——和编程语言，运行时环境一样只是机器解释世界的一个角度。而当我解锁了从更加PL的方式看待&lt;code&gt;计算&lt;/code&gt;这件事情之后，又打开了一扇通往新世界的大门。过去的几十年里&lt;code&gt;Programming Language&lt;/code&gt;的内核并没有变化多少——但是程序本身却随着应用需求变的更加庞大和复杂。而静态程序分析作为PL的重要应用，其目的就在于在编译期确保程序的可靠性，安全性等各种性质。&lt;/p&gt;
&lt;p&gt;​ &lt;strong&gt;Static Analysis analyzes a program P to reasono about its behaviors and determines whether it satisfies some properties before running P.&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;​ 主要的学习资源是&lt;a href=&quot;https://pascal-group.bitbucket.io/teaching.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;NJU-PASCAL团队的静态分析课程&lt;/a&gt;以及&lt;a href=&quot;https://book.douban.com/subject/3774682/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;龙书&lt;/a&gt;和&lt;a href=&quot;https://book.douban.com/subject/1400374/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;鲸书&lt;/a&gt;中的部分章节。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="Java" scheme="http://haotianmcihael.github.io/categories/Java/"/>
    
    
      <category term="PL" scheme="http://haotianmcihael.github.io/tags/PL/"/>
    
      <category term="Software-Analysis" scheme="http://haotianmcihael.github.io/tags/Software-Analysis/"/>
    
  </entry>
  
  <entry>
    <title>函数式编程（Ocaml）</title>
    <link href="http://haotianmcihael.github.io/2021/04/18/%E5%87%BD%E6%95%B0%E5%BC%8F%E7%BC%96%E7%A8%8B%EF%BC%88Ocaml%EF%BC%89/"/>
    <id>http://haotianmcihael.github.io/2021/04/18/函数式编程（Ocaml）/</id>
    <published>2021-04-18T09:48:24.000Z</published>
    <updated>2021-12-31T10:03:53.726Z</updated>
    
    <content type="html"><![CDATA[<h3 id="abstract">Abstract</h3><blockquote><p>​ 最近在实验室实习的过程中学到了很多有意思的东西，对<code>Programming Languages</code>也有了更深的了解，<code>Ocaml</code>是一门支持多范式的函数式编程语言。<strong>程序描述计算，但是又不仅仅是计算</strong>。<code>Ocaml</code>的学习带给我的不仅仅是语言本身，这个系列记录了其中的部分心得:</p><ul><li>关于<code>scripting/imperative/object-oriented/functional PL</code>的基本概念<ul><li><code>Higher-order Functions</code></li><li><code>State-Full vs State-Free Computation</code></li><li><code>Modelling Objects and Closures</code></li><li><code>Exceptions/Continuations to Defer Control</code></li><li><code>Polymorphism</code></li><li><code>Partial Evaluation/Lazy Programming/Modules</code></li></ul></li><li>关于<code>reason about programs</code>的方法<ul><li><code>Type Checking</code></li><li><code>Induction</code></li><li><code>Operational Semantics</code></li><li><code>QuickCheck</code></li></ul></li><li>关于设计一门语言的基本规则</li></ul></blockquote><a id="more"></a><h3 id="fps-basic-concepts">FP's Basic Concepts</h3><blockquote><p><code>Ocaml is a statically typed functional programminng language</code>。函数式编程中，<code>function</code>永远是一等公民，程序中只有<code>expression</code>的概念，通过<code>functions</code>递归处理<code>values</code>来完成<code>computations</code>。<code>Ocaml</code>通过<code>pattern matching</code>模式匹配的方式来定义递归数据结构和函数，进而写出更简洁的程序。</p><p>在函数式中还有一个和<code>imperative PL.</code>中不同的概念称为<code>effect-free programming</code>。在命令式语言中，我们通过给<code>variables/fields</code>赋值来修改程序状态并将赋值行为的结果看成是<code>effects</code>。但是比如函数式就不会<code>explicitly</code>的分配内存，甚至没有<code>exception handling</code>来改变控制流状态——<strong>不支持<code>effect</code>被称为<code>pure functiional</code>比如<code>Haskell</code></strong>。<code>Ocaml</code>不是纯函数式语言。所以支持<code>effectful/state-full programming</code>和<code>pure FP</code>。</p><p>最后一个关键字就是<code>statically typed</code>静态类型。<code>Types statically approximate the runtime behaviour of an expression.</code>意思就是<code>static type checking</code>本身是在程序运行之前进行类型检查——通过<code>syntatic structure of expressions</code>。它可以保证通过静态类型检查的程序在运行时一定不会<code>core dump</code>。这样的<code>statically typed PL.</code>比如<code>Ocaml/Java/ML/Haskell/Scala等</code>也被称为是<code>type-safe</code>的。</p></blockquote><h4 id="expressions-names-values-and-types">Expressions, Names, Values and Types</h4><blockquote><p>在函数式中没有语句<code>statements</code>，最基本的<code>expression</code>就是<code>numbers/strings/booleans</code>。</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="number">2</span> + <span class="number">3</span>;;</span><br><span class="line">&gt; - : <span class="built_in">int</span> = <span class="number">5</span></span><br><span class="line">&gt; # <span class="number">4.0</span> /. <span class="number">2.0</span>;;</span><br><span class="line">&gt; - : <span class="built_in">float</span> = <span class="number">2.0</span></span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>上面代码中的格式为<code>&lt;name&gt; : &lt;type&gt; = &lt;value&gt;</code> 。我们将<code>;;</code>之前的式子称为<code>expressions</code>，将整个计算过程称为<code>evaluation</code>，而将计算的结果称为<code>values</code>。</p><p>注意在<code>Ocaml</code>中基础运算符并没有被重载，故浮点运算都是在后面有一个<code>,</code>。这主要是因为<code>Ocaml</code>是静态类型的，不会在运行时去根据运算符参数来推导重载的<code>operators</code>。</p><p>通过类型检查的程序，其<code>evaluation</code>要么更新到一个新的<code>state</code>要么报一个<code>built-in runtime error</code>。比如:</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="number">3</span> / <span class="number">0</span>;;</span><br><span class="line">&gt; <span class="type">Exception</span>: <span class="type">Division_by_zero</span></span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><h4 id="variables-bindings-and-functions">Variables, Bindings and Functions</h4><blockquote><p><code>Ocaml</code>是一个<code>call-by-value</code>的语言——<code>it binds values to variable names not expressions</code>。这里有一个<code>binding</code>的概念，区分于<code>assignment</code>。我们只是在<code>values</code>和<code>name</code>之间建立了“链接”——并没有新的<code>state</code>被创建——我理解和<code>imperative PL.</code>不同的应该是内存模型比如<code>C</code>中对局部变量是放在栈上的但是确实创建了内存。当然这种“链接”也是可以更新的。在<code>Ocaml</code>中用<code>let</code>表达式来建立一个绑定。</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">let</span> x = <span class="number">3</span> * <span class="number">3</span>;;</span><br><span class="line">&gt; <span class="keyword">val</span> x : <span class="built_in">int</span> = <span class="number">9</span></span><br><span class="line">&gt; # <span class="keyword">let</span> x = <span class="number">42</span>;;</span><br><span class="line">&gt; <span class="keyword">val</span> x : <span class="built_in">int</span> = <span class="number">42</span></span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>变量的作用域是根据<code>binding stack</code>来确定的。局部或者临时创建的<code>binding</code>都会被<code>push</code>进<code>binding stack</code>中，在不需要的时候会被<code>GC</code>清除。</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">let</span> k = <span class="number">4</span>;;</span><br><span class="line">&gt; <span class="keyword">val</span> k : <span class="built_in">int</span> = <span class="number">4</span></span><br><span class="line">&gt; # <span class="keyword">let</span> k = <span class="number">3</span> <span class="keyword">in</span> k * k ;;</span><br><span class="line">&gt; - : <span class="built_in">int</span> = <span class="number">9</span></span><br><span class="line">&gt; # k;;</span><br><span class="line">&gt; - : <span class="built_in">int</span> = <span class="number">4</span></span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>这里有一个新的<code>let &lt;name&gt; = &lt;expression 1&gt; in &lt;expression 2&gt;</code>结构——将<code>expression1</code>的<code>value</code>绑定到<code>name</code>上，然后继续使用这个新的绑定对<code>expression 2</code>进行<code>evalutions</code>。</p><p>在函数式中<code>let</code>不仅可以绑定基本类型，还可以绑定函数。</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&gt; <span class="keyword">let</span> area : <span class="built_in">float</span> -&gt; <span class="built_in">float</span> = <span class="keyword">function</span> r -&gt; pi *. r *. r;;</span><br><span class="line">&gt; <span class="keyword">let</span> area : <span class="built_in">float</span> -&gt; <span class="built_in">float</span> = <span class="keyword">fun</span> r -&gt; <span class="number">3.14</span> *. r *. r;;</span><br><span class="line">&gt; <span class="keyword">let</span> area (r:<span class="built_in">float</span>) = pi *. r *. r;;</span><br><span class="line">&gt; <span class="keyword">let</span> a1 = area(<span class="number">2.0</span>);;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>上面的三种写法是等价的，都得到函数类型<code>val area : float -&gt; float = &lt;fun&gt;</code>。<strong>有意思的是函数作为一种绑定，也是在<code>binding stack</code>中的，所以它只能看到过去的绑定：</strong></p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">let</span> (pi*<span class="built_in">float</span>) = <span class="number">3.0</span></span><br><span class="line">&gt; <span class="keyword">val</span> pi : <span class="built_in">float</span> = <span class="number">3.14</span></span><br><span class="line">&gt; # <span class="keyword">let</span> area (r:<span class="built_in">float</span>) = pi *. r *.r;;</span><br><span class="line">&gt; <span class="keyword">val</span> area : <span class="built_in">float</span> -&gt; <span class="built_in">float</span> = &lt;<span class="keyword">fun</span>&gt;</span><br><span class="line">&gt; # <span class="keyword">let</span> a1 = area(<span class="number">2.0</span>);;</span><br><span class="line">&gt; <span class="keyword">val</span> a1 : <span class="built_in">float</span> = <span class="number">12.0</span></span><br><span class="line">&gt; # <span class="keyword">let</span> (pi*<span class="built_in">float</span>) = <span class="number">4.0</span></span><br><span class="line">&gt; <span class="keyword">val</span> pi : <span class="built_in">float</span> = <span class="number">4.0</span></span><br><span class="line">&gt; # <span class="keyword">let</span> a2 = area(<span class="number">2.0</span>);;</span><br><span class="line">&gt; <span class="keyword">val</span> a2 : <span class="built_in">float</span> = <span class="number">12.0</span></span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>最后介绍一下递归函数。在<code>Ocaml</code>中递归函数使用<code>let rec</code>来声明。比如定义<code>factorial</code>:</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">exception</span> <span class="type">Domain</span>;;</span><br><span class="line">&gt; # <span class="keyword">let</span> <span class="keyword">rec</span> fact n = </span><br><span class="line">&gt; <span class="keyword">if</span> n &lt; <span class="number">0</span> <span class="keyword">then</span> raise <span class="type">Domain</span></span><br><span class="line">&gt; <span class="keyword">else</span> <span class="keyword">if</span> n = <span class="number">0</span> <span class="keyword">then</span> <span class="number">1</span></span><br><span class="line">&gt; <span class="keyword">else</span> n * fact(n - <span class="number">1</span>);;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>在函数式中所有的函数都可以被写成尾递归的形式然后进一步优化——这是因为尾递归函数的栈帧可以被优化掉。比如:</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">let</span> fact_tr n = </span><br><span class="line">&gt; <span class="keyword">let</span> <span class="keyword">rec</span> f n acc = </span><br><span class="line">&gt; <span class="keyword">if</span> n = <span class="number">0</span> <span class="keyword">then</span> acc <span class="keyword">else</span> f (n - <span class="number">1</span>) (n * acc)</span><br><span class="line">&gt; <span class="keyword">in</span></span><br><span class="line">&gt; f n <span class="number">1</span></span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><h4 id="data-types-and-pattern-matching">Data Types and Pattern Matching</h4><blockquote><p>这一节可以在<code>Ocaml</code>中定义自己的递归/非递归的数据类型。</p><p><code>Ocaml</code>使用<code>type</code>来定义<strong>有限无序的非递归集合</strong>，这里举一个扑克牌的例子:</p></blockquote><blockquote><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">type</span> suit = <span class="type">Clubs</span> | <span class="type">Spades</span> | <span class="type">Hearts</span> | <span class="type">Diamonds</span>;;</span><br><span class="line">&gt; <span class="keyword">type</span> suit = <span class="type">Clubs</span> | <span class="type">Spades</span> | <span class="type">Hearts</span> | <span class="type">Diamonds</span></span><br><span class="line">&gt; # <span class="keyword">type</span> rank = <span class="type">Two</span> | <span class="type">Three</span> | <span class="type">Four</span> | <span class="type">Five</span> | <span class="type">Six</span> | <span class="type">Seven</span> | <span class="type">Eight</span> | <span class="type">Nine</span> | <span class="type">Ten</span> | <span class="type">Jack</span> | <span class="type">Queen</span> | <span class="type">King</span> | <span class="type">Ace</span>;;</span><br><span class="line">&gt; # <span class="keyword">type</span> card = rank * suit;;</span><br><span class="line">&gt; <span class="keyword">type</span> card = rank * suit</span><br><span class="line">&gt; # <span class="keyword">type</span> hand = <span class="type">Empty</span> | <span class="type">Hand</span> <span class="keyword">of</span> card * hand;;</span><br><span class="line">&gt; <span class="keyword">type</span> hand = <span class="type">Empty</span> | <span class="type">Hand</span> <span class="keyword">of</span> card * hand</span><br><span class="line">&gt; # <span class="keyword">let</span> <span class="keyword">rec</span> dom (s1, s2) = <span class="keyword">match</span> (s1, s2) <span class="keyword">with</span> </span><br><span class="line">&gt; | (<span class="type">Spades</span>, _) -&gt; <span class="literal">true</span></span><br><span class="line">&gt; | (<span class="type">Hearts</span>, <span class="type">Diamonds</span>)  -&gt; <span class="literal">true</span></span><br><span class="line">&gt; | (s1, s2) -&gt; s1 = s2</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p><code>Ocaml</code>使用<code>pattern matching</code>模式匹配来操作这些数据结构。这里有两个概念——<code>patterns</code>和<code>constructors</code>。每一个<code>pattern</code>由不同的<code>constructors</code>填充——这里的构造器指的是每一个和类型元素同名的构造器比如<code>Clubs</code>。<code>Ocaml</code>规定构造器首字母必须大写，且匹配是从上到下的顺序。</p><p>进一步的，为了简化代码而支持<code>inductively</code>的<code>deep pattern</code>构造形式如下:</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">let</span> <span class="keyword">rec</span> add c1 c2 = <span class="keyword">match</span> c1 c2 <span class="keyword">with</span></span><br><span class="line">&gt;   | (<span class="type">Hearts</span>, v1), (<span class="type">Hearts</span>, v2) -&gt; v1 + v2</span><br><span class="line">&gt;   | _, _ -&gt; <span class="number">0</span>;;</span><br><span class="line">&gt; <span class="keyword">val</span> add : (<span class="symbol">'a</span> -&gt; (suit * <span class="built_in">int</span>) * (suit * <span class="built_in">int</span>)) -&gt; <span class="symbol">'a</span> -&gt; <span class="built_in">int</span> = &lt;<span class="keyword">fun</span>&gt;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>这里会发现构造了一个比较复杂的类型<code>('a -&gt; (suit*int)*(suit*int)) -&gt; 'a -&gt; int</code>。其中<code>type1 * type2</code>为一个<code>pair</code>，而`<code>a</code>为任意类型。</p><p>上述函数的基本类型推导过程为两个任意类型为输入参数，<code>int</code>为输出。其中任意类型为<code>(suit*int)*(suit*int)</code>的。</p><p>上面扑克牌的例子中<code>hand</code>定义一组<strong>无限递归集合</strong>。在<code>hand</code>类型中，<code>Empty</code>和<code>Hand</code>分别为两个<code>constructors</code>。<code>Empty</code>没有输入参数，而<code>Hand</code>以<code>a tuple of a card and another hand</code>的一个<code>tuple</code>为输入参数。看下面的实例化例子:</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">let</span> hand0: hand = <span class="type">Empty</span></span><br><span class="line">&gt; <span class="keyword">let</span> hand1: hand = <span class="type">Hand</span>((<span class="type">Ace</span>, <span class="type">Hearts</span>), <span class="type">Empty</span>)</span><br><span class="line">&gt; <span class="keyword">let</span> hand2: hand = <span class="type">Hand</span>((<span class="type">Queen</span>, <span class="type">Diamonds</span>), hand1)</span><br><span class="line">&gt; <span class="keyword">let</span> hand3:hand = </span><br><span class="line">&gt; <span class="type">Hand</span>((<span class="type">Ace</span>, <span class="type">Spades</span>),</span><br><span class="line">&gt; <span class="type">Hand</span>((<span class="type">Ten</span>, <span class="type">Diamonds</span>), </span><br><span class="line">&gt; <span class="type">Hand</span>((<span class="type">Seven</span>, <span class="type">Clubs</span>),</span><br><span class="line">&gt; <span class="type">Hand</span>((<span class="type">Queen</span>, <span class="type">Spades</span>),</span><br><span class="line">&gt; <span class="type">Hand</span>((<span class="type">Eight</span>, <span class="type">Clubs</span>), <span class="type">Empty</span>)))));;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>这个<code>hand</code>类型实际上就是一棵退化的二叉树（只能靠右生长）——叶子是<code>Empty</code>， 而<code>Hand</code>构造器使用两个参数来建树。</p><p>下面我们定义一个函数，这里函数的类型是<code>suit -&gt; hand -&gt; hand</code>，功能是给定一个<code>suit -&gt; hand</code>的输入，然后从<code>hand</code>中找有匹配到<code>suit</code>类型的<code>hand</code>然后返回。</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">let</span> <span class="keyword">rec</span> extract (s:suit) (h:hand) = <span class="keyword">match</span> h <span class="keyword">with</span> </span><br><span class="line">&gt; | <span class="type">Empty</span> -&gt; <span class="type">Empty</span></span><br><span class="line">&gt; | <span class="type">Hand</span> ((_, s') <span class="keyword">as</span> c, h') -&gt;</span><br><span class="line">&gt; <span class="keyword">if</span> s = s' <span class="keyword">then</span> <span class="type">Hand</span>(c, extract s h')</span><br><span class="line">&gt; <span class="keyword">else</span> extract s h'</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>上一个函数中的<code>Empty</code>实际上就是一个终结符，意味着如果<code>hand</code>类型都不匹配的话就直接返回<code>Empty</code>。但是如果我要匹配一个<code>card</code>的话，没有匹配上该怎么返回呢？<code>Ocaml</code>引入一个<code>optional data type</code>。比如下面的函数:</p><figure class="highlight ocaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">&gt; # <span class="keyword">type</span> <span class="symbol">'a</span> option = <span class="type">None</span> | <span class="type">Some</span> <span class="keyword">of</span> <span class="symbol">'a</span></span><br><span class="line">&gt; <span class="keyword">type</span> <span class="symbol">'a</span> option = <span class="type">None</span> | <span class="type">Some</span> <span class="keyword">of</span> <span class="symbol">'a</span></span><br><span class="line">&gt; # <span class="keyword">let</span> <span class="keyword">rec</span> find (r, h) = <span class="keyword">match</span> h <span class="keyword">with</span></span><br><span class="line">&gt; | <span class="type">Empty</span>  -&gt; <span class="type">None</span></span><br><span class="line">&gt; | <span class="type">Hand</span>((r', s'), h') -&gt; <span class="keyword">if</span> r = r' <span class="keyword">then</span> <span class="type">Some</span> s'</span><br><span class="line">&gt; <span class="keyword">else</span> find(r, h')</span><br><span class="line">&gt; <span class="keyword">val</span> find: rank * hand -&gt; suit option = &lt;<span class="keyword">fun</span>&gt;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>其中<code>option type</code>表示由<code>Some</code>构造器实现的类型为`<code>a</code>的元素集合，是符合多态的任意类型。</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;​ 最近在实验室实习的过程中学到了很多有意思的东西，对&lt;code&gt;Programming Languages&lt;/code&gt;也有了更深的了解，&lt;code&gt;Ocaml&lt;/code&gt;是一门支持多范式的函数式编程语言。&lt;strong&gt;程序描述计算，但是又不仅仅是计算&lt;/strong&gt;。&lt;code&gt;Ocaml&lt;/code&gt;的学习带给我的不仅仅是语言本身，这个系列记录了其中的部分心得:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;关于&lt;code&gt;scripting/imperative/object-oriented/functional PL&lt;/code&gt;的基本概念
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Higher-order Functions&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;State-Full vs State-Free Computation&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Modelling Objects and Closures&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Exceptions/Continuations to Defer Control&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Polymorphism&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Partial Evaluation/Lazy Programming/Modules&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;&lt;/li&gt;
&lt;li&gt;关于&lt;code&gt;reason about programs&lt;/code&gt;的方法
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Type Checking&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Induction&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Operational Semantics&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;QuickCheck&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;&lt;/li&gt;
&lt;li&gt;关于设计一门语言的基本规则&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="Ocaml" scheme="http://haotianmcihael.github.io/categories/Ocaml/"/>
    
    
      <category term="PL" scheme="http://haotianmcihael.github.io/tags/PL/"/>
    
      <category term="FP" scheme="http://haotianmcihael.github.io/tags/FP/"/>
    
  </entry>
  
  <entry>
    <title>Lisp语言和Emacs</title>
    <link href="http://haotianmcihael.github.io/2021/02/16/Lisp%E8%AF%AD%E8%A8%80%E5%92%8CEmacs/"/>
    <id>http://haotianmcihael.github.io/2021/02/16/Lisp语言和Emacs/</id>
    <published>2021-02-16T13:58:30.000Z</published>
    <updated>2021-04-18T02:22:12.216Z</updated>
    
    <content type="html"><![CDATA[<h3 id="abstract">Abstract</h3><blockquote><p>​ 第一次接触<code>EMACS</code>是大二的时候，当时只顾着新奇的界面和强大的功能却忽视了其背后真正的理论。后来我写代码也一直不喜欢IDE这种东西，但现在回过头来看，这貌似也只是一种浮于表面而无关原理的执拗。</p><p>​ 这次重新回归到<code>Spacemacs</code>，我希望仔细从<code>Lisp</code>的角度理解这款编辑器，不管是其插件特性还是语言本身。</p></blockquote><a id="more"></a>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;​ 第一次接触&lt;code&gt;EMACS&lt;/code&gt;是大二的时候，当时只顾着新奇的界面和强大的功能却忽视了其背后真正的理论。后来我写代码也一直不喜欢IDE这种东西，但现在回过头来看，这貌似也只是一种浮于表面而无关原理的执拗。&lt;/p&gt;
&lt;p&gt;​ 这次重新回归到&lt;code&gt;Spacemacs&lt;/code&gt;，我希望仔细从&lt;code&gt;Lisp&lt;/code&gt;的角度理解这款编辑器，不管是其插件特性还是语言本身。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="Lisp" scheme="http://haotianmcihael.github.io/categories/Lisp/"/>
    
    
      <category term="EMACS" scheme="http://haotianmcihael.github.io/tags/EMACS/"/>
    
      <category term="PL" scheme="http://haotianmcihael.github.io/tags/PL/"/>
    
  </entry>
  
  <entry>
    <title>WebAssembly和Wabt</title>
    <link href="http://haotianmcihael.github.io/2020/11/12/WebAssembly%E5%92%8CWabt/"/>
    <id>http://haotianmcihael.github.io/2020/11/12/WebAssembly和Wabt/</id>
    <published>2020-11-12T09:01:21.000Z</published>
    <updated>2020-11-15T02:36:45.063Z</updated>
    
    <content type="html"><![CDATA[<h3 id="abstract">Abstract</h3><blockquote><p>最近参加<a href="https://github.com/deeplang-org" target="_blank" rel="noopener"><code>deeplang</code></a>的的过程中，接触到了一些编译领域的新技术，总结一下希望有时间了可以进一步的深入了解。参考资料是<a href="https://book.douban.com/subject/30396640/" target="_blank" rel="noopener">WebAssembly标准入门</a>。</p></blockquote><a id="more"></a><h3 id="points">Points</h3><blockquote><p>过去很多技术尝试将<code>C/C++</code>程序直接运行在浏览器中，比如<code>TypeScript</code> 将<code>C/C++</code>代码转换为<code>JS</code>。</p></blockquote><ul><li><code>Emscripten</code></li></ul><blockquote><p>而这个项目利用<code>LLVM</code>编译器前端编译<code>C/C++</code>代码，生成<code>LLVM</code>平台特有的跨平台中间语言代码，最终再将<code>IR</code>转换为<code>JavaScript</code>的<strong><code>asm.js</code>子集</strong>——这是<code>WebAssembly</code>的基础。</p></blockquote><ul><li><code>AOT编译</code></li><li><code>JIT编译</code></li></ul><blockquote><p><code>JIT</code>的优点在于<code>Profile-Based Optimization</code>，根据运行时信息然后随着时间的推移尽可能的得到最优的代码。</p><p>而<code>AOT</code>的优点在于无需<code>runtime</code>运行，直接静态链接至最终的程序中。</p></blockquote><ul><li><code>WebAssembly</code></li></ul><blockquote><p><code>JavaScript</code>运行在其虚拟机上，而<code>WebAssembly</code>也运行在其虚拟机上。而现在<code>Node.js 8.0</code>之后的版本也可以运行<code>WebAssembly</code>——也就是说<code>WebAssembly</code>虚拟机可以脱离<code>JS</code>的环境支持，不仅仅运行在浏览器中。</p></blockquote><h3 id="webassembly概述">WebAssembly概述</h3><blockquote><p>和<code>LLVM IR</code>很像，有两种汇编表示——<code>.wat</code>文本表示和<code>.wasm</code>机器表示。</p></blockquote><h4 id="关键概念">关键概念</h4><ul><li>模块</li></ul><blockquote><p>由<code>.wasm</code>编译而来的可执行机器码的二进制对象。</p></blockquote><ul><li>内存</li></ul><blockquote><p>网页环境下，<code>wasm</code>的内存由<code>JavaScript</code>中的<code>ArrayBuffer</code>对象实现的。</p></blockquote><ul><li>表格</li></ul><blockquote><p>引入表格对象用于存储函数引用来模拟<code>C/C++</code>指针。</p></blockquote><ul><li>实例<ul><li>导入对象 调用JS函数</li><li>导出对象 提供接口</li></ul></li></ul><blockquote><p>指一个模块及其运行时的所有状态，包括内存，表格以及导入对象等。模块只有被实例化之后才可以调用。</p></blockquote><h4 id="程序生命周期">程序生命周期</h4><blockquote><ul><li>将<code>wat</code>或者其他语言编译成<code>.wasm</code>文件</li><li>网页中使用<code>fetch</code>等获取<code>.wasm</code>文件<ul><li><code>emrun --no_browser --port 8080 .</code> 将文件运行在<code>http</code>协议上</li><li>然后打开<code>http://localhost:8080/xxx.html</code></li></ul></li><li>将<code>.wasm</code>编译为模块，编译过程中进行合法性检查</li><li>实例化，初始化导入对象，创建模块实例</li><li>执行实例的导出函数，完成操作</li></ul></blockquote><h4 id="虚拟机体系结构">虚拟机体系结构</h4><blockquote><ul><li>一个全局类型数组(函数签名)</li><li>一个全局函数数组</li><li>一个全局变量数组</li><li>一个全局表格对象(元素引用)</li><li>一个全局内存对象</li><li>一个运行时栈</li></ul></blockquote><h4 id="hello-world">Hello World</h4><blockquote><p>JavaScript部分<code>hello.html</code></p></blockquote><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">&lt;!doctype html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"utf-8"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">title</span>&gt;</span>Show me the answer<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined"></span></span><br><span class="line"><span class="undefined">            var wasmMem = new WebAssembly.Memory(&#123;initial:1&#125;);</span></span><br><span class="line"><span class="undefined">            function printStr(offset, length)&#123;</span></span><br><span class="line"><span class="undefined">                var bytes = new Uint8Array(wasmMem.buffer, offset, length);</span></span><br><span class="line"><span class="undefined">                var string = new TextDecoder('utf-8').decode(bytes);</span></span><br><span class="line"><span class="undefined">                console.log(string);</span></span><br><span class="line"><span class="undefined">                &#125;;</span></span><br><span class="line"><span class="undefined">            var importObj = &#123;js : &#123;print: printStr, mem: wasmMem&#125;&#125;; </span></span><br><span class="line"><span class="undefined">            fetch('hello.wasm').then(response=&gt;</span></span><br><span class="line"><span class="undefined">                        response.arrayBuffer()</span></span><br><span class="line"><span class="undefined">                        ).then(bytes=&gt;</span></span><br><span class="line"><span class="undefined">                          WebAssembly.instantiate(bytes, importObj)</span></span><br><span class="line"><span class="undefined">                        ).then(result =&gt; result.instance.exports.hello()</span></span><br><span class="line"><span class="undefined">                        );</span></span><br><span class="line"><span class="undefined">        </span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><blockquote><p>wat部分<code>hello.wat</code></p></blockquote><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"> ;; hello.wat</span><br><span class="line"></span><br><span class="line">(<span class="keyword">module</span></span><br><span class="line">      ;; import js::print as js_print(); </span><br><span class="line">      (import "js" "print" (func $js_print (param i32 i32)))</span><br><span class="line">      (import "js" "mem" (memory 1))  ;;import js::mem as memory</span><br><span class="line">      (data (i32.const 0) "你好，wasm")</span><br><span class="line">      (func (export "hello")</span><br><span class="line">           i32.const 0    ;; pass offset 0 to js_print</span><br><span class="line">           i32.const 13   ;; pass offset 13 to js_print</span><br><span class="line">           call $js_print </span><br><span class="line">            )</span><br><span class="line">       )</span><br></pre></td></tr></table></figure><blockquote><p>首先将<code>.wat</code>编译成为<code>.wasm</code>,需要用到<a href="https://github.com/WebAssembly/wabt" target="_blank" rel="noopener">wabt工具集</a>。还需要下载<a href="https://github.com/juj/emsdk.git" target="_blank" rel="noopener">Emscripten工具集</a>。编译好之后配置环境变量。就可以执行了。</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> wat2wasm hello.wat -o hello.wasm</span><br><span class="line"><span class="meta">$</span> emrun --no_browser --port 8080 .</span><br></pre></td></tr></table></figure><blockquote><p>然后打开<code>http://localhost:8080/xxx.html</code>，启动<code>Chrome</code>下的开发者模式，看到执行成功。</p></blockquote><h3 id="webassembly核心">WebAssembly核心</h3><h4 id="在js环境中">在JS环境中</h4><blockquote><p>浏览器中的<code>wasm</code>运行在<code>JS</code>虚拟机上，页面可以通过一组<code>JS</code>对象进行<code>wasm</code>模块的编译，载入，配置，调用等操作。</p><p>在<code>wasm</code>中的几个关键概念都有与之对应的对象：</p><ul><li>模块——<code>WebAssembly.Module</code></li><li>内存——<code>WebAssembly.Memory</code></li><li>表格——<code>WebAssembly.Table</code></li><li>实例——<code>WebAssembly.Instance</code></li></ul></blockquote><h4 id="webassembly汇编语言">WebAssembly汇编语言</h4>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;最近参加&lt;a href=&quot;https://github.com/deeplang-org&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;&lt;code&gt;deeplang&lt;/code&gt;&lt;/a&gt;的的过程中，接触到了一些编译领域的新技术，总结一下希望有时间了可以进一步的深入了解。参考资料是&lt;a href=&quot;https://book.douban.com/subject/30396640/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;WebAssembly标准入门&lt;/a&gt;。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://haotianmcihael.github.io/categories/JavaScript/"/>
    
    
      <category term="Compiler" scheme="http://haotianmcihael.github.io/tags/Compiler/"/>
    
      <category term="PL" scheme="http://haotianmcihael.github.io/tags/PL/"/>
    
      <category term="WebAssembly" scheme="http://haotianmcihael.github.io/tags/WebAssembly/"/>
    
  </entry>
  
  <entry>
    <title>LLVM漫谈（2-Implementing a Language）</title>
    <link href="http://haotianmcihael.github.io/2020/07/07/LLVM%E6%BC%AB%E8%B0%88%EF%BC%882-Implementing-a-Language%EF%BC%89/"/>
    <id>http://haotianmcihael.github.io/2020/07/07/LLVM漫谈（2-Implementing-a-Language）/</id>
    <published>2020-07-06T23:05:29.000Z</published>
    <updated>2020-07-10T08:37:02.148Z</updated>
    
    <content type="html"><![CDATA[<h1 id="abstract">Abstract</h1><blockquote><p>基于<code>LLVM</code>实现简单的编程语言<code>kaleidoscope</code>，来源于<a href="https://releases.llvm.org/9.0.0/docs/tutorial/LangImpl01.html" target="_blank" rel="noopener">LLVM9.0.0 tutorial</a>。</p><p><code>LLVM</code>的官方文档还是很不错的，阅读完其中的<strong>tutorials</strong>就能大致上手<code>LLVM</code>了。实现的代码对<code>C++11</code>要求比较高，我放在了<a href="https://github.com/haotianmichael/LLVMSet/tree/master/Kaleidoscope" target="_blank" rel="noopener">Github</a>上。</p><p>实现共分为10部分：</p><ul><li>实现<strong>词法分析器Lexer</strong></li><li>使用<strong>递归下降</strong>和<strong>运算符优先级解析</strong>实现<strong>语法分析器Parser</strong></li><li>将<strong><code>AST</code></strong>转化成<strong><code>LLVM-IR</code></strong></li><li>添加<strong><code>JIT</code>即时编译</strong>和<strong>优化器</strong>的支持</li><li>扩展——<strong>控制流</strong>，这一章会引入<strong><code>SSA</code></strong>概念</li><li>扩展——<strong>用户定义运算符</strong></li><li>扩展——<strong>自动变量</strong>，深入<strong><code>SSA</code></strong></li><li>目标代码生成</li><li>扩展——<strong>添加调试器</strong></li><li>深入——<strong>GC，异常等</strong></li></ul></blockquote><a id="more"></a><h1 id="parser">Parser</h1><h2 id="编译原理">编译原理</h2><h2 id="c原理">C++原理</h2><h1 id="llvm-ir">LLVM-IR</h1>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;abstract&quot;&gt;Abstract&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;基于&lt;code&gt;LLVM&lt;/code&gt;实现简单的编程语言&lt;code&gt;kaleidoscope&lt;/code&gt;，来源于&lt;a href=&quot;https://releases.llvm.org/9.0.0/docs/tutorial/LangImpl01.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;LLVM9.0.0 tutorial&lt;/a&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;LLVM&lt;/code&gt;的官方文档还是很不错的，阅读完其中的&lt;strong&gt;tutorials&lt;/strong&gt;就能大致上手&lt;code&gt;LLVM&lt;/code&gt;了。实现的代码对&lt;code&gt;C++11&lt;/code&gt;要求比较高，我放在了&lt;a href=&quot;https://github.com/haotianmichael/LLVMSet/tree/master/Kaleidoscope&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Github&lt;/a&gt;上。&lt;/p&gt;
&lt;p&gt;实现共分为10部分：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;实现&lt;strong&gt;词法分析器Lexer&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;使用&lt;strong&gt;递归下降&lt;/strong&gt;和&lt;strong&gt;运算符优先级解析&lt;/strong&gt;实现&lt;strong&gt;语法分析器Parser&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;将&lt;strong&gt;&lt;code&gt;AST&lt;/code&gt;&lt;/strong&gt;转化成&lt;strong&gt;&lt;code&gt;LLVM-IR&lt;/code&gt;&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;添加&lt;strong&gt;&lt;code&gt;JIT&lt;/code&gt;即时编译&lt;/strong&gt;和&lt;strong&gt;优化器&lt;/strong&gt;的支持&lt;/li&gt;
&lt;li&gt;扩展——&lt;strong&gt;控制流&lt;/strong&gt;，这一章会引入&lt;strong&gt;&lt;code&gt;SSA&lt;/code&gt;&lt;/strong&gt;概念&lt;/li&gt;
&lt;li&gt;扩展——&lt;strong&gt;用户定义运算符&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;扩展——&lt;strong&gt;自动变量&lt;/strong&gt;，深入&lt;strong&gt;&lt;code&gt;SSA&lt;/code&gt;&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;目标代码生成&lt;/li&gt;
&lt;li&gt;扩展——&lt;strong&gt;添加调试器&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;深入——&lt;strong&gt;GC，异常等&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="C++" scheme="http://haotianmcihael.github.io/categories/C/"/>
    
    
      <category term="Compiler" scheme="http://haotianmcihael.github.io/tags/Compiler/"/>
    
      <category term="LLVM" scheme="http://haotianmcihael.github.io/tags/LLVM/"/>
    
  </entry>
  
  <entry>
    <title>LLVM漫谈（1-LLVM编译框架）</title>
    <link href="http://haotianmcihael.github.io/2020/06/25/LLVM%E6%BC%AB%E8%B0%88%EF%BC%881-LLVM%E7%BC%96%E8%AF%91%E6%A1%86%E6%9E%B6%EF%BC%89/"/>
    <id>http://haotianmcihael.github.io/2020/06/25/LLVM漫谈（1-LLVM编译框架）/</id>
    <published>2020-06-25T04:53:37.000Z</published>
    <updated>2020-07-06T13:21:28.062Z</updated>
    
    <content type="html"><![CDATA[<h1 id="abstract">Abstract</h1><blockquote><p><strong>LLVM</strong>，这个星球最牛逼的编译器。</p><p>其核心设计理念就是<strong>统一的底层中间表达以及模块化的软件工程化方法。</strong>LLVM采用了<code>前端-优化器-后端</code>的组织形式，<strong>只是不同的前端和后端都采用统一的底层中间表示格式（LLVM IR）来最大可能复用优化器的代码。</strong></p><p>在<a href="http://nondot.org/sabre/" target="_blank" rel="noopener">Chris Lattner</a>将其开源后，<a href="http://llvm.org/" target="_blank" rel="noopener">LLVM</a>逐渐发展成为了成熟的编译框架，具有大量库和编译链工具。</p></blockquote><blockquote><p>我使用的平台是<code>macOS Mojave 10.14</code>，在编译的时候要注意版本是否兼容。</p><p><strong>一般系统会通过打印二进制文件名称和无法加载的动态库的名称来发现链接错误。当屏幕上打印动态库名称时要予以注意，这说明系统动态链接器和加载器无法加载该库，因为该程序与当前系统不兼容。</strong></p><p>我编译<code>LLVM 10.0.0</code>的时候出错如下：</p></blockquote><p><img src="1.png" width="50%" height="50%" alt=""></p><blockquote><p>后来发现这个版本的<code>Xcode</code>工具链最高只支持<code>LLVM 9.0.0</code>版本，然后换了版本最后编译成功。</p></blockquote><a id="more"></a><h1 id="build">BUILD</h1><table><colgroup><col style="width: 34%"><col style="width: 65%"></colgroup><thead><tr class="header"><th>Subproject Deployed On My System</th><th>Note</th></tr></thead><tbody><tr class="odd"><td><strong>llvm-core</strong></td><td><strong>modern source- and target-independent <a href="http://llvm.org/docs/Passes.html" target="_blank" rel="noopener">optimizer</a>, along with <a href="http://llvm.org/docs/CodeGenerator.html" target="_blank" rel="noopener">code generation support</a> for many popular CPUs</strong></td></tr><tr class="even"><td><strong>Clang</strong></td><td><strong>&quot;LLVM native&quot; C/C++/Objective-C compiler front-end</strong></td></tr><tr class="odd"><td><strong>LLDB</strong></td><td><strong>native debugger</strong></td></tr><tr class="even"><td><strong>LLD</strong></td><td><strong>native linker</strong></td></tr><tr class="odd"><td><strong>libcxx/libcxxabi</strong></td><td><strong>standard conformant and high-performance implementation of the C++ Standard Library,</strong></td></tr><tr class="even"><td><strong>compiler-rt</strong></td><td><strong>highly tuned implementations of the low-level code generator support routines and implementations of run-time libraries</strong></td></tr></tbody></table><h2 id="llvm">llvm</h2><blockquote><p>以上的<code>subproject</code>通过以下编译方式完成部署：</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">//ought to download source of subprojects to local, besides </span><br><span class="line"><span class="meta">$</span> git clone https://github.com/llvm/llvm-project.git  </span><br><span class="line"><span class="meta">$</span> mkdir /LLVM/llvm-build  &amp;&amp; cd //LLVM/llvm-build</span><br><span class="line"><span class="meta">$</span> cmake -G Ninja -DLLVM_ENABLE_PROJECTS='lldb;clang;compiler-rt;lld;' \    </span><br><span class="line">        -DCMAKE_INSTALL_PREFIX='/LLVM/llvm-install'  \</span><br><span class="line">        ~/where you put llvm-src</span><br><span class="line"><span class="meta">$</span> ninja &amp;&amp; ninja install</span><br><span class="line"><span class="meta">$</span> echo $?   //check</span><br></pre></td></tr></table></figure><h2 id="libcxxlibcxxabi">libcxx/libcxxabi</h2><blockquote><p>这里理清楚一个概念：</p><ul><li><code>glic</code>是<code>linux</code>最重要的运行库，其实现最底层的<code>API</code>供其他库使用比如<code>malloc/printf</code>等。</li><li><code>libstdc++</code>是一个<code>C++</code>标准库，如果跑在<code>linux</code>则依赖<code>Glibc</code></li><li><code>libcxx</code>是<code>LLVM</code>专门开发的一个代替<code>libstdc++</code>的<code>c++</code>标准库</li><li><code>compiler-rt</code>是<code>LLVM</code>开发来替代<code>libgcc</code>,主要用于为硬件不支持的低级功能提供特定于目标的支持</li></ul><p><code>clang++</code>构建可执行文件的时候使用<code>libcxx</code>或者<code>libclc</code>(OpenCL运行时库)。其中<code>libcxx</code>实现由<strong>库本身和一个低级函数层<code>libcxxabi</code>组成，这个函数层用来处理异常和运行时类型信息(RTTI)等功能，这种分离式设计使得<code>libcxxabi</code>更容易移植。</strong></p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> git clone https://github.com/llvm/llvm-project.git</span><br><span class="line"><span class="meta">$</span> cd llvm-project</span><br><span class="line"><span class="meta">$</span> mkdir /LLVM/libcxx-build &amp;&amp; cd /LLVM/libcxx-build</span><br><span class="line"><span class="meta">$</span> cmake -G Ninja -DCMAKE_C_COMPILER=clang \</span><br><span class="line">        -DCMAKE_CXX_COMPILER=clang++ \</span><br><span class="line">        -DLLVM_ENABLE_PROJECTS="libcxx;libcxxabi" \</span><br><span class="line">        -DCMAKE_INSTALL_PREFIX='/LLVM/llvm-install'   \</span><br><span class="line">        ~/where you put libcxx-src</span><br><span class="line"><span class="meta">$</span> make # Build</span><br><span class="line"><span class="meta">$</span> make check-cxx # Test</span><br><span class="line"><span class="meta">$</span> make install-cxx install-cxxabi # Install</span><br></pre></td></tr></table></figure><h2 id="warning">warning</h2><blockquote><p>其中<code>llvm</code>和<code>libcxx</code>我是分开编译的，而且<strong>编译目录和安装目录都是指定的目录，默认很可能会安装到系统路径下，从而带来错误。</strong></p><ul><li><code>~/Document/LLVM9.0.0/</code><ul><li><code>llvm</code> //llvm源代码</li><li><code>libcxx</code> //libcxx源代码</li></ul></li><li><code>/LLVM/</code><ul><li><code>llvm-build</code> //llvm编译路径</li><li><code>llvm-install</code> //llvm安装路径</li></ul></li><li><code>/LLVM/</code><ul><li><code>libcxx-build</code> //libcxx编译路径</li><li><code>libcxx-install</code> //libcxx安装路径</li></ul></li></ul></blockquote><h1 id="features">Features</h1><h2 id="核心设计">核心设计</h2><blockquote><p><code>LLVM</code>中最核心的就是<code>LLVM-IR</code> ：</p></blockquote><ul><li><strong><code>SSA</code>表示和允许快速优化的无限寄存器</strong></li><li><strong>通过将整个程序存储在磁盘IR表示中，实现便捷的链接时优化</strong></li></ul><blockquote><p>当然<code>LLVM</code>不仅仅只有一种中间表示形式：</p></blockquote><ul><li>将<code>C</code>或<code>C++</code>转换为<code>LLVM-IR</code>的时候，<code>Clang</code>将使用<strong>抽象语法树<code>AST</code>结构</strong>在内存中表示程序</li><li>将<code>LLVM-IR</code>转换为特定于机器的汇编语言的时候，<code>LLVM</code>首先将程序转换为<strong>有向无环图<code>DAG</code></strong>格式以便进行<strong>指令选择</strong>，然后将其转换回<strong>三地址表示</strong>以进行<strong>指令调度</strong></li><li>为了实现汇编器和链接器，<code>LLVM</code>使用第四种中间数据结构<code>MCModule</code>在对象文件的上下文中保存成程序表示</li></ul><blockquote><p>在整个框架中，&quot;库&quot;是一个很核心的概念。很多二进制工具都可以直接调用库来实现。而<strong>Clang作为编译驱动程序则可以通过链接小工具的库来实现其功能——代码重用。</strong>故<code>clang</code>在二进制文件中是最大的，因为它链接并利用了整个<code>LLVM</code>生态系统。<strong>而开发者一般都是将LLVM各个组件看出是库。</strong></p></blockquote><p><img src="2.png" width="50%" height="50%" alt=""></p><h2 id="编译流程">编译流程</h2><h3 id="clang">clang</h3><blockquote><p><code>clang</code>作为编译器驱动程序隐式的调用从前端到链接器的所有工具。<strong>整个过程都是在内存中完成的，并不会将中间文件输出到磁盘中。</strong></p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> clang hello.c -o hello</span><br><span class="line"><span class="meta">$</span> clang -### hello.c -o hello          //可以查看驱动程序调用的所有工具</span><br></pre></td></tr></table></figure><h3 id="独立工具">独立工具</h3><blockquote><p><code>LLVM</code>独立工具会将中间文件输出到磁盘上。</p></blockquote><table><colgroup><col style="width: 15%"><col style="width: 84%"></colgroup><thead><tr class="header"><th>Tools</th><th>Notes</th></tr></thead><tbody><tr class="odd"><td><code>opt</code></td><td><strong>在IR级别对程序进行优化的工具，输入必须是LLVM位码文件(编码的LLVM-IR)，并且生成的输出文件必须具有相同的类型</strong></td></tr><tr class="even"><td><code>llc</code></td><td><strong>通过特定后端将LLVM位码转换为目标机器汇编语言文件或目标文件的工具，可以通过传递参数来选择优化级别，打开调试选项以及启用或禁用特定与目标的优化</strong></td></tr><tr class="odd"><td><code>llvm-mc</code></td><td><strong>能够汇编指令并生成诸如<code>ELF</code>,<code>Mach-O</code>,<code>PE</code>等对香格式的可执行目标文件，也可以反汇编相同的对象，从而转储这些指令的汇编信息和内部<code>LLVM</code>机器指令数据结构</strong></td></tr><tr class="even"><td><code>lli</code></td><td><strong>是<code>LLVM IP</code>的解释器和<code>JIT</code>编译器</strong></td></tr><tr class="odd"><td><code>llvm-link</code></td><td><strong>将几个<code>LLVM</code>位码链接在一起，以产生一个包含所有输入的<code>LLVM</code>位码</strong></td></tr><tr class="even"><td><code>llvm-as</code></td><td><strong>将人工可读的<code>LLVM-IR</code>文件(称为<code>LLVM</code>汇编码)转换为<code>LLVM</code>位码</strong></td></tr><tr class="odd"><td><code>llvm-dis</code></td><td><strong>将<code>LLVM</code>位码解码成为<code>LLVM</code>汇编码</strong></td></tr></tbody></table><h4 id="emit-llvm"><code>-emit-llvm</code></h4><ul><li><strong>LLVM位码</strong>： 编码的<code>LLVM-IR</code></li><li><strong>LLVM汇编码</strong>： 人工可读的<code>LLVM-IR</code></li></ul><blockquote><p>假设<code>main.c</code>和<code>sum.c</code>:</p></blockquote><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">$ vim main.c</span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line">  </span><br><span class="line"><span class="keyword">int</span> </span><br><span class="line">main(<span class="keyword">void</span>) &#123;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">int</span> r = sum(<span class="number">3</span>, <span class="number">4</span>);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"r = %d\n"</span>, r);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">$ vim sum.c</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (x + y);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>如果直接使用<strong>编译器驱动程序<code>Clang</code></strong>的话：</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> clang main.c sum.c -o sum</span><br></pre></td></tr></table></figure><blockquote><p>如果使用独立工具的话:</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> clang -emit-llvm -c main.c -o main.bc</span><br><span class="line"><span class="meta">$</span> clang -emit-llvm -c sum.c -o sum.bc</span><br></pre></td></tr></table></figure><blockquote><p>其中<code>-emit-llvm</code>编译选项可以让<code>clang</code>根据<code>-c</code>或者<code>-S</code>参数来生成<strong><code>LLVM</code>位码</strong>还是<strong><code>LLVM</code>汇编码</strong>。</p></blockquote><ul><li><code>-c</code> 生成LLVM位码</li><li><code>-c -S</code>生成LLVM汇编码</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> clang -emit-llvm -c -S main.c -o main.ll</span><br><span class="line"><span class="meta">$</span> clang -emit-llvm -c -S sum.c -o sum.ll</span><br></pre></td></tr></table></figure><h4 id="llc">llc</h4><blockquote><p>该工具从每个LLVM位码文件内生成特定于目标的可重定位目标文件，并通过将其链接到系统链接器来构建可执行文件</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> llc -filetype=obj main.bc -o main.o</span><br><span class="line"><span class="meta">$</span> llc -filetype= obj sum.bc -o sum.o</span><br><span class="line"><span class="meta">$</span> clang main.o sum.o -o sum</span><br></pre></td></tr></table></figure><h4 id="llvm-link">llvm-link</h4><blockquote><p>该工具将多个LLVM位码文件连接成为最终的LLVM位码文件</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> llvm-link main.bc sum.bc -o sum.linked.bc</span><br><span class="line"><span class="meta">$</span> llc -filetype=obj sum.linked.bc -o sum.linked.o</span><br><span class="line"><span class="meta">$</span> clang sum.linked.o -o sum</span><br></pre></td></tr></table></figure><blockquote><p>可以看到所有的二进制工具都围绕几个中间形式的表示展开，这些中间表示在编译过程中占很重要的作用。</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;abstract&quot;&gt;Abstract&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;LLVM&lt;/strong&gt;，这个星球最牛逼的编译器。&lt;/p&gt;
&lt;p&gt;其核心设计理念就是&lt;strong&gt;统一的底层中间表达以及模块化的软件工程化方法。&lt;/strong&gt;LLVM采用了&lt;code&gt;前端-优化器-后端&lt;/code&gt;的组织形式，&lt;strong&gt;只是不同的前端和后端都采用统一的底层中间表示格式（LLVM IR）来最大可能复用优化器的代码。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在&lt;a href=&quot;http://nondot.org/sabre/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Chris Lattner&lt;/a&gt;将其开源后，&lt;a href=&quot;http://llvm.org/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;LLVM&lt;/a&gt;逐渐发展成为了成熟的编译框架，具有大量库和编译链工具。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;我使用的平台是&lt;code&gt;macOS Mojave 10.14&lt;/code&gt;，在编译的时候要注意版本是否兼容。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;一般系统会通过打印二进制文件名称和无法加载的动态库的名称来发现链接错误。当屏幕上打印动态库名称时要予以注意，这说明系统动态链接器和加载器无法加载该库，因为该程序与当前系统不兼容。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;我编译&lt;code&gt;LLVM 10.0.0&lt;/code&gt;的时候出错如下：&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.png&quot; width=&quot;50%&quot; height=&quot;50%&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;后来发现这个版本的&lt;code&gt;Xcode&lt;/code&gt;工具链最高只支持&lt;code&gt;LLVM 9.0.0&lt;/code&gt;版本，然后换了版本最后编译成功。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="C++" scheme="http://haotianmcihael.github.io/categories/C/"/>
    
    
      <category term="Compiler" scheme="http://haotianmcihael.github.io/tags/Compiler/"/>
    
      <category term="LLVM" scheme="http://haotianmcihael.github.io/tags/LLVM/"/>
    
  </entry>
  
  <entry>
    <title>LLVM漫谈（0-编译系统概述）</title>
    <link href="http://haotianmcihael.github.io/2020/06/02/LLVM%E6%BC%AB%E8%B0%88%EF%BC%880-%E7%BC%96%E8%AF%91%E7%B3%BB%E7%BB%9F%E6%A6%82%E8%BF%B0%EF%BC%89/"/>
    <id>http://haotianmcihael.github.io/2020/06/02/LLVM漫谈（0-编译系统概述）/</id>
    <published>2020-06-01T20:36:42.000Z</published>
    <updated>2020-06-24T20:25:51.484Z</updated>
    
    <content type="html"><![CDATA[<h1 id="abstract">Abstract</h1><blockquote><p>从开始接触编译系统的学习已经快半年了，有些心得总结一下。</p><p>什么是编译系统？我想从两个角度谈一下：</p><p>首先从技术角度来讲，广义的编译系统负责将高级语言转换成为CPU可执行的二进制机器代码。编译系统包括<strong>编译器</strong>， <strong>汇编器</strong>，<strong>静态/动态链接器</strong>，<strong>操作系统装载器</strong>以及<strong>运行库</strong>。</p><ul><li>编译器： 是系统前半部分的核心，负责将源文件<strong>.c</strong>转换成为汇编文件<strong>.s</strong>。</li><li>汇编/链接/装载器：是系统后半部分的核心，主要是将<strong>.s</strong>转换为<strong>二进制ELF文件</strong>并进一步围绕<strong>ELF</strong>处理。</li><li>运行库：是操作系统层面用来支持<strong>高级语言运行环境</strong>比如内存池，标准API等的库文件。</li></ul><p>然后从专业的角度来讲，我觉得可以分为<strong>系统层面</strong>和<strong>算法优化层面</strong>，系统层面主要是整个编译框架比如LLVM，核心是和操作系统交互的部分，这部分内容是编译器工程师的基本功，需要掌握细节。而算法优化层面指的是<strong>编译优化</strong>——编译器工程师的最理想的方向就是做优化，这一部分需要精通，从静态单赋值SSA等传统的静态分析优化到现在主流的深度学习推理引擎比如TVM等。</p><p>本文主要是对系统层面的学习，对系统层面的学习止步于对LLVM编译框架的使用和源代码阅读。</p></blockquote><a id="more"></a><h1 id="思路">思路</h1><blockquote><p>本文主要分为三个部分，编译，ELF以及库文件。</p><p>首先简单介绍一下编译器的整体框架和编译流程。</p><p>**核心部分是学习*nix操作系统下的ELF文件的格式——普通c/c++高级程序在目标文件中是如何存储的；目标文件是如何被链接器链接在一起，并且形成可执行文件的过程。目标文件在链接时符号处理，重定位和地址分配如何进行。可执行文件如何被装载并且执行，可执行文件与进程的虚拟空间之间如何映射。讲解动态链接。 **</p><p>最后讲解一下运行库的原理和操作系统与编译系统交互的部分。</p></blockquote><h1 id="编译器">编译器</h1><blockquote><p>编译器分为前端和后端，直观上讲<strong>就是将源语言形式化的表示并翻译成为汇编目标语言</strong>。前端主要有：</p><ul><li><p><strong>词法分析器</strong></p></li><li><strong>语法分析器</strong>，</li><li><p><strong>语义分析器</strong></p></li></ul><p>等完成源语言的语法推导，语义处理，然后将特定信息收集到符号表中，不同的编译器前端会生成不同的<strong>中间表示</strong>——比如<strong>LLVM IR</strong>就是一种非常重要的中间代码。</p><p>后端主要是：</p><ul><li><p><strong>寄存器分配</strong></p></li><li><p><strong>指令选择</strong></p></li><li><p><strong>指令调度</strong></p></li></ul><p><strong>后端和目标机的关系比较紧密，也是优化的重点平台。 </strong></p></blockquote><h1 id="汇编器">汇编器</h1><blockquote><p>当编译器将源文件翻译成为汇编语言之后，<strong>.s</strong>文件需要进一步的被<strong>汇编器</strong>进行转换成为<strong>可重定位目标文件（Relocatable File）</strong>。</p><p>因为汇编语言本质上就是助记符，<strong>汇编器就是将汇编文件翻译成为机器可执行的初步的二进制文件，这里的初步是汇编器汇编之后生成的.o文件还不能直接在操作系统中运行，这是因为.o文件中很多符号和变量的地址都没有确定——属于相对地址，所以无法加载到进程地址空间中，而且其运行还需要操作系统提供的运行库提供支持，这一切都需要链接器来完成。所以.o文件又称为可重定位目标文件。</strong></p><p>从<strong>.s</strong>文件翻译成为<strong>.o</strong>文件，必须按照特定指令集的机器码，还有ELF文件的格式来转换，实现起来不是很难——<strong>需要精通目标机器的体系结构，以及目标机器的ELF文件格式</strong>。这里不涉及体系结构的知识。</p></blockquote><h1 id="elf">ELF</h1><h2 id="elf简介">ELF简介</h2><blockquote><p>其实从汇编器的实现中就已经用到了ELF的知识。</p><p>ELF可以说是编译系统工具链中的最重要的文件格式了，离开编译器之后的所有编译步骤几乎都和ELF相关或者围绕ELF展开，所以对它的研究非常有意义。</p><p>最早是由Unix System V Release3提出了COFF的概念以及使用规范。后来Windows基于COFF指定了PE格式标准，而Linux也以ELF作为基本格式。COFF的主要贡献就是提供了“段segment”的概念。</p></blockquote><h2 id="elf类型">ELF类型</h2><blockquote><p>根据ELF文件标准，可以分为下面4类：</p></blockquote><table><colgroup><col style="width: 24%"><col style="width: 37%"><col style="width: 37%"></colgroup><thead><tr class="header"><th>ELF文件类型</th><th>说明</th><th>实例</th></tr></thead><tbody><tr class="odd"><td><strong>可重定位目标文件(Relocatable File)</strong></td><td>由汇编器生成的.o文件，又被称为<strong>目标文件</strong>，因为需要连接重定位其文件中的符号地址所以叫Relocatable；另外，<strong>静态链接库.a其实也是使用ar程序将很多.o文件压缩并对其进行索引和编号。所以也算此类型。</strong></td><td>Linux下的.o文件，.a静态库文件；Windows下的.obj，.lib静态库文件</td></tr><tr class="even"><td><strong>可执行文件(Executable File)</strong></td><td>是可以直接执行的程序，又被称为<strong>可执行文件</strong>。</td><td>/usr/bash文件；Windows下的.exe</td></tr><tr class="odd"><td><strong>共享目标文件(Shared Object File)</strong></td><td><strong>共享</strong>就是动态链接的本质。其可以被链接器继续链接生成新的目标文件；也可以和可执行文件结合，作为进程映像的一部分来运行。</td><td>Linux下的.so；Windows下的DLL</td></tr><tr class="even"><td><strong>核心转储文件(Core Dump File)</strong></td><td>当进程意外终止的时候，系统可以将该进程的地址空间及终止时的一些其他信息转储到核心转储文件中。</td><td>Linux下的core dump</td></tr></tbody></table><h2 id="elf格式">ELF格式</h2><h3 id="分段">分段</h3><blockquote><p>以目标文件<strong>.o</strong>为例介绍，目标文件中的内容至少含有编译后的机器指令<strong>代码和数据</strong>。按照COFF的历史规定，ELF格式也是按照<strong>段-segment</strong>来管理的。下面是ELF文件的基本格式：</p></blockquote><p><img src="1.png" width="50%" height="50%" alt=""></p><blockquote><p>从图中可以看到，ELF的开头是<strong>ELF Header</strong>，描述了整个文件的文件属性，包括文件是否可执行，是静态链接还是动态链接及入口地址（如果是可执行文件的话），目标硬件，操作系统等信息。ELF中第二重要的数据结构是<strong>Section Header table</strong>——描述文件中各个段的数组。段表描述了文件中各个段在文件中的偏移量以及段的属性等，从其中可以得到每个段的所有信息。<strong>ELF Header</strong>后面就是各个段的内容。分段的目的如下：</p></blockquote><ul><li>当指令和数据分离的时候，<strong>操作系统可以分配不同的读写权限便于管理</strong></li><li>当指令和数据分离的时候，<strong>CPU缓存的命中率会更高</strong></li><li>当指令和数据分离的时候，<strong>多个进程可以共享同一份指令，节省开销</strong></li></ul><h3 id="elf-header">ELF Header</h3><blockquote><p>是ELF文件最开头的一个结构体。它包含了描述整个文件的基本属性。因为ELF文件在各个平台下都通用，为了对每个成员的大小做出明确的规定以便保持兼容性。ELF使用typedef定义了一套自己的变量体系类似<code>Elf32_Addr</code>这种。下面简单列出文件头中的关键信息：</p></blockquote><ul><li>ELF魔数，文件机器字节长度，数据存储方式，版本，运行平台，</li><li>ABI版本，ELF重定位类型，硬件平台，<strong>入口地址（OS加载的虚拟地址，可重定位目标文件该值为0）</strong>，程序头入口和长度，</li><li><strong>Section Header table的位置（以偏移量的方式）</strong>和长度及段的数量</li></ul><blockquote><p>魔数：当年的UNIX是在PDP小型机器上开发的，而当时的系统加载可执行文件直接从第一个字节开始运行，人们一般将第一条指令设置为跳转指令，这个魔数就是当时的JMP指令，为了兼容性被保留下来。</p></blockquote><h3 id="section-header-table">Section Header table</h3><blockquote><p>段表描述了ELF的各个段segment的信息比如段名，段长度，段偏移以及段的读写权限等。 实现上，一般会采取一个结构体数组来表示段表。</p></blockquote><ul><li>段类型： 很多类型比如<code>.data</code>,<code>.text</code>或者<code>.rel</code>,<code>.string</code>等</li><li>段标志位：表示该段在进程内虚拟地址空间中的属性</li></ul><h3 id="relocation-table">Relocation Table</h3><blockquote><p>目标文件中代码段和数据段那些对绝对地址的引用，需要在链接的时候重定位。每一个需要重定位的位置，最后都会有一个<strong>相应的重定位表</strong>比如<code>.rel.txt</code>,<code>.rel.data</code>等。</p></blockquote><h3 id="sections">Sections</h3><ul><li><code>.data</code>: 保存初始化的全局变量和局部静态变量</li><li><code>.bss</code>: 保存未初始化的全局变量和局部静态变量</li><li><code>.data</code>: 保存代码段，等等</li></ul><blockquote><p>整个ELF文件有很多段比如字符串段，调试信息段strip等。包括在不同的平台标准上ELF格式都不同，这里不展开说了。</p></blockquote><h1 id="链接器">链接器</h1><blockquote><p>链接的本质就是要把多个不同的目标文件之间相互“粘”在一起——<strong>实际上就是目标文件之间地址的引用，及对函数和变量的地址的引用。</strong></p></blockquote><h2 id="符号">符号</h2><blockquote><p><strong>在链接过程中，每一个ELF目标文件都会有一个相应的符号表，这个符号表对应ELF中的一个段<code>.symtab</code>，这个表里记录了目标文件所用到的所有符号，每个定义的符号有一个符号值，对于变量和函数来说该值就是其地址。</strong></p><p>ELF的符号可以大致分为：</p><ul><li><strong>定义</strong>在目标文件中的<strong>全局符号</strong></li><li>在目标文件内中<strong>引用</strong>的<strong>全局符号，又称为外部符号</strong></li><li>段名</li><li>局部符号</li><li>链接器在生成可执行文件的时候，自行定义的特殊符号比如程序起始地址<code>__executable_start</code></li></ul><p>其中最重要的就是<strong>全局符号</strong>，因为其他的符号只是在目标文件中可见，而全局符号的作用域是跨文件的。</p><p>UNIX系统通过<strong>符号修饰</strong>和<strong>函数签名</strong>机制防止不同目标文件中的符号名称冲突。</p></blockquote><h3 id="强弱符号定义引用">(强/弱符号)(定义/引用)</h3><blockquote><p>编译器默认函数和初始化了的全局变量为强符号，未初始化的全局变量为弱符号。<strong>强符号和弱符号都是针对定义来说的，不是针对符号的引用。</strong></p><ul><li>不同的目标文件中不允许有同名的强符号</li><li>强符号可以屏蔽弱符号</li></ul><p>而当外部变量的符号引用在目标文件被链接成为可执行文件时，如果是强引用，如果定义不存在则报错，而弱引用不会报错。<strong>强/弱引用一般用于库文件的链接过程。</strong></p></blockquote><h2 id="静态链接">静态链接</h2><blockquote><p>链接器一般都采用<strong>Two-pass Linking</strong>的方式：</p><ul><li><strong>空间与地址分配：</strong> 扫描所有的输入文件，获得它们各个段的长度，属性和位置，<strong>并将输入目标文件中的符号表中的所有符号定义和符号引用收集起来，统一放到一个全局符号表。</strong></li><li><strong>符号解析与重定位：</strong> 读取输入文件中段的数据，重定位信息；进行<strong>符号解析与重定位，调整代码中的地址。</strong><ul><li>各个段中的符号地址</li><li>代码中的地址引用</li></ul></li></ul></blockquote><h3 id="空间与地址分配">空间与地址分配</h3><blockquote><p>链接器能够获得所有输入目标文件的段长度，并且将它们合并，计算出输出文件中各个段合并后的长度和位置，并建立映射关系。</p></blockquote><p><img src="2.png" width="50%" height="50%" alt=""></p><h3 id="符号解析与重定位">符号解析与重定位</h3><blockquote><p>这里有两部分，符号的地址和代码中的地址。其中链接器在完成地址和空间分配之后就可以确定所有<strong>符号</strong>的虚拟地址了，而链接器就可以根据符号的地址(<strong>通过查找由所有输入目标文件的符号表组成的全局符号表</strong>)对每个需要<strong>重定位的指令——即会用到外部符号的指令</strong>进行地址修正。</p></blockquote><h4 id="重定位表">重定位表</h4><blockquote><p>链接器通过ELF文件中的重定位表来完成指令中地址的调整。</p></blockquote><h4 id="abi">ABI</h4><blockquote><p>指的是和二进制兼容性有关的内容：</p></blockquote><ul><li>内置类型的大小和在存储器中的放置方式（大端，小端，对齐方式等）</li><li>组合类型（结构体，数组等）的存储方式和内存分布</li><li>外部符号与用户定义的符号之间的命名方式和解析方式</li><li>函数调用方式，比如参数入栈顺序，返回值如何保存等</li><li>堆栈的分布方式，比如参数和局部变量在堆栈中的位置，参数传递方式等</li><li>寄存器使用约定</li></ul><h4 id="静态链接库">静态链接库</h4><blockquote><p>静态链接库实际上就是一组目标文件的集合。即很多目标文件经过压缩打包之后的一个文件。</p></blockquote><p><img src="3.png" width="50%" height="50%" alt=""></p><h2 id="装载">装载</h2><h3 id="虚拟地址空间">虚拟地址空间</h3><blockquote><p>首先32位CPU指的是CPU的数据线是32位的，但是其地址线不一定只有32位，PAE地址扩展方式修改了页映射的方式使得可以访问到更多的物理内存。而操作系统提供一个窗口映射的方式，将这些额外的内存映射到进程地址空间中。应用程序可以根据需要来申请和映射，比如Linux中的mmap系统调用。</p><p>关于现代操作系统的虚拟内存管理机制这里不展开介绍。</p></blockquote><h3 id="内核装载elf可执行文件">内核装载ELF可执行文件</h3><blockquote><p>一共有如下四个步骤：</p></blockquote><h4 id="进程创建">进程创建</h4><blockquote><p><strong>第一步是创建一个独立的虚拟地址空间。 </strong></p><p>创建虚拟空间实际上并不是创建空间而是创建映射函数所需要的相应的数据结构。在i386的Linux下，<strong>创建虚拟地址空间实际上就是分配一个页目录</strong>就可以了，甚至不设置页映射关系——这些等到后面程序发生缺页中断的时候再进行设置（<strong>当程序发生了页错误，操作系统将从物理内存中分配一个物理页，然后将该“缺页”从磁盘中读取到内存中，在设置缺页和物理页框的映射关系</strong>）。</p><p><strong>这一步本质上就是完成虚拟空间到物理内存的映射关系。</strong></p></blockquote><h4 id="完成elf和进程的映射">完成ELF和进程的映射</h4><blockquote><p><strong>第二步是读取可执行文件头，并且建立虚拟空间与可执行文件的映射关系。 </strong></p><p>很明显，这种映射关系是保存在操作系统中的一个数据结构（类似页目录）。<strong>Linux中将进程虚拟空间中的一个段叫做虚拟内存区域（VMA，Virtual Memory Area）。</strong>操作系统在内部保存这种VMA结构，就是因为当程序发生段错误的时候，可以通过查找这样一个数据结构来定位错误页在可执行文件中的位置。</p></blockquote><h4 id="运行">运行</h4><blockquote><p><strong>第三步是将CPU的指令寄存器设置成可执行文件的入口地址，启动运行。</strong></p><p>操作系统进行内核堆栈和用户堆栈的切换，CPU运行权限的切换，然后设置CPU的指令寄存器并将控制权交给进程。</p></blockquote><h4 id="缺页中断">缺页中断</h4><blockquote><p>在上述步骤都完成之后，其实可执行文件的真正指令和数据都没有被装载进内存中。<strong>操作系统只是通过可执行文件头部的信息建立起来可执行文件和进程虚存之间的映射关系而已。</strong></p><p><strong>当发生一次缺页中断的时候，操作系统将查询这个数据结构，然后找到空页面所在的VMA ，计算出相应的页面在可执行文件中的偏移，然后在物理内存中分配一个物理页面，将进程中该虚拟页与物理页之间建立映射关系，完成真正的装载。</strong></p></blockquote><p><img src="4.png" width="50%" height="50%" alt=""></p><h3 id="进程虚拟地址空间分布">进程虚拟地址空间分布</h3><h4 id="elf文件链接视图和执行视图">ELF文件链接视图和执行视图</h4><blockquote><p>操作系统装载ELF可执行文件的时候是以段segment为单位的，一般只关心跟装载有关的问题，最主要的是段的权限(可读，可写，可执行)。ELF文件中，段的权限往往只有为数不多的几种组合，基本上是三种：</p></blockquote><ul><li>以代码段为代表的权限为可读可执行的段</li><li>以数据段和BSS段为代表的权限为可读可写的段</li><li>以只读数据段为代表的权限为只读的段</li></ul><blockquote><p><strong>一般对于相同权限的段，把它们合并在一起作为一个段进行映射——成为segment。多个sections组合成为segment，装载的时候一个segment在进程虚拟空间中只有一个对应的VMA，这样可以明显的减少内部碎片，从而节省了内存空间。 </strong></p><p>所以对于系统来讲，section是链接视图，而segment是执行视图。</p><p>在操作系统装载的时候，VMA除了用来映射可执行文件中的各个segment以外，事实上进程虚拟空间中的堆栈也是以VMA的形式存在的。所以总结一下，操作系统通过给进程空间划分出一个个VMA来管理进程的虚拟空间，基本原则就是相同权限属性的，有相同映像文件的映射成一个VMA，一个进程基本上可以分为如下几种VMA区域：</p></blockquote><ul><li>代码VMA，权限只读、可执行；有映像文件</li><li>数据VMA，权限可读写，可执行；有映像文件</li><li>堆VMA，权限可读写，可执行；无映像文件，匿名，可向上扩展</li><li>栈VMA，权限可读写，不可执行；无映像文件，匿名，可向下扩展</li></ul><p><img src="5.png" width="50%" height="50%" alt=""></p><h4 id="段地址对齐">段地址对齐</h4><blockquote><p>因为装载过程是通过虚拟内存的页映射机制完成的。假设页大小默认为4096Bytes，所以如果要将一段物理内存和进程虚拟地址空间之间建立映射关系，<strong>这段内存空间的长度必须是4K的整数倍，并且这段空间在物理内存和进程虚拟地址空间中的起始地址必须是4096的整数倍。 </strong> 系统为了防止碎片化，也会采取一些优化的映射方案，这里不深入了。</p></blockquote><h2 id="动态链接">动态链接</h2><blockquote><p>静态链接有<strong>空间浪费</strong>和<strong>更新困难</strong>两个问题。</p><p>共享文件在内存中只存在一份，这样不仅仅节省内存，还增加CPU缓存的命中率。ELF动态链接文件称为<strong>.so</strong>共享对象文件，而Windows动态链接库称为<strong>.dll</strong>文件。</p><p>当程序被装载的时候，系统的<strong>动态链接器</strong>会将程序所需要的所有动态链接库装载到进程的地址空间，并且将程序中所有<strong>未决议的符号</strong>绑定到相应的动态链接库中，并进行重定位工作。<strong>也就是说动态链接器把链接这个过程从本来的程序装载前被推迟到装载的时候。</strong>这样的话<strong>动态链接与静态链接相比，性能损失大约在5%以下。这点损失换取空间上的节省和程序构建和升级时的灵活性，是想当值得的。</strong></p></blockquote><p><img src="6.png" width="50%" height="50%" alt=""></p><h3 id="地址无关代码">地址无关代码</h3><blockquote><p>由性质可以知道<strong>共享对象在编译的时候不能假设自己在进程虚拟地址空间中的位置。</strong>而可执行文件基本可以确定自己在虚拟地址空间中的起始位置。这里注意动态链接的模块概念：可执行文件模块和正常静态链接一样，而共享对象模块即动态链接库就不行。</p><p>那共享对象在被加载的时候，如何确定它在进程虚拟地址空间中的位置？</p><p>这里提出一个问题：<code>如果直接将目标文件推迟到装载的时候再链接可以吗？</code></p><p>答案是不行。<strong>动态链接模块被装载映射到虚拟空间之后，指令部分是在多个进程中共享的，由于装载的时候重定位的方法需要修改指令（会影响代码段中的绝对地址引用），所以没有办法做到同一份指令被多个进程共享。</strong></p><p>所以最终的解决办法就是：<strong>将共享的指令部分中那些需要被修改的部分分离出来，跟数据部分放在一起，这样指令部分就可以保持不变，而数据部分可以在每个进程中拥有一份副本——所谓地址无关代码(PIC, Pisition-independent Code)技术。</strong></p></blockquote><h4 id="代码段地址无关代码">代码段地址无关代码</h4><blockquote><p>这里把共享对象模块中的地址引用按照是否跨模块分为两类：模块内部引用和模块外部引用；按照不同的引用方式有可以分为指令引用和数据访问。</p></blockquote><ul><li>模块内部的函数调用，跳转等</li><li>模块内部的数据访问，比如模块中定义的全局变量，静态变量</li><li>模块外部的函数调用，跳转等</li><li>模块外部的数据访问，比如模块中定义的全局变量，静态变量</li></ul><p><img src="7.png" width="50%" height="50%" alt=""></p><blockquote><p>这里提一下模块间，因为这些全局变量的地址是跟模块装载地址有关的。ELF的做法就是在数据段中建立一个<strong>指向这些变量的指针数组，也被称为全局偏离表(Global Offset Table GOT)</strong>，当代码需要引用该全局变量的时候，可以通过GOT中相对应的项间接引用。</p></blockquote><p><img src="8.png" width="50%" height="50%" alt=""></p><blockquote><p>链接器在装载模块的时候会查找每个变量所在的地址，然后填充GOT中的各个项，以确保每个指针所指向的地址正确。GOT在数据段所以可被修改。总结一下：</p></blockquote><table><thead><tr class="header"><th></th><th>指令跳转，调用</th><th>数据访问</th></tr></thead><tbody><tr class="odd"><td>模块内部</td><td>相对跳转和调用</td><td>相对地址访问</td></tr><tr class="even"><td>模块外部</td><td>间接跳转和调用GOT</td><td>间接访问GOT</td></tr></tbody></table><h5 id="共享模块的全局变量问题">共享模块的全局变量问题</h5><blockquote><p>这里需要注意，如果一个模块引用了一个定义在共享对象的全局变量的时候。编译器无法判断该global是在同一个模块的其他目标文件中，还是定义在另一个共享对象中。</p><p>解决办法是所有的全局变量都通过GOT访问。</p></blockquote><h4 id="数据段地址无关代码">数据段地址无关代码</h4><blockquote><p>数据段中也会出现绝对地址引用的情况：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&gt; <span class="keyword">static</span> <span class="keyword">int</span> a;</span><br><span class="line">&gt; <span class="keyword">static</span> <span class="keyword">int</span> *p = &amp;a;</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure></blockquote><blockquote><p>这里的指针p就是一个绝对地址。一般来说，如果发现数据段中有绝对地址引用，编译器和链接器会产生一个重定位表。</p><p>对于可执行文件来说，默认情况下，如果可执行文件是动态链接的，那么编译器会使用PIC的方法来产生可执行文件的代码段部分，以便于不同的进程能够共享代码段，节省内存。所以一般动态链接的可执行文件中会有<code>.got</code>段。</p></blockquote><h3 id="动态链接过程">动态链接过程</h3><blockquote><p>动态链接情况下，可执行文件的装载与静态链接情况基本一样。首先操作系统会读取可执行文件的头部，检查文件的合法性，然后文件头中读取每个“Segment”的虚拟地址，文件地址和属性，并将它们映射到进程虚拟空间的相应位置，这些步骤跟前面的静态链接情况下的装载基本无异。在静态链接情况下，操作系统接着就可以把控制权转交给可执行文件的入口地址，然后程序开始执行，一切很顺利。</p><p>但是在动态链接下，操作系统还不能再装载完可执行文件之后就把控制权交给可执行文件，因为我们知道可执行文件依赖于很多共享对象，<strong>这时候可执行文件里对于很多外部符号的引用还处于无效地址的状态，即还没有跟相应的共享对象中的实际位置链接起来</strong>。所以在映射完可执行文件之后，操作系统会其中一个动态链接器。</p><p>动态链接分为三步：启动动态链接器本身，然后装载所有需要的共享对象，最后是重定位和初始化。</p></blockquote><h3 id="共享库">共享库</h3><blockquote><p>因为动态链接的诸多优点，操作系统中一般有很多共享对象——按照合理的组织和使用方式构建起来的共享库。共享库的更新会影响ABI的使用。</p></blockquote><h1 id="运行库">运行库</h1><blockquote><p>经过前面的介绍，基本上编译系统的概念都介绍完了。下面的部分涉及操作系统接口，也只是简单的介绍一下。</p><p>操作系统装载程序之后，首先运行的代码并不是<code>main</code>的第一行，而是某些别的代码，这些代码负责准备好<code>main</code>函数执行所需要的环境，并且负责调用<code>main</code>函数，这时候才可以申请内存，使用系统调用，触发异常，访问I/O。在函数返回之后会记录<code>main</code>的返回值，调用<code>atexit</code>注册的函数，然后结束进程。</p><p>运行这些代码的函数成为<strong>入口函数</strong>或者<strong>入口点</strong>。这些都是运行库的一部分。个人理解运行库的概念就是操作系统和应用层的接口，区别于系统调用，比如Glibc那样。</p></blockquote><h1 id="常用二进制工具基于llvm">常用二进制工具(基于LLVM)</h1><table><thead><tr class="header"><th>命令</th><th>说明</th></tr></thead><tbody><tr class="odd"><td><code>llvm-size</code></td><td>查看ELF文件的各个段的大小</td></tr><tr class="even"><td><code>llvm-readelf -h</code></td><td>查看ELF Header(部分)</td></tr><tr class="odd"><td><code>llvm-readelf -S</code></td><td>查看ELF Header(全部)</td></tr><tr class="even"><td><code>llvm-objdump -s</code></td><td>将所有ELF段以十六进制的方式打印</td></tr><tr class="odd"><td><code>llvm-objdump -d</code></td><td>将所有指令段反汇编 <strong>字节寻址,小端</strong></td></tr><tr class="even"><td><code>llvm-nm</code></td><td>查看ELF文件的符号表</td></tr><tr class="odd"><td><code>c++filt</code></td><td>解析函数签名</td></tr><tr class="even"><td><code>ld a.o b.o -e main -o ab</code></td><td>直接使用链接器生成可执行文件ab（将main函数作为程序入口）</td></tr><tr class="odd"><td><code>llvm-objdump -r</code></td><td>查看目标文件的重定位表</td></tr><tr class="even"><td><code>ar -t libc.a</code></td><td>查看静态链接库包含了那些文件</td></tr><tr class="odd"><td><code>clang -fPIC -shared lib.so  lib.c</code></td><td>编译一个共享对象文件</td></tr></tbody></table>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;abstract&quot;&gt;Abstract&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;从开始接触编译系统的学习已经快半年了，有些心得总结一下。&lt;/p&gt;
&lt;p&gt;什么是编译系统？我想从两个角度谈一下：&lt;/p&gt;
&lt;p&gt;首先从技术角度来讲，广义的编译系统负责将高级语言转换成为CPU可执行的二进制机器代码。编译系统包括&lt;strong&gt;编译器&lt;/strong&gt;， &lt;strong&gt;汇编器&lt;/strong&gt;，&lt;strong&gt;静态/动态链接器&lt;/strong&gt;，&lt;strong&gt;操作系统装载器&lt;/strong&gt;以及&lt;strong&gt;运行库&lt;/strong&gt;。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;编译器： 是系统前半部分的核心，负责将源文件&lt;strong&gt;.c&lt;/strong&gt;转换成为汇编文件&lt;strong&gt;.s&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;汇编/链接/装载器：是系统后半部分的核心，主要是将&lt;strong&gt;.s&lt;/strong&gt;转换为&lt;strong&gt;二进制ELF文件&lt;/strong&gt;并进一步围绕&lt;strong&gt;ELF&lt;/strong&gt;处理。&lt;/li&gt;
&lt;li&gt;运行库：是操作系统层面用来支持&lt;strong&gt;高级语言运行环境&lt;/strong&gt;比如内存池，标准API等的库文件。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;然后从专业的角度来讲，我觉得可以分为&lt;strong&gt;系统层面&lt;/strong&gt;和&lt;strong&gt;算法优化层面&lt;/strong&gt;，系统层面主要是整个编译框架比如LLVM，核心是和操作系统交互的部分，这部分内容是编译器工程师的基本功，需要掌握细节。而算法优化层面指的是&lt;strong&gt;编译优化&lt;/strong&gt;——编译器工程师的最理想的方向就是做优化，这一部分需要精通，从静态单赋值SSA等传统的静态分析优化到现在主流的深度学习推理引擎比如TVM等。&lt;/p&gt;
&lt;p&gt;本文主要是对系统层面的学习，对系统层面的学习止步于对LLVM编译框架的使用和源代码阅读。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="C++" scheme="http://haotianmcihael.github.io/categories/C/"/>
    
    
      <category term="Compiler" scheme="http://haotianmcihael.github.io/tags/Compiler/"/>
    
      <category term="LLVM" scheme="http://haotianmcihael.github.io/tags/LLVM/"/>
    
      <category term="Linker" scheme="http://haotianmcihael.github.io/tags/Linker/"/>
    
      <category term="Loader" scheme="http://haotianmcihael.github.io/tags/Loader/"/>
    
  </entry>
  
  <entry>
    <title>GCC-RISCV交叉编译工具链</title>
    <link href="http://haotianmcihael.github.io/2020/02/16/Gcc-RISCV%E4%BA%A4%E5%8F%89%E7%BC%96%E8%AF%91%E5%B7%A5%E5%85%B7%E9%93%BE/"/>
    <id>http://haotianmcihael.github.io/2020/02/16/Gcc-RISCV交叉编译工具链/</id>
    <published>2020-02-15T18:16:45.000Z</published>
    <updated>2020-06-26T02:52:19.865Z</updated>
    
    <content type="html"><![CDATA[<h3 id="absrtact">Absrtact</h3><blockquote><p>旨在x86平台上编译RISCV架构的可执行文件。</p><p>RISCV作为一款极具使命感的指令集，其软件栈也是非常完整的。交叉编译环境可以在<a href="https://github.com/riscv/riscv-gnu-toolchain" target="_blank" rel="noopener">这里</a>下载。</p><p>交叉编译工具链包括:</p><ul><li><code>GCC</code>编译器</li><li>C运行库<ul><li><code>Glibc</code> : <code>gnu</code>旗下的库，作为<code>linux</code>的标准库和内核打交道</li><li><code>Newlib</code>： 在嵌入式中使用广泛</li></ul></li><li><code>Buntils</code>二进制套件</li><li><code>GDB</code>调试器</li></ul></blockquote><a id="more"></a><h3 id="不同版本的工具链">不同版本的工具链</h3><table><thead><tr class="header"><th>Version</th><th>Note</th></tr></thead><tbody><tr class="odd"><td>Riscv-unknown-linux-gnu-gcc</td><td><sup>1</sup>32位架构的<sup>2</sup><strong>Linux版本</strong></td></tr><tr class="even"><td>Riscv-unknown-elf-gcc</td><td>32位架构的<strong>非Linux版本</strong></td></tr></tbody></table><ul><li>每个版本都有32位架构和64位架构，这里的位数和运行本机的字长无关，<strong>指的是没有通过<code>-march=</code>和<code>-mabi=</code>选项指定RISCV架构的位宽</strong></li><li><strong>Linux版本</strong>指的是<strong>该工具链使用Linux系统中运行的<code>Glibc</code>作为C运行库——</strong>另外的个版本则使用<code>Newlibc</code>库</li></ul><h3 id="交叉编译选项">交叉编译选项</h3><h4 id="march">-march=</h4><p>​ 由于<span class="math inline">\(RISCV\)</span>为模块化指令，该选项支持不同的模块化指令集组合,关于<span class="math inline">\(RISCV\)</span>模块见后面的介绍:</p><ul><li><code>rv32i[m][a][f[d]][c]</code></li><li><code>rv32g[c]</code></li><li><code>rv64i[m][a][f[d]][c]</code></li><li><code>rv64g[c]</code></li></ul><h4 id="mabi">-mabi=</h4><p>​ 该选项选定了目标平台所支持的<code>ABI</code>函数调用:</p><ul><li><code>ilp32</code>，<code>ilp32f</code>，<code>ilp32d</code> 32位架构(int-32位 long-32位 long long-64位)</li><li><code>lp64</code>，<code>lp64f</code>，<code>lp64d</code> 64位架构(int-32位 long-64位)</li></ul><h4 id="后缀">后缀</h4><table><thead><tr class="header"><th>suffix</th><th>Note</th></tr></thead><tbody><tr class="odd"><td>--</td><td>使用浮点类型操作直接使用RISCV浮点指令进行支持，但是当浮点数作为函数参数进行传递时，无论是单精度还是双精度均需要存储器中的堆栈进行传递</td></tr><tr class="even"><td>-f</td><td>使用浮点类型操作直接使用RISCV浮点指令进行支持，但是当浮点数作为函数参数进行传递时，单精度通过寄存拿起传递，双精度通过内存堆栈传递</td></tr><tr class="odd"><td>-d</td><td>使用浮点类型操作直接使用RISCV浮点指令进行支持，但是当浮点数作为函数参数进行传递时，单/双精度均可通过寄存器传递</td></tr></tbody></table><p><strong>只有<code>-march=</code>支持浮点扩展，(-mabi=)才允许加上<code>f/d</code>后缀以支持浮点运算。</strong></p><p><img src="1.png" width="50%" height="50%" alt=""></p><h3 id="riscv汇编语言简介">RISCV汇编语言简介</h3><p><img src="2.png" width="50%" height="50%" alt=""></p><h3 id="总结">总结</h3><blockquote><p>目前交叉编译平台主要有两部分: <code>riscv32-unknown-elf-gcc</code>平台编译生成<span class="math inline">\(RISCV\)</span>可执行文件，而<code>qemu-riscv32</code>平台运行<code>RISCV</code>可执行文件。</p><p><code>qemu</code>是一款很强大的虚拟机，分为系统模式和用户模式，用户模式运行可执行文件，而系统模式模拟特定指令集从而运行操作系统镜像文件。</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;absrtact&quot;&gt;Absrtact&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;旨在x86平台上编译RISCV架构的可执行文件。&lt;/p&gt;
&lt;p&gt;RISCV作为一款极具使命感的指令集，其软件栈也是非常完整的。交叉编译环境可以在&lt;a href=&quot;https://github.com/riscv/riscv-gnu-toolchain&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;这里&lt;/a&gt;下载。&lt;/p&gt;
&lt;p&gt;交叉编译工具链包括:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;GCC&lt;/code&gt;编译器&lt;/li&gt;
&lt;li&gt;C运行库
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Glibc&lt;/code&gt; : &lt;code&gt;gnu&lt;/code&gt;旗下的库，作为&lt;code&gt;linux&lt;/code&gt;的标准库和内核打交道&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Newlib&lt;/code&gt;： 在嵌入式中使用广泛&lt;/li&gt;
&lt;/ul&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Buntils&lt;/code&gt;二进制套件&lt;/li&gt;
&lt;li&gt;&lt;code&gt;GDB&lt;/code&gt;调试器&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="Assembly" scheme="http://haotianmcihael.github.io/categories/Assembly/"/>
    
    
      <category term="RISCV" scheme="http://haotianmcihael.github.io/tags/RISCV/"/>
    
      <category term="Compiler" scheme="http://haotianmcihael.github.io/tags/Compiler/"/>
    
  </entry>
  
  <entry>
    <title>从syscall的角度理解操作系统</title>
    <link href="http://haotianmcihael.github.io/2019/06/25/%E4%BB%8Esyscall%E7%9A%84%E8%A7%92%E5%BA%A6%E7%90%86%E8%A7%A3%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    <id>http://haotianmcihael.github.io/2019/06/25/从syscall的角度理解操作系统/</id>
    <published>2019-06-25T06:34:20.000Z</published>
    <updated>2020-06-30T21:49:42.494Z</updated>
    
    <content type="html"><![CDATA[<h1 id="abstract">Abstract</h1><blockquote><p>操作系统控制硬件资源并完成底层抽象。</p><p>UNIX和C的发展一脉相承。20世纪80年代以来，针对UNIX的各种<strong>标准化工作</strong>对其实现提供了具体的限制以此来完善系统不同实现之间的可移植性——<strong>ISO-C，IEEE-POSIX以及Single UNIX Specification</strong>等等。</p><p><strong>这些标准本质上是应用程序和操作系统接口的子集。</strong>一个操作系统的具体实现除了内核之外，最重要的就是对API的封装。<strong>UNIX一般通过系统调用和运行库的方式来完成应用程序接口，比如GNU为Linux开发的Gblic就支持ISO-C和IEEE-POSIX等核心标准。</strong></p><p>个人觉得从<strong>系统编程和内存</strong>的角度去理解<strong>文件系统</strong>和<strong>进程/线程</strong>等OS核心概念会比较有趣。</p></blockquote><a id="more"></a><h1 id="文件系统">文件系统</h1><blockquote><p>文件系统是一种持久性存储的系统抽象。并涉及到<strong>磁盘空间</strong>和<strong>内存空间</strong>的IO操作，比较难理解：</p><ul><li><strong>文件描述符表</strong>，<strong>打开文件表</strong>，<strong>i节点</strong>是文件存储的本质，而<strong>文件长度</strong>，<strong>文件偏移量</strong>，<strong>文件所占磁盘空间</strong>，<strong>文件用户权限</strong>是文件的重要属性。可以从<code>dup</code>，<code>lseek</code>，<code>stat/chmod</code>等函数理解</li><li><strong>理解内核获取文件的过程是最核心的</strong>。这涉及到<strong>内核页高速缓存</strong>，<strong>用户区间缓冲区</strong>，<strong>标准IO缓冲区</strong>等核心概念。可以从<code>read/write</code>，<code>fgets/fputs</code>，<code>sync/flush</code>函数理解</li><li>从<strong>用户时间</strong>和<strong>CPU时间</strong>理解标准IO函数和<code>read/write</code>等系统调用。<strong>系统调用的开销要大于函数调用。</strong>而这一切都取决于双方缓冲区的选择</li></ul></blockquote><h1 id="进程">进程</h1><blockquote><p>进程是独立功能的程序在数据集合上的一次动态执行过程。</p><ul><li>准备</li><li>就绪</li><li>阻塞</li><li>执行</li><li>结束</li></ul><p>内核为每个进程维护一个<strong>进程控制块</strong>，从<code>fork/exit/wait</code>等函数可以理解这些状态。另外<code>fork/exec</code>区分了父子进程在创建前后所共享的资源：</p><ul><li>代码段（<strong>但是子进程获得父进程数据空间，堆栈空间的副本</strong>）</li><li>打开的文件描述符</li><li>实际ID，有效ID，设置ID</li><li>存储映像</li></ul></blockquote><h1 id="线程">线程</h1><blockquote><p>每个线程都包含又表示执行环境所必需的信息，其中包括进程中表示线程的线程ID，一组寄存器值，栈，调度优先级和策略，信号屏蔽字，errno变量以及线程私有数据。<strong>一个进程的的所有信息对该进程的所有线程都是共享的，包括可执行程序的代码，程序的全局内存和堆内存，栈以及文件描述符。</strong></p><ul><li>进程是资源分配单位</li><li>线程是CPU调度单位</li></ul><p><strong>多线程切换因为相同那那个的地址空间——页表，因此切换更快。</strong></p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;abstract&quot;&gt;Abstract&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;操作系统控制硬件资源并完成底层抽象。&lt;/p&gt;
&lt;p&gt;UNIX和C的发展一脉相承。20世纪80年代以来，针对UNIX的各种&lt;strong&gt;标准化工作&lt;/strong&gt;对其实现提供了具体的限制以此来完善系统不同实现之间的可移植性——&lt;strong&gt;ISO-C，IEEE-POSIX以及Single UNIX Specification&lt;/strong&gt;等等。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;这些标准本质上是应用程序和操作系统接口的子集。&lt;/strong&gt;一个操作系统的具体实现除了内核之外，最重要的就是对API的封装。&lt;strong&gt;UNIX一般通过系统调用和运行库的方式来完成应用程序接口，比如GNU为Linux开发的Gblic就支持ISO-C和IEEE-POSIX等核心标准。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;个人觉得从&lt;strong&gt;系统编程和内存&lt;/strong&gt;的角度去理解&lt;strong&gt;文件系统&lt;/strong&gt;和&lt;strong&gt;进程/线程&lt;/strong&gt;等OS核心概念会比较有趣。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="C" scheme="http://haotianmcihael.github.io/categories/C/"/>
    
    
      <category term="OS" scheme="http://haotianmcihael.github.io/tags/OS/"/>
    
      <category term="UNIX" scheme="http://haotianmcihael.github.io/tags/UNIX/"/>
    
      <category term="Syscall" scheme="http://haotianmcihael.github.io/tags/Syscall/"/>
    
  </entry>
  
  <entry>
    <title>Mit6.828(Fall 2018) Lab2</title>
    <link href="http://haotianmcihael.github.io/2018/11/22/Mit6-828-Fall-2018-Lab2/"/>
    <id>http://haotianmcihael.github.io/2018/11/22/Mit6-828-Fall-2018-Lab2/</id>
    <published>2018-11-21T20:51:42.000Z</published>
    <updated>2020-06-26T02:31:31.026Z</updated>
    
    <content type="html"><![CDATA[<h2 id="lab2">Lab2</h2><h3 id="before-lab">Before Lab</h3><blockquote><p>Lab2的主要内容:<code>Physical Page Management</code>,<code>Virtual Memroy</code>,<code>Kernel Address Space</code>;<strong>代码实现JOS中的内存分页管理功能，有页面管理和页表管理两部分。</strong>和理论相比，代码实现需要考虑的细节真是太多。实验最终的目的是通过几个检测函数:</p></blockquote><p><img src="1.png" width="50%" height="50%" alt=""></p><blockquote><p>Lab1完成时整个JOS内存布局如下图:</p></blockquote><p><img src="9.png" width="50%" height="50%" alt=""></p><blockquote><p>切换合并Git分支的时候可能会出现冲突，需要手动注释掉Lab1中的一些测试代码来解决，这里不再赘述。 <a id="more"></a></p></blockquote><h3 id="memlayout.h和pmap.h">memlayout.h和pmap.h</h3><blockquote><p>掌握这两个文件中的函数是完成Lab2必不可少的条件，在此进行总结。<br>#### pmap.h Lab2中需要重点关注的对象有三个:物理内存，虚拟内存，空闲链表<strong><code>struct PageInfo</code></strong>。该头文件中的函数实现了三者之间的相互转化。</p></blockquote><p><img src="2.png" width="50%" height="50%" alt=""></p><blockquote><p>另外还有几个外部变量也需要重点关注一下: * <code>bootstack</code> 内核栈 * <code>pages</code> 所有物理内存按照4KB划分的对应映射数组 * <code>npage</code> 数组元素个数 * <code>kern_pgdir</code> 页目录 <strong>注意pages不是空闲链表，仅仅只是一个数组而已。而pages数组索引就是所有页式物理内存的索引。</strong>所以<code>struct PageInfo *pp</code>和物理内存关系很紧密：**(pp-pages)*4KB就是该数组元素对应的物理页面page的物理地址**，这也是<code>page2pa</code>函数的原型。</p></blockquote><h4 id="memlayout.h">memlayout.h</h4><blockquote><p>介绍了JOS虚拟内存的详细布局，定义了所有需要使用的宏。<strong>在Lab2中，我们需要完成的是内核部分虚拟地址到物理的映射。所以重点关注的是UTOP之上的布局.</strong>当然需要注意的细节就很多了，比如大小，权限，每一部分内存的作用等。</p></blockquote><p><img src="3.png" width="50%" height="50%" alt=""></p><blockquote><p>上图只是一个简化图，结合源码看会好很多，比如一些和大小有关的宏定义:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#define PGSIZE          4096            // bytes mapped by a page</span><br><span class="line">#define KSTKSIZE        (8*PGSIZE)              // size of a kernel stack</span><br><span class="line">#define PTSIZE          (PGSIZE*NPTENTRIES) // bytes mapped by a page directory entry</span><br><span class="line">#define NPTENTRIES      1024            // page table entries per page table</span><br></pre></td></tr></table></figure><blockquote><p>其中阴影部分指的是<code>page_init()</code>函数之前已经初始化好的。这是因为在<code>page_init()</code>函数之前分页机制还没有建立好，所以只能用字节分配的方法分配物理内存。这些阴影部分的物理内存都是<code>boot_alloc(uing32_t n)</code>函数分配的；而<code>page_init()</code>函数之后，所有的物理内存分配都是以页<strong>Page</strong>为单位，所以字节分配函数<code>boot_alloc()</code>也被禁用了。</p></blockquote><h4 id="总结">总结</h4><blockquote><p>我觉得整个Lab2就是在实现两种映射：<strong>物理内存到空闲链表的映射</strong>和<strong>虚拟内存到物理内存的映射</strong>。而上述的两个头文件基本讲清楚了映射需要实现的步骤和原理，应该反复阅读。初此之外还应该对C语言的<strong>强转</strong>，<strong>指针</strong>，<strong>位运算</strong>有一个很深的了解，最好先看一下TCPL。</p></blockquote><h3 id="physical-page-management">Physical Page Management</h3><blockquote><p>理论课很大的一部分缺失就是没有对物理内存布局进行讲解。而没有物理内存的基础，映射（Mapping）根本无从谈起。页面管理分析如下:<br>从代码角度看，Lab2接上Lab1中<code>kern/entry.S</code>跳转到<code>kern/init.c</code>中的<code>i386_init()</code>开始执行。可以发现<code>mem_init()</code>函数，整个Lab2就是完善这个内存函数的过程。而第一部分物理内存就是完成将物理内存映射到空闲链表上。<br>PartOne需要<strong>完成物理内存的管理，并实现物理内存的布局。</strong>这里的布局指的是对空闲链表的管理。所以<strong>我们首先要创建空闲链表，然后用空闲链表对这一整块物理内存进行管理，当然管理的单位是Page（4K）</strong>。<br>基本上需要实现的代码都在<code>pmap.c</code>中。我们用Taglist统筹一下该文件中所有的函数和宏定义:</p></blockquote><p><img src="4.png" width="50%" height="50%" alt=""></p><h4 id="boot_alloc">boot_alloc</h4><blockquote><p>彼时<code>mem_init()</code>函数中还只有一个函数————<code>i386_detect_memory()</code>。用来检测物理内存的硬件状态及其参数，其中有几个比较重要的变量: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">npages: 未分配</span><br><span class="line">npages_basemem: 160</span><br><span class="line">kern_pgdir: 未分配</span><br><span class="line">basemem: 640KB</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>其中<code>basemem=640K</code>是因为在<code>inc/memlayout.h</code>中定义过一个宏:<code>#define IOPHYSMEM 0xA0000</code>。在Lab1的内存布局中有详细的分布，在这个宏定义的地址往上是 实模式下IDT和第一部分bootsector。所以将宏定义以下到<code>0x00000000</code>的内存称为base_mem。而这部分需要被映射到空闲链表中，所以按照页式单位可以映射640K/4K=160个pages。<br>接下来就是实现代码。因为代码太多不打算贴出来，主要是做Lab的思路并分享一些完善内存布局过程中画的草稿。<br>从<code>mem_init()</code>的进度来看，首先需要实现<code>boot_alloc()</code>函数，然后用该函数直接分配物理内存给<code>kern_pgdir</code>和<code>pages</code>两个数据结构。而<code>boot_alloc</code>中值得一提就是<code>extern char end[]</code>和对齐函数<code>ROUNDUP(a, n)</code>。<strong>在Lab1完成之后，我们将内核放到<code>0x10000000</code>这个位置直到<code>end</code>结束，所以<code>end</code>是链接器做链接时内核加载结束的地址。</strong>而<code>ROUNDUP(a, n)</code>的功能是向上对齐。因为我们的操作都是4KB对齐的，所以需要在<code>end</code>后面进行对齐操作。详细见Lab1的内存布局。<strong>最后函数返回的是已分配内存的虚拟首地址。</strong><br>指导书中推荐使用断言<strong>assert</strong>进行调试的方法。不过我一般使用输出调试<strong>cprintf+return</strong>。当分配好<code>kern_pgdir</code>和<code>pages</code>之后，我们使用输出调试看一下这两个数据结构的虚拟地址: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">void boot_alloc() &#123;</span><br><span class="line">cprintf(&quot;before allocating nextfree is %x\n&quot;, nextfree);</span><br><span class="line">/*</span><br><span class="line">address allocator</span><br><span class="line">*/</span><br><span class="line">cprintf(&quot;after allocating nextfree is %x\n&quot;, nextfree);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>看一下输出结果:</p></blockquote><p><img src="5.png" width="50%" height="50%" alt=""></p><blockquote><p>然后就是实现<code>page_init()</code>函数。<strong>此时我们已经建立了一个和全部物理内存相对应的数组pages</strong>。这为接下来空闲链表的实现奠定了基础:<strong><code>page_init()</code>函数就是按照Hints中提示的物理内存的基本布局，找到那些空闲的物理内存并将其对应的pages数组元素链接起来形成一个链表。</strong>可能不好理解，直接上图:</p></blockquote><p><img src="6.jpeg" width="50%" height="50%" alt=""></p><blockquote><p>这个功能是由下面简单的几句C语言实现的： <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Part of the code:</span><br><span class="line"></span><br><span class="line"> for (i = 1; i &lt; npages_basemem; i++) &#123;</span><br><span class="line">                pages[i].pp_ref = 0;</span><br><span class="line">                pages[i].pp_link = page_free_list;</span><br><span class="line">                page_free_list = &amp;pages[i];</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p><code>page_init</code>函数实现了空闲链表。至此内存的布局已经很清楚了：</p></blockquote><p><img src="8.jpeg" width="50%" height="50%" alt=""></p><h4 id="page_alloc">page_alloc</h4><blockquote><p><code>page_alloc</code>函数实现物理内存的页式分配————分配一页空闲的物理内存。根据上面的思路:<strong>pages数组就是全部物理内存的映射，而page_free_list是空闲物理内存的映射所以是部分pages元素链成的。</strong>所以我们实际上可以通过<code>page_free_list</code>指针来确定具体的物理地址:**(page_free_list-pages)*4K**。<br>实现很简单，当中需要注意的细节初始化函数<code>memset(void *v, int c, size_t n)</code>中需要的地址是虚拟地址。这里先参考一下<code>kern_pgdir</code>的实现和初始化。</p></blockquote><p><img src="7.png" width="50%" height="50%" alt=""></p><blockquote><p>所以在实现中<strong>需要将page_free_list指向的pages元素映射到具体的物理地址<code>page2pa</code>, 然后将物理地址转化为虚拟地址<code>PADDR</code></strong>并返回。</p></blockquote><h4 id="细节">细节</h4><blockquote><p>至此物理映射到空闲链表的过程已经实现。<code>boot_alloc</code>和<code>page_alloc</code>最终返回的都是<strong>虚拟地址</strong>。还有一些宏定义和函数比如<code>PGNUM(la)</code>，<code>memset(void *v, int c, size_t n)</code>的参数也是虚拟地址。在Lab2中一个核心问题就是区分<strong>物理地址</strong>还是<strong>虚拟地址</strong>。<br>因为mmu的缘故，访问内存的时候会进行一次线性地址到物理地址的转换。所不能直接使用物理地址。<strong>我们索性所有自定义用到地址的地方都使用虚拟地址，如果需要物理地址再使用<code>PADDR(la)</code>转化。</strong></p></blockquote><h3 id="virtual-memory">Virtual Memory</h3><h4 id="question">Question</h4><blockquote><p>首先区分<strong>逻辑地址</strong>，<strong>线性地址</strong>和<strong>物理地址</strong>的概念。Lab2屏蔽了逻辑地址到线性地址的转化，也就是GDT段式存储管理。直接从线性地址开始。<br>指导书中重点讲解了虚拟内存，物理内存和指针这几个概念。觉得有一些意思，就把翻译贴上了:</p></blockquote><p><img src="10.png" width="50%" height="50%" alt=""></p><blockquote><p>另外，在之后的Lab中会遇到很多<strong>一个物理页面同时被映射到好几块虚拟内存中</strong>的情况。JOS使用<code>struct PageInfo-&gt;pp_ref</code>来管理页目录对页表页的引用的数量。见下面代码: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">mystery_t x;</span><br><span class="line">char* value = return_a_pointer();</span><br><span class="line">*value = 10;</span><br><span class="line">x = (mystery_t) value;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>根据上面的分析，指针<code>value</code>是虚拟地址。而对value进行强转并赋值的只能是虚拟地址了。</p></blockquote><h4 id="pgdir_walk">pgdir_walk</h4><blockquote><p>在实现下面的代码之前，默认熟悉<code>inc/mmu.h</code>中的所有内容。<br>该函数的作用很关键。<strong>给定一个虚拟地址，pgdir_walk函数返回一个指向该地址对应页表项的指针。而这个函数主要是为完成一部分虚拟地址到一部分物理地址的映射函数<code>boot_map_region</code>做准备的。所以只需要定位到页表项就可以，而不需要加上后12位偏移量。</strong>JOS的页目录（表）项<strong>PDE&amp;PTE</strong>的格式见下:</p></blockquote><p><img src="11.png" width="50%" height="50%" alt=""></p><blockquote><p>该函数的分析如下: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">pte_t *</span><br><span class="line">pgdir_walk(pde_t *pgdir, const void *va, int create)</span><br><span class="line">&#123;</span><br><span class="line">        uintptr_t ptIndex = PTX(va);    //PTX取出页表项的偏移量</span><br><span class="line">        struct  PageInfo * newAlloc;     //空闲链表指针</span><br><span class="line">        if(!(pgdir[PDX(va)] &amp; PTE_P)) &#123;   //&amp;位运算是取出PTE_P这一位，看页表是否存在</span><br><span class="line">                if(create) &#123;</span><br><span class="line">                        if((newAlloc = page_alloc(1)) == NULL)</span><br><span class="line">                                return NULL;</span><br><span class="line">                        newAlloc-&gt;pp_ref++;</span><br><span class="line">// |位运算是合并位； 【20】+【12】分别为页目录项（注意不是页表项）20位的地址，后12位的标志位</span><br><span class="line">                        pgdir[PDX(va)] = page2pa(newAlloc) | PTE_P | PTE_U | PTE_W;             </span><br><span class="line">                &#125;else &#123;</span><br><span class="line">                        return NULL;</span><br><span class="line">                &#125;       </span><br><span class="line">        &#125;</span><br><span class="line">        pte_t * pdPtr = KADDR(PTE_ADDR(pgdir[PDX(va)])); //取出页目录项的前20位</span><br><span class="line">        return pdPtr + ptIndex;   //具体的指向具体页表项的指针</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>其中对页表项进行重新映射是关键的操作，便于理解还可以先取出前20位地址然后再加上标志位: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line">if(create) &#123;</span><br><span class="line">                        if((newAlloc = page_alloc(1)) == NULL)</span><br><span class="line">                                return NULL;</span><br><span class="line">                  newAlloc-&gt;pp_ref++;</span><br><span class="line">                      pgdir[PDX(va)] = page2pa(newAlloc);   </span><br><span class="line">                      test = KADDR(pgdir[PDX(va)]);   //先取出20位</span><br><span class="line">  pgdir[PDX(va)] = page2pa(newAlloc) | PTE_P | PTE_U | PTE_W;</span><br><span class="line">                      return test + ptIndex;//直接返回</span><br><span class="line">                &#125;else &#123;</span><br><span class="line">                        return NULL;</span><br><span class="line">                &#125;</span><br><span class="line">...</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>分页机制原理见下图，而<code>pgdir_walk</code>函数完成的是前两部分的，最后的<code>offset</code>并不在其中:</p></blockquote><p><img src="12.png" width="50%" height="50%" alt=""></p><h4 id="boot_map_region">boot_map_region</h4><blockquote><p>接上<code>pgdir_walk</code>函数返回虚拟地址相对应的页表项指针。注意该函数的参数中有一项是权限<code>perm</code>是和<code>PTE_P</code>一起的，一旦映射就存在了。因为是页表项所以是4KB对齐的（因为屏蔽掉后12位）。我们使用页表项完成虚地址到物理地址的映射: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">static void</span><br><span class="line">boot_map_region(pde_t *pgdir, uintptr_t va, size_t size, physaddr_t pa, int perm)</span><br><span class="line">&#123;</span><br><span class="line">    int i;</span><br><span class="line">        for(i=0; i&lt;size/PGSIZE; i++, va+=PGSIZE, pa+=PGSIZE) &#123;  //4K 对齐</span><br><span class="line">                pte_t *pte = pgdir_walk(pgdir,(void*)va, 1);   </span><br><span class="line">                if(pte == NULL) panic(&quot;boot_map_region panic : out of memory!\n&quot;);</span><br><span class="line">                *pte = pa | perm | PTE_P;    //完成映射，并设置标志位来确定页表权限</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>后面几个函数实现都很简单，不再赘述。<br>至此，我们实现了内存页式管理的页表管理部分。</p></blockquote><h3 id="kernel-address-space">Kernel Address Space</h3><h4 id="mapping">Mapping</h4><blockquote><p>关于学习JOS对虚拟内存空间的整体布局。JOS是32位的操作系统，一共有4G的虚拟空间。<strong>以<code>ULIM</code>为分界线，界限以下为用户空间，以上为内核空间。</strong>其中从<code>ULIM</code>到<code>KERNBASE</code>部分作为内核的系统栈共4M空间。对于内存详细的布局可以从<code>inc/memlayout.h</code>中看到，这里的重点是因为内核和用户空间同时被映射到物理内存中，所以需要一种方式来避免两者之间相互读写。<strong>而且JOS和其他现代操作系统不同的是，整个OS只有一个页目录<code>kern_pgdir</code>（应该是每一个进程有一套完整的页表系统）。</strong><br>方法是权限位:</p></blockquote><p><img src="13.png" width="50%" height="50%" alt=""></p><blockquote><p>当一个程序试图访问一个虚拟地址的数据的时候，x86系统的保护机制运行为: * 先检查短权限位DPL，不过和页式分配无关 * 再检查页目录相应表项的访问权限，如果不过也产生异常 * 最后检查二级页表相应页表的访问权限，不够就产生异常 x86对权限的原则是<strong>不在页目录这一环节限制太多，让最终的访问控制在二级页表这一环节上在设置。</strong>实际上Intel手册上给出了一个页目录加页表的访问控制的组合控制效果:</p></blockquote><p><img src="15.png" width="50%" height="50%" alt=""></p><blockquote><p>实现<code>mem_init</code>函数的最后一部分内容，实现JOS的虚拟映射:<br>首先我们将虚拟地址中<strong>[UPAGES, sizeof(pages)]</strong>部分映射到物理地址<strong>[pages, sizeof(pages)]</strong>上。<br>然后我们将虚拟地址中<strong>[KSTACKTOP-KSTKSIZE, KSTACKTOP]</strong>部分映射到<strong>[bootstack, KSTKSIZE]</strong>上。<br>最后将<code>KERNBASE</code>以上的虚拟地址映射到所有物理地址上。 这里注意一下一个细节: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">void mem_init() &#123;</span><br><span class="line">...</span><br><span class="line">// Permissions: kernel R, user R</span><br><span class="line">kern_pgdir[PDX(UVPT)] = PADDR(kern_pgdir) | PTE_U | PTE_P;</span><br><span class="line">    ... </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>这是在<code>mem_init</code>函数申请<code>kern_pgdir</code>之后就发生的映射，而这句代码本质上就是映射函数<code>boot_map_region</code>。目的很简单————<strong>完成<code>kern_pgdir</code>自身到<code>UVPT</code>的映射</strong>。我们不难明白，整个JOS只有一个页目录。Lab基本结束，这时候的JOS内存整体布局如下:</p></blockquote><p><img src="14.png" width="50%" height="50%" alt=""></p><h4 id="question-1">Question</h4><blockquote><p>第一个问题上面刚回答过了，就是映射的细节问题。<br>在映射的时候设置了<code>PTE_U</code>的用户才有读写权限。<br>我们的<code>UPAGES</code>有4MB的空间，一个<code>struct PageInfo</code>大小8B。所以能管理的物理块为**4MB/8*4KB = 2GB<strong>。<br>管理内存的开销一共有</strong>物理映射pages<strong>,</strong>页目录<strong>,</strong>页表<strong>。如果第三个问题成立也就是说物理内存最大一共有2G的话，那pages大小一共有2G/4K<em>8=4M。而页目录<code>kern_pgdir</code>一共有4KB大小；相应的页表是4MB。一共有8MB+4KB。 ##### entry.S 这部分是在Lab1的时候遗留下来的问题，因为在系统刚启动的时候初始化过一个页目录<code>entry_pgdir</code>。关于细节可以参看<code>kern/entrypgdir.c</code>和<code>kern/entry.S</code>。这里介绍一些QEMU的新用法来进行调试（其实也在Lab2中是要求掌握的）: </em> <code>xp/Nx paddr</code> 输出从paddr物理地址开始的N个字节的值 * <code>info register</code> 输出当前寄存器的状态 * <code>info mem</code> 输出当前完成映射的虚拟地址和对应权限 * <code>info pg</code> 输出当前页表，页目录和页表项是分开的 其实我们从Lab1的时候已经调试过，<code>movl    %eax,%cr0</code>执行结束的时候</strong>打开分页机制**。但是这次可以直接看到分页的结果和映射的结果还是很爽的，结合<code>kern/entrypgdir.c</code>来看一下便很容易回答最后一个问题:</p></blockquote><p><img src="16.png" width="50%" height="50%" alt=""></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">// The entry.S page directory maps the first 4MB of physical memory</span><br><span class="line">// starting at virtual address KERNBASE (that is, it maps virtual</span><br><span class="line">// addresses [KERNBASE, KERNBASE+4MB) to physical addresses [0, 4MB)).</span><br><span class="line">// We choose 4MB because that&apos;s how much we can map with one page</span><br><span class="line">// table and it&apos;s enough to get us through early boot.  We also map</span><br><span class="line">// virtual addresses [0, 4MB) to physical addresses [0, 4MB); this</span><br><span class="line">// region is critical for a few instructions in entry.S and then we</span><br><span class="line">// never use it again.</span><br><span class="line">//</span><br><span class="line">// Page directories (and page tables), must start on a page boundary,</span><br><span class="line">// hence the &quot;__aligned__&quot; attribute.  Also, because of restrictions</span><br><span class="line">// related to linking and static initializers, we use &quot;x + PTE_P&quot;</span><br><span class="line">// here, rather than the more standard &quot;x | PTE_P&quot;.  Everywhere else</span><br><span class="line">// you should use &quot;|&quot; to combine flags.</span><br><span class="line">__attribute__((__aligned__(PGSIZE)))</span><br><span class="line">pde_t entry_pgdir[NPDENTRIES] = &#123;</span><br><span class="line">        // Map VA&apos;s [0, 4MB) to PA&apos;s [0, 4MB)</span><br><span class="line">        [0]</span><br><span class="line">                = ((uintptr_t)entry_pgtable - KERNBASE) + PTE_P,</span><br><span class="line">        // Map VA&apos;s [KERNBASE, KERNBASE+4MB) to PA&apos;s [0, 4MB)</span><br><span class="line">        [KERNBASE&gt;&gt;PDXSHIFT]</span><br><span class="line">                = ((uintptr_t)entry_pgtable - KERNBASE) + PTE_P + PTE_W</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><blockquote><p>不难发现其实在<code>entry_pgdir</code>中已经完成了<code>0x00000000-0x00400000</code>到<code>0xf0000000-0xf0400000</code>和到<code>0x00000000-0x00400000</code>的映射。而实现映射的方式也是直接讲地址写到页表项<code>entry_pgdir</code>中。<br>通过调试，发现两次页目录的创建过程中，映射的地址都不一样:</p></blockquote><p><img src="17.png" width="50%" height="50%" alt=""></p><blockquote><p>至此Lab2完成，Challenge时间关系不做了。这是<a href="https://github.com/haotianmichael/JOS" target="_blank" rel="noopener">源代码</a>.</p></blockquote><p><img src="18.png" width="50%" height="50%" alt=""></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;lab2&quot;&gt;Lab2&lt;/h2&gt;
&lt;h3 id=&quot;before-lab&quot;&gt;Before Lab&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;Lab2的主要内容:&lt;code&gt;Physical Page Management&lt;/code&gt;,&lt;code&gt;Virtual Memroy&lt;/code&gt;,&lt;code&gt;Kernel Address Space&lt;/code&gt;;&lt;strong&gt;代码实现JOS中的内存分页管理功能，有页面管理和页表管理两部分。&lt;/strong&gt;和理论相比，代码实现需要考虑的细节真是太多。实验最终的目的是通过几个检测函数:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;1.png&quot; width=&quot;50%&quot; height=&quot;50%&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Lab1完成时整个JOS内存布局如下图:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;9.png&quot; width=&quot;50%&quot; height=&quot;50%&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;切换合并Git分支的时候可能会出现冲突，需要手动注释掉Lab1中的一些测试代码来解决，这里不再赘述。
    
    </summary>
    
      <category term="C" scheme="http://haotianmcihael.github.io/categories/C/"/>
    
    
      <category term="MIT 6.828" scheme="http://haotianmcihael.github.io/tags/MIT-6-828/"/>
    
      <category term="OS" scheme="http://haotianmcihael.github.io/tags/OS/"/>
    
      <category term="Linux" scheme="http://haotianmcihael.github.io/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>Mit6.828(Fall 2018) Shell</title>
    <link href="http://haotianmcihael.github.io/2018/11/17/Mit6-828-Fall-2018-Shell/"/>
    <id>http://haotianmcihael.github.io/2018/11/17/Mit6-828-Fall-2018-Shell/</id>
    <published>2018-11-17T07:48:07.000Z</published>
    <updated>2020-06-26T02:31:09.037Z</updated>
    
    <content type="html"><![CDATA[<h2 id="hwshell">HW:Shell</h2><h3 id="abstract">Abstract</h3><blockquote><p>6.828除了Lab之外还有针对小型操作系统XV-6的Readings和HomeWork。整个6.828也是围绕这两条线（Lab和HW）在走。Lab1让我适应了JOS的实验环境和调试技巧，但是Shell和System Calls才是真正属于操作系统的知识。关于XV-6的HomeWork都是建立在Readings基础之上的，本次HW<strong>需要实现一个小型的Shell</strong>，需要阅读的Readings有:</p></blockquote><ul><li>XV-6.Chapter.0（重点资料）</li><li>man fork, (3)exec，open， close</li><li>TCPL</li></ul><blockquote><p>学习过程中最有意思的当属斩获新技能和新工具了:)介绍一个新的vim插件神器————<a href="http://vim-taglist.sourceforge.net/manual.html#taglist-using" target="_blank" rel="noopener">Taglist</a>。</p></blockquote><p><img src="27.png" width="50%" height="50%" alt=""></p><a id="more"></a><blockquote><p>简单来讲，它以目录树的形式列出当前文件中的symbol:函数名，宏定义和变量。帮助我们从宏观上快速了解源码。本身支持跳转。安装manual中讲的很清楚，就不再赘述了。下面是我的配置: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">nnoremap &lt;silent&gt; &lt;F8&gt; :TlistToggle&lt;CR&gt;&lt;CR&gt;   &quot;按f8开启Taglist</span><br><span class="line">let Tlist_Show_One_File=0                    </span><br><span class="line">let Tlist_Exit_OnlyWindow=1                  </span><br><span class="line">let Tlist_Use_Left_Window=1                 </span><br><span class="line">let Tlist_File_Fold_Auto_Close=1             </span><br><span class="line"></span><br><span class="line">     add the above Configs to your ~/.vimrc</span><br></pre></td></tr></table></figure></p></blockquote><h3 id="操作系统调用system-calls">操作系统调用——System Calls</h3><blockquote><p>操作系统的工作总结起来就是<strong>抽象</strong>和<strong>资源分配</strong>。<br>资源有内存分配，CPU调度等。而抽象指的就是系统调用。实验需要实现Shell中的命令，重定向和管道。每一个功能都是使用系统调用来实现的，所以将System Calls称为操作系统的接口一点都不过分。</p></blockquote><h3 id="executing-simple-commands">Executing Simple Commands</h3><h4 id="fork">fork</h4><blockquote><p><code>fork</code>系统调用创建一个子进程，对于父进程返回子进程的pid，对于子进程返回0。见下面代码:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">int pid = fork(); //创建了子进程</span><br><span class="line">if(pid == 0) &#123;   //父子进程都从这里开始执行，唯一区别就是pid不一样</span><br><span class="line">   printf(&quot;child: existing!\n&quot;);</span><br><span class="line">   exit();</span><br><span class="line">&#125;else if(pid &gt; 0) &#123;</span><br><span class="line">printf(&quot;parent: %d&quot;, pid);</span><br><span class="line">wait();  //父进程等待</span><br><span class="line">printf(&quot;child is done\n&quot;);</span><br><span class="line">&#125;else &#123;</span><br><span class="line">fprintf(stdeer, &quot;fork error!\n&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p><strong>其中<code>wait()</code>系统调用返回一个退出的子进程。</strong>上面这段代码其实就是整个Shell执行指令的原理:<strong>Shell提示符由父进程提供，然后创建一个子进程执行真正的命令，而父进程则<code>wait()</code>直到子进程返回,然后父进程等待下一条命令。</strong><br>执行效果如下:</p></blockquote><p><img src="28.png" width="50%" height="50%" alt=""></p><blockquote><p>回到Shell中来，我们需要修改子进程中的代码，能够正确执行简单的命令。涉及到<code>exec()</code>函数族。 #### exec exec提供了在一个进程中执行另一个进程的方法: 根据参数提供的文件路径和名称找到可执行文件，并执行。执行结束之后原调用进程的内容除进程号之外其他的全部被替换。<strong>通常和fork一起联合实现进程中执行程序。</strong><code>exec</code>函数族是由6个以exec开头的函数构成的，这6个函数的用法根据名称的不同会有些微的区别:</p></blockquote><p><img src="29.png" width="50%" height="50%" alt=""></p><h5 id="查找可执行文件的方式">查找可执行文件的方式</h5><blockquote><p>其中<code>execlp</code>，<code>execvp</code>在查找可执行文件的时候不用写文件的<strong>绝对路径</strong>，系统会自动根据环境变量“<span class="math inline">\(PATH”查找。`env`命令可以列出系统中当前的\)</span>PATH。</p></blockquote><h5 id="参数传递方式">参数传递方式</h5><blockquote><p>其中<code>execlp</code>,<code>execl</code>,<code>execle</code>这三个第五个字母是<code>l</code>的函数参数只能分别列出来————<code>const char *argv...</code>。而<code>execv</code>,<code>execve</code>,<code>execvp</code>这三个第五个字母是<code>v</code>的函数参数可以整体构造数组传递————<code>char *const argv[]</code>。<strong>所有的参数都应该以<code>NULL</code>结尾</strong>。</p></blockquote><h5 id="环境变量">环境变量</h5><blockquote><p>其中<code>execle</code>和<code>execle</code>可以在使用指定的环境变量。通过在参数末尾加上<code>char *const envp[]</code>的方法。</p></blockquote><blockquote><p>总结见下图:</p></blockquote><p><img src="30.png" width="50%" height="50%" alt=""></p><h5 id="exec函数族使用注意事项">exec函数族使用注意事项</h5><ul><li>因为exec函数经常调用失败，所以需要加上判断语句</li><li>最后参数一定用<code>NULL</code>结尾 <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">int exec() &#123;</span><br><span class="line">int ret;</span><br><span class="line">if( fork() == 0 )&#123;</span><br><span class="line">if((ret = execlp(&quot;ls&quot;, &quot;ls&quot;, &quot;-al&quot;, NULL)) &lt; 0)  //参数以NULL结束</span><br><span class="line">fprintf(stderror, &quot;execlp error!\n&quot;);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="实现命令">实现命令</h4><blockquote><p><a href="https://pdos.csail.mit.edu/6.828/2018/homework/sh.c" target="_blank" rel="noopener">Shell代码</a>分为两个部分:词法分析和命令执行；<br>实验需要修改<code>runcmd()</code>函数代码，实现具体的EXEC。需要关注函数类型，参数两个细节。根据调试知道<code>' '</code>为EXEC，而<code>execcmd</code>结构体中argv[0]中存储的是命令的名称，所以我们使用<code>execvp</code>来实现，<code>execvp</code>函数的第一个参数是可执行文件的名称，往后的参数可以用数组表示，这样可以保证该命令的可变参数得到实现。代码如下:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">case &apos; &apos;:</span><br><span class="line">           ecmd = (struct execcmd*)cmd;</span><br><span class="line">           if(ecmd-&gt;argv[0] == 0)</span><br><span class="line">               _exit(0);</span><br><span class="line">           int pid, ret;</span><br><span class="line">           if ((pid = fork()) == 0) &#123;</span><br><span class="line">               if((ret = execvp(ecmd-&gt;argv[0], ecmd-&gt;argv)) &lt; 0) &#123;</span><br><span class="line">                   fprintf(stderr, &quot;Bash: command not found:%s\n&quot;, ecmd-&gt;argv[0]);</span><br><span class="line">                   exit(-1);</span><br><span class="line">               &#125;</span><br><span class="line">               exit(0);</span><br><span class="line">           &#125; else if (pid &gt; 0) &#123;</span><br><span class="line">               wait(0);</span><br><span class="line"></span><br><span class="line">           &#125;</span><br><span class="line">           break;</span><br></pre></td></tr></table></figure><blockquote><p>注意因为是多线程编程，所以调试的时候会出现很多不可思议的现象。<strong>可以先将fork()部分注释掉，进入执行函数看一下现象，然后输出调试。</strong>执行效果如下:</p></blockquote><p><img src="31.png" width="50%" height="50%" alt=""></p><h3 id="io-redirection">I/O Redirection</h3><h4 id="文件描述符">文件描述符</h4><blockquote><p>文件描述符是很重要的概念，它使得重定向成为可能。<code>File Descriptor</code>是一个整数，常常把其指向的对象称为文件。本实验中需要了解<code>open</code>，<code>creat</code>，<code>close</code>，<code>dup2</code>等系统调用返回值就是一个文件描述符。<strong>我们需要做的就是将Shell本身的文件描述符指向其他的文件描述符，从而实现重定向。</strong>Shell保证在任何时候都有3个打开的文件描述符————<code>0-标准输入</code>,<code>1-标准输出</code>,<code>2-错误输出</code>。<br>下面是会用到的几个系统调用: * <code>open(&quot;file_name&quot;, O_RDWR):</code> 打开文件，打开成功返回值最小的文件描述符————标准输入0，打开失败返回-1 * <code>create(&quot;file_name&quot;, 0777):</code> 创建文件，返回值同open * <code>close(fd):</code> 释放一个文件描述符 * <code>dup2(oldfd, newfd):</code> 将oldfd复制给newfd</p></blockquote><blockquote><p>重定向不需要考虑子进程的问题，但还是需要深入了解<code>fork</code>函数的机理，<strong>一旦fork，父进程拥有和子进程一模一样的文件描述符</strong>。而重定向需要考虑的是在子进程中将标准输入（输出）定向到其他文件即可。<br>#### &lt; 输入重定向 较简单，一旦文件不存在直接报错: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">case &apos;&lt;&apos;:</span><br><span class="line">           rcmd = (struct redircmd*)cmd;</span><br><span class="line">           close(0);//关掉父进程的标准输入</span><br><span class="line">           if(open(rcmd-&gt;file, O_RDONLY) &lt; 0) &#123;</span><br><span class="line">               fprintf(stderr, &quot;Bash: No such file or directory:  %s\n&quot;, rcmd-&gt;file);  //输入不存在直接报错</span><br><span class="line">           &#125;else &#123;</span><br><span class="line">//一旦open函数执行成功，它返回得文件描述符就是0，所以这时候该文件就变成了输入,定向成功</span><br><span class="line">               runcmd(rcmd-&gt;cmd);  //这里就是执行EXEC部分，所以fork的时候标准输入也是关闭的（和父进程一样close）</span><br><span class="line">           &#125;</span><br><span class="line">           break;</span><br></pre></td></tr></table></figure></p></blockquote><h4 id="输出重定向">&gt; 输出重定向</h4><blockquote><p>如果文件不存在。创建新的文件时需要注意文件的权限。<code>creat</code>函数的参数0777权限为<code>-rwx rwx rwx</code>。关于umask的知识见<a href="https://my.oschina.net/alphajay/blog/112428" target="_blank" rel="noopener">这里</a>。 <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">case &apos;&gt;&apos;:</span><br><span class="line">rcmd = (struct redircmd*)cmd;</span><br><span class="line">int fd;</span><br><span class="line">if((fd = open(rcmd-&gt;file, O_RDWR)) &lt; 0) &#123;  //fd为新打开文件的文件描述符</span><br><span class="line">if((fd = creat(rcmd-&gt;file, 0777)) &lt; 0 ) &#123;</span><br><span class="line">fprintf(stderr, &quot;Bash: No such file or directory:  %s\n&quot;, rcmd-&gt;file);</span><br><span class="line">&#125;else &#123;</span><br><span class="line">dup2(fd, 1);   //将标准输出指向到新打开的文件fd上，所以输出就到了该文件上（实现了重定向）</span><br><span class="line">runcmd(rcmd-&gt;cmd);</span><br><span class="line">&#125;</span><br><span class="line">&#125;else &#123;</span><br><span class="line">dup2(fd, 1);</span><br><span class="line">runcmd(rcmd-&gt;cmd);</span><br><span class="line">&#125;</span><br><span class="line">break;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>执行效果如下:</p></blockquote><p><img src="32.png" width="50%" height="50%" alt=""></p><h3 id="implement-pipes">Implement pipes</h3><blockquote><p>管道是一个小小的内和缓冲区，它以<strong>文件描述符对</strong>的形式提供给进程，从管道的一端写数据可以从管道的另一端读取。系统调用<code>pipe</code>将数组<code>int p[2]</code>初始化为管道的文件描述符。接下来做的无非就是重定向了。 <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"> case &apos;|&apos;:</span><br><span class="line">pcmd = (struct pipecmd*)cmd;</span><br><span class="line">int p[2];</span><br><span class="line">pipe(p);    //管道初始化</span><br><span class="line">if(fork() == 0) &#123;    </span><br><span class="line">dup2(p[0], 0);   //将标准输入重定向到管道的输入p[0]上</span><br><span class="line">close(p[0]);</span><br><span class="line">close(p[1]);</span><br><span class="line">runcmd(pcmd-&gt;right);</span><br><span class="line">&#125;else &#123;</span><br><span class="line">dup2(p[1], 1);  //讲标注输出重定向奥管道额输出p[1]上</span><br><span class="line">runcmd(pcmd-&gt;left);  </span><br><span class="line">close(p[0]);</span><br><span class="line">close(p[1]);</span><br><span class="line">&#125;</span><br><span class="line">break;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>执行效果如下:</p></blockquote><p><img src="33.png" width="50%" height="50%" alt=""></p><h3 id="make-grade">Make grade</h3><blockquote><p>6.828对于HW的测试是直接在主机上测试的，编译Shell文件:<code>gcc Shell</code>得到<code>a.out</code>。已知该文件t.sh: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ls &gt; y</span><br><span class="line">cat &lt; y | sort | uniq | wc &gt; y1</span><br><span class="line">cat y1</span><br><span class="line">rm y1</span><br><span class="line">ls |  sort | uniq | wc</span><br><span class="line">rm y</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>在主机上，执行<code>./a.out &lt; t.sh</code>与在主机Shell上执行<code>source t.sh</code>对比结果，如下:</p></blockquote><p><img src="34.png" width="50%" height="50%" alt=""></p><blockquote><p>本次实验的源代码在<a href="https://github.com/haotianmichael/JOS/tree/master/Shell" target="_blank" rel="noopener">Github</a>上。<br>第一次接触Linux系统编程，很多零碎的知识和细节学起来很是吃力。不过在独立搞懂Shell的机制之后又觉得很爽，还是很期待之后的挑战:)</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;hwshell&quot;&gt;HW:Shell&lt;/h2&gt;
&lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;6.828除了Lab之外还有针对小型操作系统XV-6的Readings和HomeWork。整个6.828也是围绕这两条线（Lab和HW）在走。Lab1让我适应了JOS的实验环境和调试技巧，但是Shell和System Calls才是真正属于操作系统的知识。关于XV-6的HomeWork都是建立在Readings基础之上的，本次HW&lt;strong&gt;需要实现一个小型的Shell&lt;/strong&gt;，需要阅读的Readings有:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;ul&gt;
&lt;li&gt;XV-6.Chapter.0（重点资料）&lt;/li&gt;
&lt;li&gt;man fork, (3)exec，open， close&lt;/li&gt;
&lt;li&gt;TCPL&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;学习过程中最有意思的当属斩获新技能和新工具了:)介绍一个新的vim插件神器————&lt;a href=&quot;http://vim-taglist.sourceforge.net/manual.html#taglist-using&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Taglist&lt;/a&gt;。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;27.png&quot; width=&quot;50%&quot; height=&quot;50%&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="C" scheme="http://haotianmcihael.github.io/categories/C/"/>
    
    
      <category term="MIT 6.828" scheme="http://haotianmcihael.github.io/tags/MIT-6-828/"/>
    
      <category term="OS" scheme="http://haotianmcihael.github.io/tags/OS/"/>
    
      <category term="Linux" scheme="http://haotianmcihael.github.io/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>Mit6.828(Fall 2018) Lab1</title>
    <link href="http://haotianmcihael.github.io/2018/11/05/Mit6-828-Fall2018-Lab1/"/>
    <id>http://haotianmcihael.github.io/2018/11/05/Mit6-828-Fall2018-Lab1/</id>
    <published>2018-11-04T21:31:02.000Z</published>
    <updated>2020-06-26T02:30:46.578Z</updated>
    
    <content type="html"><![CDATA[<h2 id="lab1">Lab1</h2><h3 id="before-lab">Before Lab</h3><blockquote><p>Lab1一共有三部分:<code>Bootstrap</code>,<code>BootLoader</code>,<code>Kernel</code>;前两部分都是GDB调试为主，熟悉GDB的调试技巧和操作系统的启动流程。最后实现一小部分<code>monitor</code>中<strong>test_backtrace</strong>的功能。<br>如果<code>GDB</code>调试出错，可能是因为调试器没有链接到操作系统上：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">add </span><br><span class="line">add-auto-load-safe-path /Your Working Dir/.gdbinit</span><br><span class="line">line to your configuration file &quot;/root/.gdbinit&quot;</span><br></pre></td></tr></table></figure></p></blockquote><a id="more"></a><h3 id="pc-bootstrap">PC Bootstrap</h3><h4 id="getting-started-with-x86-assembly">Getting Started with x86 assembly</h4><h5 id="exercise-1">Exercise 1</h5><blockquote><p>JOS中使用AT&amp;T格式的汇编语言，在Lab0中介绍了相关资料。实验要求熟悉<strong>x86汇编</strong>和<strong>Inline汇编</strong>两种写法。</p></blockquote><h4 id="simulating-the-x86">Simulating the x86</h4><blockquote><p>Lab1的工作目录: * <code>/boot</code>目录包含和启动相关的文件。 * <code>/kern</code>目录包含内核和监视器相关的文件。 * <code>/lib</code>目录包含一些链接使用的库函数比如printf。 * <code>/inc</code>目录包含头文件，申明了需要使用的数据结构。 * <code>/obj</code>目录是make之后相应的反汇编代码，供调试使用。 Lab1主要分析<code>/obj/kern/kern.asm</code>和<code>obj/boot/boot.asm</code>两个文件。这两个文件分别是<code>/boot</code>和<code>/kern</code>目录下的文件编译连接后的目标文件被反汇编而生成的。之所以这样做是因为反汇编之后可以看到每一条指令在内存中的绝对地址。这样调试的时候非常方便。至于<strong>实模式</strong>和<strong>保护模式</strong>下的地址转换在后续的实验中也会重点关注。</p></blockquote><h4 id="the-pcs-physical-address-space">The PC's Physical Address Space</h4><blockquote><p>最早的8086机器只有1Mb的寻址空间，后面的80x86系列机器为了向后兼容所以衍生出实模式和保护模式这两种概念，但是BIOS一直都存在于从<code>0x00000000</code>到<code>0x00100000</code>这1Mb的空间中。JOS的内存布局也是规定只有<code>0x00000000</code>到<code>0x10000000</code>这256MB大小的空间，但是默认地址线是32位的。<strong>这个很重要，因为后面实验对JOS进行虚拟内存分配的时候页框地址最大也只能是<code>0x10000000</code>，内核如何处理映射就是一个问题。</strong>具体布局见下图:</p></blockquote><p><img src="6.png" width="50%" height="50%" alt=""></p><h4 id="the-rom-bios">The ROM BIOS</h4><h5 id="exercise-2">Exercise 2</h5><blockquote><p>这里介绍一下常用的GDB调试参数: * <code>si</code>: 单步调试 * <code>info register</code>: 查看当前各寄存器的值 * <code>x/Nx Addr</code>: 查看内存地址Addr之后<strong>N字</strong>的内容 * <code>x/Ni Addr</code>: 查看内存地址Addr之后的N条反汇编指令 * <code>x/Ni $eip</code>: 查看CPU当前执行的下N条指令（其中$eip可以换成不同的寄存器，这里就不一一列举了） 通过分析前面的JOS内存的布局，BIOS作为固件存在于<code>0xf0000</code>到<code>0xfffff</code>这64KB的空间上。<strong>注意启动为实模式，CPU的地址线寻址都是20位的。</strong>启动仿真器之后看到第一条代码停在了<code>0xffff0</code>的地址上，说明这是BIOS程序的入口。<code>0xffff0</code>到BIOS程序顶部<code>0xfffff</code>只有16字节的空间，需要更大的运行空间，因此第一条指令<code>ljmp   $0xf000,$0xe05b</code>；也就是跳转到<code>0xfe05b</code>这个地址正式开始运行BIOS的程序。<br>BIOS本身也是一个很复杂的系统，但是和OS关系不大。通过调试BIOS的代码我们知道了它的功能: * 建立中断向量表及相应的中断例程 * 初始化部分硬件及自检(POST) * 激活<code>INT 19</code>中断来加载启动盘第一扇区512字节的内容到内存(Linux) 这里应该说明第一扇区的内容是<code>/boot</code>目录下的内容:<code>boot.S</code>和<code>main.c</code>,这两个文件最后被编译链接成为可执行目标文件(这里需要ELF的知识)。在Linux系统中，这个可执行目标文件大小就是512字节，正好放在启动盘的第一个sector中,被称为<strong>bootsect</strong>。而BIOS的主要工作之一就是将<code>bootsect</code>加载到内存中，执行完任务之后，BIOS跳转到<code>bootsect</code>的初始位置。至此BIOS将权限交给操作系统，OS继续完成剩下的启动过程。 这里发现了一个小问题:就是在查看<code>/obj/boot</code>目录的时候，发现出现了<code>boot.out</code>和<code>boot</code>两个文件，如下:</p></blockquote><p><img src="7.png" width="50%" height="50%" alt=""></p><blockquote><p><code>strip</code>命令将ELF文件中的符号表信息等调试信息删掉，减少文件本身的大小。理论上讲<code>bootsect</code>最后应该被链接到启动盘<code>kernel</code>的第一个扇区上，换句话讲最后的<strong>操作系统启动盘</strong>只有一个。查看<code>/obj/boot</code>目录下生成的这个两个文件:<code>boot.out</code>的格式为<strong>可执行目标文件</strong>，<code>boot</code>是<strong>x86 boot sector</strong>的格式，这两个文件中一定有一个作为<code>bootsect</code>链接使用。见下图:</p></blockquote><p><img src="8.png" width="50%" height="50%" alt=""></p><blockquote><p>可是经过分析这两个文件大小都是<strong>8K</strong>,经过优化之后也要<strong>4K</strong>大小，和<code>512Bytes</code>相差太远。而且文件<code>boot</code>还不可以使用二进制工具分析。这个细节方面应该和<strong>链接</strong>关系很大，<a href="https://book.douban.com/subject/3652388/" target="_blank" rel="noopener">《程序员的自我修养》</a>这本书是国内少有的讲链接装载的好书，有时间再拜读吧。</p></blockquote><h3 id="the-boot-loader">The Boot Loader</h3><h4 id="code">Code</h4><h5 id="分析bootboot.s">分析boot/boot.S</h5><blockquote><p>分析一下<code>/boot/boot.S</code>这个文件:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br></pre></td><td class="code"><pre><span class="line">##############1.申明部分###############</span><br><span class="line"></span><br><span class="line">.set PROT_MODE_CSEG, 0x8       </span><br><span class="line">.set PROT_MODE_DSEG, 0x10       </span><br><span class="line">.set CR0_PE_ON,      0x1        </span><br><span class="line"></span><br><span class="line">#################2.16位实模式部分##########</span><br><span class="line"></span><br><span class="line">.globl start</span><br><span class="line">start:</span><br><span class="line">  .code16                     </span><br><span class="line">  cli                      %禁止中断   </span><br><span class="line">  cld                        </span><br><span class="line"></span><br><span class="line">  xorw    %ax,%ax            %清零段寄存器</span><br><span class="line">  movw    %ax,%ds            </span><br><span class="line">  movw    %ax,%es            </span><br><span class="line">  movw    %ax,%ss            </span><br><span class="line">  </span><br><span class="line">###开启A20地址线##</span><br><span class="line">seta20.1:</span><br><span class="line">  inb     $0x64,%al          % 将64号端口的内容读到%al寄存器中   </span><br><span class="line">  testb   $0x2,%al           %检测%al的第二位是否为零（代表输入缓冲区是否为满，可以对端口继续读写）</span><br><span class="line">  jnz     seta20.1           %不为零则重复执行seta20.1</span><br><span class="line"></span><br><span class="line">  movb    $0xd1,%al          %将$0xd1写入%al寄存器</span><br><span class="line">  outb    %al,$0x64          %将%al寄存器的值写入64号端口</span><br><span class="line"></span><br><span class="line">seta20.2:                     %同样的方式</span><br><span class="line">  inb     $0x64,%al              </span><br><span class="line">  testb   $0x2,%al</span><br><span class="line">  jnz     seta20.2</span><br><span class="line"></span><br><span class="line">  movb    $0xdf,%al              </span><br><span class="line">  outb    %al,$0x60          %两个端口读写   激活A20地址线</span><br><span class="line">###实模式————&gt;保护模式##</span><br><span class="line">  lgdt    gdtdesc    %加载GDT</span><br><span class="line">  movl    %cr0, %eax</span><br><span class="line">  orl     $CR0_PE_ON, %eax    %取出%CR0寄存器的第零位并置1</span><br><span class="line">  movl    %eax, %cr0      %实模式向保护模式的转换</span><br><span class="line"></span><br><span class="line">  ljmp    $PROT_MODE_CSEG, $protcseg    </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">###########3.32位保护模式部分#############</span><br><span class="line"></span><br><span class="line">  .code32</span><br><span class="line">protcseg:</span><br><span class="line">  movw    $PROT_MODE_DSEG, %ax   </span><br><span class="line">  movw    %ax, %ds               </span><br><span class="line">  movw    %ax, %es               </span><br><span class="line">  movw    %ax, %fs               </span><br><span class="line">  movw    %ax, %gs               </span><br><span class="line">  movw    %ax, %ss               </span><br><span class="line"></span><br><span class="line">  movl    $start, %esp</span><br><span class="line">  call bootmain</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">spin:   %BUG Point！</span><br><span class="line">  jmp spin</span><br><span class="line"></span><br><span class="line">###########4.数据区################</span><br><span class="line">.p2align 2                       </span><br><span class="line">gdt:</span><br><span class="line">  SEG_NULL</span><br><span class="line">  SEG(STA_X|STA_R, 0x0, 0xffffffff)</span><br><span class="line">  SEG(STA_W, 0x0, 0xffffffff)   </span><br><span class="line"></span><br><span class="line">gdtdesc:</span><br><span class="line">  .word   0x17                     </span><br><span class="line">  .long   gdt</span><br></pre></td></tr></table></figure><blockquote><p><strong>申明部分</strong>两个宏定义是在保护模式下的段描述符的申明，对于保护模式后面还有介绍，这里权把它们看成是<code>CS</code>和<code>DS</code>段寄存器。<br><strong>实模式部分</strong>首先关掉了中断<code>cli</code>，因为接下来就要进行实模式下中断服务例程向保护模式下IDT中断描述符的交接。期间系统无法响应正常的中断服务。<code>cld</code>将标志位DF置零，DF和字符串操作相关;清零段寄存器。并打开了第21（A20）到第32根地址线，在没打开之前高于1MB的地址总是会“回滚”到0，这也是Linux检测保护模式和实模式的一个方法。<strong>接下来加载GDT，<code>movl %eax, %cr0</code>这句话是将系统控制寄存器<code>%CR0</code>的第0位（PE位）置一，意味着处理器工作方式变为保护模式。</strong>这里没有直接对<code>%CR0</code>进行操作，而是通过<code>%eax</code>来实现主要是为了不破坏寄存器的其他位，值得借鉴。最后<code>ljmp $PROT_MODE_CSEG, $protcseg</code>是跳转指令，<strong>需要注意的是现在已经是保护模式了，而在实模式下和保护模式下对于地址的转换方式已经从段寻址变成GDT寻址。</strong>这里只需要知道跳转到了<code>protcseg</code>这个地址。<br><strong>保护模式</strong>初始化了重要的段寄存器，然后跳转到<code>boot/main.c/bootmain</code>函数，开始将启动盘剩余的内核部分载入内存。<strong>注意在AT&amp;T格式的汇编语言中，操作数的字长是由操作符的最后一个字母决定的，后缀'b','w','l'分别表示字节（byte:8位），字（word:16位）和长字（long:32位）。</strong><br><strong>数据区部分</strong>都是在保护模式下建立GDT全局描述符的时候相关的宏定义。</p></blockquote><h5 id="分析bootmain.c">分析boot/main.c</h5><blockquote><p>分析一下<code>boot/main.c</code>这个文件:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">#define SECTSIZE 512</span><br><span class="line">#define ELFHDR ((struct Elf *) 0x100000)</span><br><span class="line"></span><br><span class="line">void readsect(void*, uint32_t);</span><br><span class="line">void readseg(uint32_t, uint32_t, uint32_t);</span><br><span class="line">void bootmain(void);</span><br></pre></td></tr></table></figure><blockquote><p>先看这两个宏定义，<code>SECTSIZE</code>是磁盘一个<code>sector</code>的大小，一般读写操作都要求地址对齐，这个宏定义就会派上用场。<code>ELFHDR</code>是一个指向ELF文件结构体的指针，不得不说<strong>强转</strong>是C语言最有力的工具之一，由此可知<code>0x100000</code>便是内存载入的首地址。这个地址也是BIOS程序结束的地方，可见JOS对于内存的规划分配还是很精确的。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">void</span><br><span class="line">readseg(uint32_t pa, uint32_t count, uint32_t offset)</span><br><span class="line">&#123;</span><br><span class="line">    //pa是加载地址    count是加载文件大小    offset是在内核文件中的偏移量</span><br><span class="line">        uint32_t end_pa;</span><br><span class="line">        end_pa = pa + count;   //结束地址</span><br><span class="line">        pa &amp;= ~(SECTSIZE - 1);  //将加载地址与最小单位512对齐</span><br><span class="line">        offset = (offset / SECTSIZE) + 1;   //计算加载部分在启动盘中的哪一个sector中，因为sector从1开始算起</span><br><span class="line">        while (pa &lt; end_pa) &#123;</span><br><span class="line">                readsect((uint8_t*) pa, offset);  </span><br><span class="line">                pa += SECTSIZE;    //这句话表明 最终分配的内存有可能是大于实际需要内存的，因为总是以512为最小分配单元分配的</span><br><span class="line">                offset++;  </span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p><code>readseg</code>函数中的对齐，看下面这个sample函数:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line"></span><br><span class="line">long SECSIZE = 0x200;  </span><br><span class="line">long pa = 0x30301;</span><br><span class="line">long to = 0x10387;</span><br><span class="line"></span><br><span class="line">to &amp;= ~(SECSIZE - 1);</span><br><span class="line">pa &amp;= ~(SECSIZE - 1);</span><br><span class="line"></span><br><span class="line">printf(&quot;secsize is : %lx\n&quot;, SECSIZE);</span><br><span class="line">printf(&quot;pa is : %lx\n&quot;, pa);</span><br><span class="line">printf(&quot;to is : %lx\n&quot;, to);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>之所以需要对齐，因为磁盘和内存之间读写如果按照规定的最小粒度进行，CPU的访问性能会提高。具体见<a href="https://www.ibm.com/developerworks/library/pa-dalign/" target="_blank" rel="noopener">IBM.alignment</a>。上述函数的执行结果见下图:</p></blockquote><p><img src="9.png" width="50%" height="50%" alt=""></p><blockquote><p><code>readsect</code>函数都是一些端口操作。也没仔细研究Orz。下面主要看一下<code>bootmain</code>函数剩下的部分:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">   ph = (struct Proghdr *) ((uint8_t *) ELFHDR + ELFHDR-&gt;e_phoff);</span><br><span class="line">   eph = ph + ELFHDR-&gt;e_phnum;</span><br><span class="line">   for (; ph &lt; eph; ph++)</span><br><span class="line">            readseg(ph-&gt;p_pa, ph-&gt;p_memsz, ph-&gt;p_offset);</span><br><span class="line">((void (*)(void)) (ELFHDR-&gt;e_entry))();</span><br></pre></td></tr></table></figure><blockquote><p>关于JOS对于ELF文件的申明见<code>/inc/elf.h</code>。一共有三个结构体，<code>Elf</code>代表文件头；<code>Proghdr</code>代表加载时候segment的信息；<code>Secthdr</code>代表运行时section的信息；<br>在Lab0中，我们分析过对一个程序加载的时候是以<code>segments</code>为最小粒度的。所以我们重点关注<code>Elf</code>和<code>Proghdr</code>这两个结构体的细节。<br>* <code>e_phoff</code>: segment表在整个程序中的偏移量 * <code>e_phnum</code>: segment表项的个数 * <code>ph-&gt;p_pa</code>: 该segment在内存中的加载地址 * <code>ph-&gt;p_memsz</code>: 该segment的大小 * <code>ph-&gt;p_offset</code>: 该segment相对于表起始地址的偏移量 所以<code>ph</code>就是segment段表的开始地址。而<code>eph</code>就是segment段表的项数。这个循环是把所有的segments都加载到相对应的内存地址中。<br>加载结束后，再次跳转<code>((void (*)(void)) (ELFHDR-&gt;e_entry))()</code>,通过调试反汇编代码，发现跳转的这个地址为<code>call *0x10018</code>,注意这里是一个指针，可以看到内核函数真正的入口在<code>0x0010000c</code>。所以正确的操作见下图:</p></blockquote><p><img src="10.png" width="50%" height="50%" alt=""></p><h5 id="exercise-3">Exercise 3</h5><blockquote><p>在上面分析的过程基本上回答了所有的问题: * <code>%CR0</code>标志位的改变意味着实模式切换到保护模式 * <code>call *0x10018</code>是最后一条BootLoader执行的指令，<code>movw $0x1234, 0x472</code>是内核第一条指令，这条指令在<code>0x0010000c</code>这个入口地址。 * 通过读取ELF文件中关于加载segments的信息，因为main函数最开始加载了8个sectors的内容到内存中，这部分内容就是和ELF格式和细节信息有关系。</p></blockquote><h4 id="loading-the-kernel">Loading the Kernel</h4><h5 id="exercise-4">Exercise 4</h5><blockquote><p>重温C语言指针，实验中推荐<a href="https://book.douban.com/subject/1236999/" target="_blank" rel="noopener">TCPL</a>。分析实验中给出的部分示例代码，体会指针的魅力:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">c = (int *) ((char *) c + 1);</span><br><span class="line">*c = 500;</span><br><span class="line">printf(&quot;5: a[0] = %d, a[1] = %d, a[2] = %d, a[3] = %d\n&quot;,</span><br><span class="line">a[0], a[1], a[2], a[3]);</span><br></pre></td></tr></table></figure><blockquote><p>这段代码将c强转成为char类型的指针之后加一，然后赋值为500；问题就出现在char类型和int类型的指针大小是不一样的。int类型为4个字节，而char类型只有1个字节;所以这样势必会导致赋值的时候改变数组中原来的布局。已知a[1]原来的值400，16进制为0x190;a[2]的值原来为301，16进制为0x12D;500的16进制为0x1F4;一个地址单元为一个字节，为见下图:</p></blockquote><p><img src="11.png" width="50%" height="50%" alt=""></p><blockquote><p>可以看到读写操作是按照字节为最小单元，最后导致a[1]的值变成0x1F490也就是十进制的128144；而a[2]由于被抹掉以个字节，所以变成0x100也就是256。下图是最终的运行结果:</p></blockquote><p><img src="12.png" width="50%" height="50%" alt=""></p><h5 id="exercise-5">Exercise 5</h5><blockquote><p>关于链接器和加载器，最经典的当然是这本<a href="https://www.iecc.com/linker/" target="_blank" rel="noopener">loader and linker</a>。这是中译本<a href="https://paper.seebug.org/papers/Archive/refs/elf/《链接器和加载器》中译本.pdf" target="_blank" rel="noopener">ll.Ch</a>。不过我做实验的时候没有想明白他们为什么要在这里引入这两个复杂的概念；题目很简单，但是不知道<code>LMA</code>和<code>VMA</code>是和虚拟内存有关系还是和链接器，加载器有关系。后来用<code>readelf -h kernel</code>发现<code>VMA</code>就是虚拟地址，所以这里他们所谓的linking address和loading address只是实验中自行定义的字面意思<strong>加载的地址</strong>和<strong>执行的地址</strong>，和链接器加载器没有关系！！另外在Lab0中已经分析过加载时候的<code>section</code>是所有<code>sections</code>组合成的一个聚合节，这里不考虑链接时候的单个<code>section</code>的概念。<strong>这样的话，就可以明白实验讲解的思路————引入使用ELF中<code>segments</code>的概念来解释加载过程，而加载过程时候已经开启了分页机制，所以这时候用<code>loading address</code>和<code>linking address</code>来解释<code>0xf0100000</code>和<code>0x00100000</code>的区别。而实际上<code>0xf0100000</code>就是映射到<code>0x00100000</code>上，相关细节在Kernel部分会继续分析。</strong> readelf和objdump的操作见下图:</p></blockquote><p><img src="14.png" width="50%" height="50%" alt=""></p><blockquote><p>将链接地址改掉之后，第一条报错的应该是和链接地址直接相关的指令，跳转指令<code>ljmp $PROT_MODE_CSEG, $protcseg</code>;</p></blockquote><h5 id="exercise-6">Exercise 6</h5><blockquote><p>前面分析过<code>#define ELFHDR ((struct Elf *) 0x100000)</code>所以我们可以知道，内核的装载地址(load address)就是<code>0x00100000</code>，而内核开始执行的地址应该是<code>e_entry</code>也就是<code>0x0010000c</code>。从BIOS到BootLoader的时候，内核还没有开始装载。所以<code>0x00100000</code>是空的。</p></blockquote><p><img src="13.png" width="50%" height="50%" alt=""></p><h3 id="the-kernel">The Kernel</h3><h4 id="using-virtual-memory-to-work-around-position-dependence">Using virtual memory to work around position dependence</h4><blockquote><p>Lab1中的地址映射: <code>0x00000000</code>————<code>0x00400000</code>映射到<code>0x00000000</code>————<code>0x00400000</code>；<code>0xf0000000</code>————<code>0xf0400000</code>映射到<code>0x00000000</code>————<code>0x00400000</code>;<br>在实验过程中关注了一下地址: 在执行到<code>kern/entry.S</code>文件之前，所有的内存地址都是物理地址（书中代言为线性地址，但是线性地址=物理地址所以没有太大区分）。直到<code>%CR0</code>寄存器的PG标志位被置零。这时候分页机制才被激活。<code>0x00100000</code>和<code>0xf0100000</code>都映射到<code>0x00100000</code>，所有两个地址存储的数值是一样的。而在映射还没有建好之前是不一样的。</p></blockquote><h5 id="exercise-7">Exercise 7</h5><blockquote><p><code>%CR0</code>寄存器PG位控制分页机制的实现。看到<code>mov %eax, %cr0</code>执行结束之后，索引地址变成了<code>0xf0100010</code>。地址映射不对，第一条错误的指令一定会是跳转指令<code>jmp *%eax</code>。具体的分页细节在Lab2中分析。实验操作见下图:</p></blockquote><p><img src="15.png" width="50%" height="50%" alt=""></p><h4 id="code-1">Code</h4><h5 id="分析kernprintf.c">分析kern/printf.c</h5><blockquote><p>下面分析一下<code>kern/printf.c</code>这个文件:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">static void putch(int ch, int *cnt)</span><br><span class="line">&#123;</span><br><span class="line">        cputchar(ch);//显示屏输出函数console</span><br><span class="line">        *cnt++;   //每进行一次显示屏输出，参数自加</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int vcprintf(const char *fmt, va_list ap)</span><br><span class="line">&#123;</span><br><span class="line">        int cnt = 0;   //cnt将自身指针传给putch(int, int*)，在函数里进行自加</span><br><span class="line">        vprintfmt((void*)putch, &amp;cnt, fmt, ap);  //格式判断</span><br><span class="line">        return cnt;                  //返回最终输出的参数个数</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int cprintf(const char *fmt, ...)</span><br><span class="line">&#123;</span><br><span class="line">        va_list ap;</span><br><span class="line">        int cnt;</span><br><span class="line">        va_start(ap, fmt);</span><br><span class="line">        cnt = vcprintf(fmt, ap);//变参传递</span><br><span class="line">        va_end(ap);</span><br><span class="line">        return cnt;  //返回参数个数</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p><code>cprintf</code>是主函数，这里用到了C语言的变参特性。变参在C库<code>stdarg.h</code>中定义至少要有一个固定的参数:<code>fun(const char *fmt, ...)</code>。主要有三个宏组成: * <code>va_list</code>: 申明可变参数指针ap，依次指向省略号表示的可变参数 * <code>va_start(ap, lastFix)</code>: 初始化ap，开始指向第一个可变参数 * <code>va_arg(ap, type)</code>: 将ap指向下一个可变参数 * <code>va_end(ap)</code>: 清除ap指针，结束函数 可以看到变参<code>fmt</code>和变参指针<code>ap</code>被一路传到<code>vcprintf</code>———&gt;<code>vprintfmt</code>。在<code>vprintfmt</code>中使用。该文件中重点关注两个函数: * 在<code>lib/printfmt.c</code>中的函数: <code>vprintfmt(void (*putch)(int, void*), void *putdat, const char *fmt, va_list ap)</code> * 在<code>kern/console.c</code>中的函数: <code>cputchar(int c)</code> 经过观察之后就可以知道: <strong><code>vprintfmt</code>函数判断字符串输出参数的类型并调用<code>cputchar</code>函数将相应的输出显示到显示屏上</strong>。<br>在函数分析之前再介绍一个vim的新技能:搜索高亮<code>:match Search /xxx/</code>,可以对现在正在关注的关键字进行高亮显示，调试的时候很有用:</p></blockquote><p><img src="22.png" width="50%" height="50%" alt=""></p><h6 id="分析函数vprintfmt">分析函数vprintfmt</h6><blockquote><p><code>vprintfmt</code>函数其实很简单，主体就是一个while循环:在遇到<code>%</code>之前直接输出，遇到<code>%</code>之后开始判断格式并输出。</p></blockquote><h6 id="分析函数cputchar">分析函数cputchar</h6><blockquote><p><strong>在<code>vprintfmt</code>函数中需要注意一个变量:<code>ch = *(unsigned char *) fmt++</code>。<code>ch</code>代表了当前ap指针指向的变参，也就是我们需要输出的内容。</strong><code>cputchar(ch)</code>中的参数正好就是这个<code>ch</code>。我们使用Ctags跟踪这个<code>ch</code>参数经过的函数:<code>putch(int ch, int *)</code>————&gt;<code>cputchar(int ch)</code>————&gt;<code>cons_putc(int ch)</code>。现在基本上可以看清所有的控制台输出操作都是定义在<code>console.c</code>文件中的。我们直接分析一下<code>cons_putc(int ch)</code>这个函数:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">static void cons_putc(int c)</span><br><span class="line">&#123;</span><br><span class="line">        serial_putc(c);</span><br><span class="line">        lpt_putc(c);</span><br><span class="line">        cga_putc(c);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>上三个子函数都涉及到的内联汇编<code>inb()</code>和<code>outb()</code>两个函数在<code>inc/x86.h</code>中有定义。其中<code>serial_putc</code>是串口输出，<code>lpt_putc</code>是并口输出，而<code>cga_putc</code>是显示屏输出，具体不细分析了: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">for (i = 0; !(inb(0x378+1) &amp; 0x80) &amp;&amp; i &lt; 12800; i++)</span><br><span class="line">       delay();    //判断数据缓冲区寄存器是否为空，为空则一直循环</span><br><span class="line">   outb(0x378+0, c);     //将C参数代表的内容写到端口中</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>另外，在<code>cga_putc(c)</code>函数中，有一段代码: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">if (crt_pos &gt;= CRT_SIZE) &#123;</span><br><span class="line">    int i;</span><br><span class="line">    memmove(crt_buf, crt_buf + CRT_COLS, (CRT_SIZE - CRT_COLS) * sizeof(uint16_t));</span><br><span class="line">    for (i = CRT_SIZE - CRT_COLS; i &lt; CRT_SIZE; i++)</span><br><span class="line">        crt_buf[i] = 0x0700 | &apos; &apos;;</span><br><span class="line">    crt_pos -= CRT_COLS;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>我们知道<code>cga_putc(c)</code>函数是用于显示屏输出的函数，而查看这几个宏定义:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">static uint16_t crt_pos //光标</span><br><span class="line">#define CRT_ROWS        25   //显示屏行</span><br><span class="line">#define CRT_COLS        80    //显示屏列</span><br><span class="line">#define CRT_SIZE        (CRT_ROWS * CRT_COLS)  //显示屏面积</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>简单来讲就是如果输出满屏之后，需要页面向上滚动一行。</p></blockquote><h4 id="formatted-printing-to-the-console">Formatted Printing to the Console</h4><h5 id="exercise-8">Exercise 8</h5><blockquote><p>经过上面的分析之后，知道格式化输出是在<code>kern/printfmt.c/vprintfmt()</code>这个函数中。找到相应的位置修改就行。</p></blockquote><p><img src="16.png" width="50%" height="50%" alt=""></p><h5 id="questions">Questions</h5><blockquote><p>这是Exercise.8后面的一些问题，当然很简单了:<br>1.cputchar(int c) 2.一句话，就是满屏时候的处理方法 3.主要是变参指针<code>ap</code>和格式化变参<code>fmt</code>的指向问题，之前分析过<br>4.将代码加到<code>kern/monitor.c</code>中，输出见下图。因为57616的十六进制就是ell。而0x0x00646c72在小端存储的机器上用char*表示就是<code>rld\0</code>。如果想要用大端存储的话，只要反过来就行0x726c6400。</p></blockquote><p><img src="17.png" width="50%" height="50%" alt=""></p><blockquote><p>5.根据变参的定义，如果fmt参数不够，那最终ap指针会指向一个未知内存区域。所以输出的数不一定。见下图:</p></blockquote><p><img src="18.png" width="50%" height="50%" alt=""></p><blockquote><p>6.关于变参的定义是在<code>inc/stdarg.h</code>中,我们看一下<code>va_arg</code>是如何一次一次取出变参的: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">#define va_arg(ap, type) __builtin_va_arg(ap, type) //Fall 2018</span><br><span class="line"></span><br><span class="line">#define va_arg(ap, type) \</span><br><span class="line">(*(type *)((ap) += __va_size(type), (ap) - __va_size(type)))  //Fall 2015</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>可以看到va_arg是通过地址往后增长来取出下一个变参的。<strong>而正常编译器是从右往左的顺序将参数入栈的（因为栈是从高地址向低地址延伸的）。</strong>如果这时候栈的顺序变了，那只需要将va_arg函数中的对地址的加法改为减法就行。</p></blockquote><h4 id="the-stack">The Stack</h4><h5 id="exercise-9">Exercise 9</h5><blockquote><p>关于栈的定义在<code>kern/entry.S</code>中。见下图:</p></blockquote><p><img src="19.png" width="50%" height="50%" alt=""></p><blockquote><p>因为栈是从高地址向低地址延伸的，所以<code>mov $(bootstacktop) %esp</code>将栈顶地址赋值给<code>%esp</code>寄存器。而在数据段中对栈大小的定义也很清楚了————<code>KSTKSIZE</code>是一个宏定义，大小为8x4KB=32KB。</p></blockquote><h5 id="exercise-10">Exercise 10</h5><blockquote><p>需要深入了解栈调用的机制，就需要了解<strong>stack pointer</strong><code>%esp</code>和<strong>base pointer</strong><code>%ebp</code>这两个寄存器的用法。我们通过调试<code>test_backtrace()</code>函数来深入了解。(实际上只要有函数调用就会有栈，但很明显test_backtrace不是第一个调用的函数。当然你也可以从第一个函数i386_init开始，这个时候%esp也刚刚完成初始化0xf0110000,感觉会更爽一些)。<br><code>test_backtrace</code>函数的调用发生在<code>kern/init.c/i386_init()</code>中，所以我们第一个断点设置在<code>0xf01000de</code>。从反汇编代码中可以看出，开始调用<code>test_backtrace</code>之前，还有两条指令:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">movl $0x5, (%esp)</span><br><span class="line">call f0100040 &lt;test_backtrace&gt;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>在还没有开始执行的时候，查看一下寄存器的状态<code>info registers</code>:<code>%esp</code>的值为<code>0xf010ffe0</code>，<code>%ebp</code>的值为<code>0xf010fff8</code>。<br>然后开始执行第一句，将参数5<strong>movl</strong>到栈顶，注意这里不是压栈而是直接存进去的，所以栈指针<code>%esp</code>是不会移动的。但是这时候栈顶元素应该是0x5。我们可以通过<code>info registers</code>命令和<code>x/8x $esp</code>来分别验证，效果如下:</p></blockquote><p><img src="20.png" width="50%" height="50%" alt=""></p><blockquote><p>在看一下第二条指令<code>call f0100040</code>。<code>call</code>指令可以分成两条指令:<code>push %eip</code>和<code>jmp 0xf0100040</code>。所以看到<code>push</code>指令是正宗的压栈指令，这时候<code>%esp</code>寄存器的值是要-4的，而且递减满堆栈的顺序是<strong>先-4，再压栈</strong>。这次栈顶元素应该是下一条指令的地址<code>0xf01000ea</code>，而且<code>%esp</code>的值还得-4。我们可以通过<code>info registers</code>和<code>x/8x $esp</code>来分别验证:</p></blockquote><p><img src="21.png" width="50%" height="50%" alt=""></p><blockquote><p>第三条指令就正式进入到<code>test_backtrace</code>函数内部了。分析一下函数内部的<strong>调用代码</strong>: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">void test_backtrace(int x)</span><br><span class="line">&#123;</span><br><span class="line">f0100040:       55                      push   %ebp</span><br><span class="line">f0100041:       89 e5                   mov    %esp,%ebp</span><br><span class="line">f0100043:       53                      push   %ebx</span><br><span class="line">f0100044:       83 ec 14                sub    $0x14,%esp</span><br><span class="line">f0100047:       8b 5d 08                mov    0x8(%ebp),%ebx</span><br><span class="line">        cprintf(&quot;entering test_backtrace %d\n&quot;, x);</span><br><span class="line">f010004a:       89 5c 24 04             mov    %ebx,0x4(%esp)</span><br><span class="line">f010004e:       c7 04 24 20 1a 10 f0    movl   $0xf0101a20,(%esp)</span><br><span class="line">f0100055:       e8 c1 09 00 00          call   f0100a1b &lt;cprintf&gt;</span><br><span class="line">        if (x &gt; 0)</span><br><span class="line">f010005a:       85 db                   test   %ebx,%ebx    </span><br><span class="line">f010005c:       7e 0d                   jle    f010006b &lt;test_backtrace+0x2b&gt;</span><br><span class="line">                test_backtrace(x-1);</span><br><span class="line">f010005e:       8d 43 ff                lea    -0x1(%ebx),%eax</span><br><span class="line">f0100061:       89 04 24                mov    %eax,(%esp)</span><br><span class="line">f0100064:       e8 d7 ff ff ff          call   f0100040 &lt;test_backtrace&gt;</span><br><span class="line">f0100069:       eb 1c                   jmp    f0100087 &lt;test_backtrace+0x47&gt;</span><br><span class="line">        else</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>基本上所有被调用的函数开头都会有这两条指令:<code>push %ebp</code>;<code>mov %esp, %ebp</code>;<br>实验指导书上也解释了<code>%ebp</code>寄存器的作用:<strong>On entry to a C function, the function's prologue code normally saves the previous function's base pointer by pushing it onto the stack, and then copies the current esp value into ebp for the duration of the function.</strong>每一个函数都有一个<code>%ebp</code>值，作为函数的栈帧，需要在每次调用新函数的时候压栈以保存上一个函数的返回地址。<br>这个时候莫名其妙的压栈了一个寄存器<code>%ebx</code>。我们将<code>%ebx</code>全局高亮显示发现，后面紧接着会有<code>mov 0x8(%ebp), %ebx</code>。那<code>0x8(%ebp)</code>存储的到底是啥？不难想到就是参数5。然后<code>%esp</code>指针接着扩展当前函数的栈空间。关注一下第二次调用<code>test_backtrace()</code>，前一条指令<code>mov %eax, (%esp)</code>和第一次调用的时候完全一样，而<code>%eax</code>中存的也是参数5-1=4;当然调用指令<code>call</code>也需要一次压栈。总结一下，如果是从<code>push %ebp</code>开始算起是函数栈的开头的话，那我们每一个函数调用会花费<code>4（push %ebp）+4(push %ebx)+20(sub $0x14,%esp)+4(call)</code>一共是32Bytes字节的空间。所以整个栈的布局见下图:</p></blockquote><p><img src="23.png" width="50%" height="50%" alt=""></p><blockquote><p>当然在调用过程中还调用了<code>cprintf()</code>这个函数，不过20字节用来分配栈空间也够了。 ##### Exercise 11 经过上面的分析，栈的布局已经很清楚了。所以我们需要知道对于<code>read_ebp()</code>函数来说读出的是当前<code>%ebp</code>指针指向的地址，所以按照上面栈的布局:<code>(%ebp)--&gt;上一个%ebp</code>,<code>0x4（%ebp）--&gt;%eip</code>,<code>0x8(%ebp)--&gt;参数1</code>...当然这里的参数比5个少。<br>下面就是考验C语言的时刻，一定要注意格式: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">int</span><br><span class="line">mon_backtrace(int argc, char **argv, struct Trapframe *tf)</span><br><span class="line">&#123;</span><br><span class="line">        // Your code here.</span><br><span class="line">        /*</span><br><span class="line">         parameter &quot;argc&quot; indicates numbers of paras passed by the command-line;  parameter &quot;argv&quot; indicates specific paras accordingly.</span><br><span class="line">        */</span><br><span class="line">        struct Eipdebuginfo info;</span><br><span class="line">        cprintf(&quot;Stack backtrace:\n&quot;);</span><br><span class="line">        uint32_t *ebp = (uint32_t*)read_ebp();</span><br><span class="line">        while(ebp) &#123;</span><br><span class="line">                uint32_t eip = ebp[1];</span><br><span class="line">                cprintf(&quot;  ebp %x  eip %x  args &quot;, ebp, eip);</span><br><span class="line">                cprintf(&quot;%08.x &quot;, *(ebp+2));//这里提一下指针的用法，指导书中有讲解</span><br><span class="line">                cprintf(&quot;%08.x &quot;, *(ebp+3));</span><br><span class="line">                cprintf(&quot;%08.x &quot;, *(ebp+4));</span><br><span class="line">                cprintf(&quot;%08.x &quot;, *(ebp+5));</span><br><span class="line">                cprintf(&quot;%08.x\n&quot;, *(ebp+6));</span><br><span class="line"></span><br><span class="line">                ebp = (uint32_t*)*(ebp);</span><br><span class="line">        &#125;</span><br><span class="line">        return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></blockquote><h5 id="exercise-12">Exercise 12</h5><blockquote><p>对每一个<code>%eip</code>，尝试着给出其<strong>文件名称</strong>，<strong>函数名称</strong>和<strong>行号</strong>。这些信息都属于调试的信息。实验要做的是找到这些调试信息，并按照规定格式将它们输出。这里需要了解<a href="https://sourceware.org/gdb/onlinedocs/stabs.html#Stab-Sections" target="_blank" rel="noopener">STAB</a>符号表的概念。<br>首先查看下<code>kern/kernel.ld</code>链接脚本。发现了有关<code>_STAB_*</code>的两个信息————<code>.stab</code>和<code>.stabstr</code>。其中stab是今天的重点。<code>kern/kdebug.c</code>中的宏定义说明了stab和stabstr的关系。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">extern const struct Stab __STAB_BEGIN__[];      // Beginning of stabs table</span><br><span class="line">extern const struct Stab __STAB_END__[];        // End of stabs table</span><br><span class="line">extern const char __STABSTR_BEGIN__[];          // Beginning of string table</span><br><span class="line">extern const char __STABSTR_END__[];            // End of string table</span><br></pre></td></tr></table></figure><blockquote><p>再看一下<code>inc/stab.h</code>对stab定义的数据结构: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">// Entries in the STABS table are formatted as follows.</span><br><span class="line">struct Stab &#123;</span><br><span class="line">        uint32_t n_strx;        // index into string table of name    该项对应的在stabstr节内的索引偏移</span><br><span class="line">        uint8_t n_type;         // type of symbol   该项描述的符号类型   重点关注</span><br><span class="line">        uint8_t n_other;        // misc info (usually empty)   </span><br><span class="line">        uint16_t n_desc;        // description field  源文件的行号  重点关注</span><br><span class="line">        uintptr_t n_value;      // value of symbol   地址值   重点关注</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>通过<code>objdump -G obj/kern/kernel</code>来查看当前ELF文件中的符号表信息。见下图:</p></blockquote><p><img src="24.png" width="50%" height="50%" alt=""></p><blockquote><p>仔细观察上图，发现每一列的分类就是按照stab数据结构分类的。重点关注一下<code>n_type</code>这个关键字，它表示该描述项的符号类型，上图中出现有很多<code>SO</code>，<code>FUN</code>...等符号类型。将这些列单独列出来进行对比:</p></blockquote><p><img src="25.png" width="50%" height="50%" alt=""></p><blockquote><p>很明显<code>SO</code>指的是文件名，<code>FUN</code>指的是函数名。<strong>注意这是objdump对应的符号表，JOS对符号表有自己定义的数据结构见<code>inc/stab.h</code></strong>。输出的顺序都是按照地址来排序的，很整齐。所以<code>stab_binsearch()</code>查找函数原理也就猜的差不多了，<strong>这些调试信息和相应地址的关系是通过符号表来连接的。当然所有的这些知识都可以通过<code>kern/kdebug.c</code>这个文件中对<code>stab</code>的注释（见过最全的注释了）学习到。所以我们最后分析一下这个文件:</strong><br>整个文件一共有两个函数<code>debuginfo_eip</code>和<code>stab_binsearch</code>。JOS建立了一个数据结构<code>Eipdebuginfo</code>用来存放调试信息，<code>debuginfo_eip</code>调用<code>stab_binsearch</code>函数完成一个实例化的<code>Eipdebuginfo</code>，所有的输出信息其实最后都存储在这个实例中。 <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">struct Eipdebuginfo &#123;</span><br><span class="line">        const char *eip_file;           // Source code filename for EIP</span><br><span class="line">        int eip_line;                   // Source code linenumber for EIP</span><br><span class="line"></span><br><span class="line">        const char *eip_fn_name;        // Name of function containing EIP</span><br><span class="line">                                        </span><br><span class="line">        int eip_fn_namelen;             // Length of function name</span><br><span class="line">        uintptr_t eip_fn_addr;          // Address of start of function</span><br><span class="line">        int eip_fn_narg;                // Number of function arguments</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p><code>stab_binsearch(const struct Stab *stabs, int *region_left, int *region_right, int type, uintptr_t addr)</code>函数本质上就是一个二分查找。<strong><code>type</code>指的就是符号表项的符号类型，该函数每次查找的时候都需要确定查找的符号类型</strong>。我们需要完成的部分是行号，通过检查<code>inc/stab.h</code>文件，很容易知道行号的宏定义是<code>S_LINE</code>。二分查找失败的标志就是<code>LeftA &gt; rightA</code>。所以代码也很简单了:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">// Your code here.</span><br><span class="line">       stab_binsearch(stabs, &amp;lline, &amp;rline, N_SLINE, addr);   </span><br><span class="line">       if(lline &lt;= rline)&#123;</span><br><span class="line">               info-&gt;eip_line = stabs[lline].n_desc;  //确定行号</span><br><span class="line">       &#125;else &#123;</span><br><span class="line">               return -1;</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>最后将monitor的命令补充完整就更简单了: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">add :</span><br><span class="line">debuginfo_eip(eip, &amp;info);</span><br><span class="line">                cprintf(&quot;\t%s:%d: %.*s+%d\n&quot;, info.eip_file, info.eip_line, info.eip_fn_namelen, info.eip_fn_name, info.eip_fn_addr);</span><br><span class="line">to kern/monitor.c/mon_backtrace()</span><br><span class="line"></span><br><span class="line">add :</span><br><span class="line">        &#123;&quot;backtrace&quot;, &quot;Display each stack frame called&quot;, mon_backtrace &#125;,</span><br><span class="line">to kern/monitor/commands[]</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>运行结果见下图:</p></blockquote><p><img src="26.png" width="50%" height="50%" alt=""></p><blockquote><p>实验过程中有几处细节涉及到编译器的优化，因为还不是很了解编译链接的细节和原理，所以被我忽略掉了。<br>至此Lab1结束。</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;lab1&quot;&gt;Lab1&lt;/h2&gt;
&lt;h3 id=&quot;before-lab&quot;&gt;Before Lab&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;Lab1一共有三部分:&lt;code&gt;Bootstrap&lt;/code&gt;,&lt;code&gt;BootLoader&lt;/code&gt;,&lt;code&gt;Kernel&lt;/code&gt;;前两部分都是GDB调试为主，熟悉GDB的调试技巧和操作系统的启动流程。最后实现一小部分&lt;code&gt;monitor&lt;/code&gt;中&lt;strong&gt;test_backtrace&lt;/strong&gt;的功能。&lt;br&gt;
如果&lt;code&gt;GDB&lt;/code&gt;调试出错，可能是因为调试器没有链接到操作系统上：&lt;br&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;add &lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;add-auto-load-safe-path /Your Working Dir/.gdbinit&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;line to your configuration file &amp;quot;/root/.gdbinit&amp;quot;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="C" scheme="http://haotianmcihael.github.io/categories/C/"/>
    
    
      <category term="MIT 6.828" scheme="http://haotianmcihael.github.io/tags/MIT-6-828/"/>
    
      <category term="OS" scheme="http://haotianmcihael.github.io/tags/OS/"/>
    
      <category term="Linux" scheme="http://haotianmcihael.github.io/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>Mit6.828(Fall 2018) Lab0</title>
    <link href="http://haotianmcihael.github.io/2018/11/05/Mit6-828-Fall-2018-Lab0/"/>
    <id>http://haotianmcihael.github.io/2018/11/05/Mit6-828-Fall-2018-Lab0/</id>
    <published>2018-11-04T16:35:45.000Z</published>
    <updated>2020-06-26T02:31:46.798Z</updated>
    
    <content type="html"><![CDATA[<h2 id="lab0">Lab0</h2><h3 id="abstract">Abstract</h3><blockquote><p>从<code>8086汇编</code>实验之后，前后两周的时间勉强通过Lab1。<br>最大的感触是<strong>基础太差</strong>:对C语言的理解，对程序编译链接本质的理解，对<code>GDB</code>等工具的使用……当然也包括弱弱的英语理解能力。导致完成Lab1用了这么久的时间。 但是这次触动中却又多了一丝<code>见山不是山，见山又是山</code>的感觉。主要体现在我对操作系统的理解最终都回归到代码上来。比如<code>printf</code>函数的实现就涉及C语言中变参的特性。有理由相信这种<strong>血浓于水</strong>的关系在后续的Lab中还会进一步加强，所以我也希望这会是我学习OS最完美的方法论——————<strong>Talk is cheap, show me the code.</strong>。 截止到Lab1，整个学习的过程显得很清晰————就是<strong>通过调试<code>kernel</code>的反汇编代码来理解运行原理并加以修改</strong>。Lab0是我在实验过程中掉进的坑，查阅的资料和逐渐点亮的技能树，缩短实验前的准备时间对理解OS本身还是很有帮助的。 <a id="more"></a></p></blockquote><h3 id="实验环境的搭建">6.828实验环境的搭建</h3><ul><li>虚拟机环境: Ubuntu14.4 (<strong>32位</strong>)</li><li>仿真器: Qemu <code>git clone https://github.com/mit-pdos/6.828-qemu.git qemu</code></li><li>实验代码: Lab <code>git clone https://pdos.csail.mit.edu/6.828/2018/jos.git lab</code> &gt; 虚拟机环境<code>32位</code>，因为<code>JOS</code>就是32位的操作系统。<br>&gt; 仿真器使用MIT进行patched过的(见上链接)。原因是实验中分页机制是有意修改过的，使用<code>patched version</code>的话在后面<code>Exercise</code>中不需要手动转换地址。<br>&gt; 关于实验代码，默认熟悉<code>Git</code>和<code>MakeFile</code>。每做完一个<code>Exercise</code>可以使用<code>make grade</code>进行测试。<br>&gt; 在整个实验环境搭建的过程中，可能会因为虚拟机发型版本的不同而出现不同的链接库丢失，Google一下。详细的搭建过程见<a href="https://pdos.csail.mit.edu/6.828/2018/tools.html#" target="_blank" rel="noopener">Tools Guide</a></li></ul><h3 id="关于6.828">关于6.828</h3><blockquote><p>MIT的OS课程。学习共有三条主线:<code>Lectures</code>,<code>Readings</code>,<code>Labs</code>。<br>当然最重要的就是<code>Labs</code>。关于剩余的两部分，主要围绕一个叫做<code>xv6</code>的小型OS展开的。简单来讲，这是一个<strong>麻雀虽小五脏俱全</strong>的OS。有<code>Documents</code>和<code>Source Code</code>两份资料。知乎上评价很高，有人将其翻译成为中文版本上传至Github.<a href="https://github.com/ranxian/xv6-chinese" target="_blank" rel="noopener">XV6_Ch</a>，传说看懂源码之后可以超神……<br>做<code>Lab</code>的过程中，课程会提供大量的Readings来阅读，当然都是英文的。详细见<a href="https://pdos.lcs.mit.edu/6.828/2018/reference.html" target="_blank" rel="noopener">Reference</a>。</p></blockquote><h3 id="汇编和内联汇编">80386汇编和内联汇编</h3><h4 id="att-80386">AT&amp;T-80386</h4><blockquote><p>在之前的汇编实验中，我们学习了16位的<code>Intel-8086</code>汇编。而在32位机器上需要使用32位的<code>80386</code>汇编语言。虽然对于<code>Linux</code>来说，<code>8086</code>和<code>80386</code>的机器是有很大的差别的，主要体现在实模式和保护模式上。但是就语言本身而言，更多的是递进的关系而语法差别并不大。目前还没有遇到保护模式相关的障碍。<br><code>x86</code>的汇编主要有两种格式:<code>Intel</code>和<code>AT&amp;T</code>。<code>6.828</code>使用的是<code>AT&amp;T</code>,而实际上Linux中的汇编也是后者居多。<strong>汇编语言本身主要有指令集<code>Instruction Set</code>和伪指令<code>Directives</code>两大部分内容</strong>。当然最全的文献是Intel自家的Documents，不过我觉得有些太杂和多了。下面的的资料在实验过程中帮了我很多。值得细看。</p></blockquote><ul><li>MIT----<a href="https://pdos.lcs.mit.edu/6.828/2018/readings/pcasm-book.pdf" target="_blank" rel="noopener">pcasm-book</a>。</li><li>Wikipedia----<a href="https://en.wikipedia.org/wiki/X86_instruction_listings#Added_with_80386" target="_blank" rel="noopener">x86 Instruction Set</a></li><li>Oracle----<a href="https://docs.oracle.com/cd/E26502_01/html/E28388/eoiyg.html" target="_blank" rel="noopener">x86 Assembler Directives</a></li></ul><h4 id="内联汇编">内联汇编</h4><blockquote><p>因为操作系统本身就是和硬件打交道最频繁的系统软件，有些地方需要使用到汇编来简单粗暴的完成工作。<strong>内联汇编</strong>就是嵌入在C语言中的汇编语言，格式和原来的有很大的不同。不过IBM的那份资料可以解决一大半问题，也是难得的中文文档……</p></blockquote><ul><li>MIT----<a href="http://www.delorie.com/djgpp/doc/brennan/brennan_att_inline_djgpp.html" target="_blank" rel="noopener">Brennan's Guide to Inline Assembly</a></li><li>IBM----<a href="https://www.ibm.com/developerworks/cn/linux/l-assembly/index.html" target="_blank" rel="noopener">汇编语言开发指南</a></li></ul><h3 id="elf文件">ELF文件</h3><blockquote><p>实名Diss网上各种对ELF的讲解，故意弄混<code>section</code>和<code>segment</code>的概念。加上对二进制工具使用的不熟练，让我一度以为链接的难度系数是整个实验过程中最高的……直到我看到<code>CSAPP</code>中关于链接部分的讲解。</p></blockquote><h4 id="编译驱动程序">编译驱动程序</h4><blockquote><p><code>Compiler driver</code>指的是从C文件变成二进制文件的机器操作过程。我们从两个角度理解这个过程:<strong>机器运行的角度和C文件类型变化的角度</strong>。编译器和汇编器将多个<code>C</code>文件编译成相应的多个<strong>可重定位目标文件</strong>，也就是平时的<code>.o</code>文件，这种文件由不同的代码节和数据节构成，<strong>节</strong>在原文中是<code>section</code>。而链接器将多个<code>.o</code>文件链接成为一个<strong>可执行目标文件</strong>也就是平时的二进制文件。见下图:</p></blockquote><p><img src="3.png" width="50%" height="50%" alt=""></p><blockquote><p>熟悉一个程序的第一步是关注其输入输出。一句话总结链接器的功能:<strong>以一组可重定位目标文件和命令行参数作为输入，生成一个完全链接的可以加载和运行的可执行目标文件作为输出。</strong> 站在机器的角度，编译器和汇编器将ASCII格式的C文件编译成为字节块<strong>section</strong>的集合，而链接器做的就是将<strong>不同</strong>可重定位目标文件中的各种<code>sections</code>做归类，<strong>确定被链接块的运行时的内存位置</strong>。这是重点，目标文件纯粹是字节块的集合，而链接器本身对程序也所知甚少，她做的仅仅是对<strong>字节文件</strong>做处理，将多个文件的字节块放到了一个文件中。</p></blockquote><h4 id="链接过程">链接过程</h4><blockquote><p>链接器做两件事: * 符号解析: 输入文件的符号引用都对应到其定义上。 * 重定位: 编译器和汇编器生成的文件地址都是从0开始的，并没有和内存挂上关系。而链接器将这些不同的<code>section</code>定位到内存中的一个地址。也就是其加载地址。 重点关注第二步重定位，本质上就是将多个可重定位文件中同类型的<code>.section</code>合并为一个大的聚合节。但是注意这时候还没有<code>segment</code>的概念,聚合节还是叫做<code>.section</code>。我查阅了<code>CSAPP</code>的英文版，全书第一次提到<code>segment</code> 是在链接器部分的最末尾，也就是讲重定位的时候。下面是原文:</p></blockquote><p><img src="4.png" width="50%" height="50%" alt=""></p><blockquote><p>明显看到<code>segment</code>还是内存的概念而非可执行文件中的部分。所以从头到尾链接过程都没有生成过<code>segment</code>这样一种东西，其只是多个<code>sections</code>对应于内存中的<strong>映射</strong>。全书对链接部分的讲解也很少提及<code>segment</code>，<strong>所以我们对链接的理解最小单位应该是<code>section</code>，链接过程就是对<code>section</code>进行操作;而最后重定位的时候将多个大的<code>section</code>映射到一段内存中，映射的单位才是<code>segment</code></strong>。现在看下图就很清楚了:</p></blockquote><p><img src="5.png" width="50%" height="50%" alt=""></p><blockquote><p>从文件类型的角度来看: 目前我们谈到的ELF文件有可重定位目标文件和可执行目标文件。可以使用二进制工具ELF文件进行分析。关于链接更本质的过程还是很复杂的，Lab1后面会有一部分和<strong>符号表</strong>相关的实验，建议先详细看看<code>CSAPP</code>和下面提供的资料。 * MIT----<a href="https://pdos.csail.mit.edu/6.828/2018/readings/elf.pdf" target="_blank" rel="noopener">The definitive standard for the ELF format</a> * Wikipedia----<a href="https://en.wikipedia.org/wiki/Executable_and_Linkable_Format" target="_blank" rel="noopener">Executable and Linkable Format</a> * ELF中文文档----<a href="https://paper.seebug.org/papers/Archive/refs/elf/Understanding_ELF.pdf" target="_blank" rel="noopener">ELF.Ch</a> ### 二进制分析工具 6.828中主要使用<code>objdump</code>，基本工具可以通过<code>man</code>来查看用法，这里列出几个最常见的参数。</p></blockquote><h4 id="objdump">Objdump</h4><blockquote><p>对目标文件进行分析，在Linux平台上有三种<code>object file</code>:<code>Relocatable object File</code>,<code>Executable object File</code>,<code>Shared object File</code>，一直没搞懂为啥都叫<code>object File</code>。不过因为这三种目标文件对应于链接的不同阶段，所以对于链接过程本质的理解直接影响这里使用这个二进制分析工具。</p></blockquote><ul><li><code>-f</code> Display file headers</li><li><code>-d</code> Disassembly</li><li><code>-h</code> Section headers</li><li><code>-x</code> All headers</li><li><code>-S</code> Display source code</li></ul><h4 id="nm">Nm</h4><blockquote><p>列出目标文件中的符号表内容，应该和重定位关系密切。</p></blockquote><h3 id="阅读源码cli工具">阅读源码CLI工具</h3><blockquote><p>虽然我是<a href="https://www.gnu.org/software/emacs/" target="_blank" rel="noopener">Emacs</a>党，但是不得不承认VIM在进行文本操作的时候还是更<strong>方便:)</strong>一些。</p></blockquote><h4 id="vim-ctags">VIM-Ctags</h4><blockquote><p>这个就很强大了，用于在<code>tags</code>文件所在的根目录下寻找函数声明和宏定义，在阅读源码的时候基本上不知道的定义和关键字都可以使用<code>Ctags</code>来进行定位，非常实用。<br>安装成功之后，在工程目录下面执行命令<code>ctags -R *</code>对当前目录递归建立索引，会生成<code>tags</code>文件。在源码中遇到未见过的函数便将光标放在其上<code>Ctrl - }</code>即可跳转到其声明的位置。<code>Ctrl - o</code>自动返回。</p></blockquote><h4 id="vim_taglist">VIM_TagList</h4><blockquote><p>这个实际上没怎么用，功能是在VIM中打开目录树，便于程序员操作。可以先留下来，进一步观察。</p></blockquote><h4 id="tmux">Tmux</h4><blockquote><p>这又是一个神器，终端分屏功能强大，有<code>window</code>,<code>pannel</code>等概念。和<code>on-my-zsh</code>配合使用简直了。不愿意折腾的话也可以在<code>bash</code>下直接使用。我们在<code>GDB</code>调试OS的时候使用她作为分屏工具，需要在家目录下加入配置文件<code>~/.tmux.conf</code>。<br><img src="1.png" width="50%" height="50%" alt=""></p></blockquote><h4 id="vimgrep">VimGrep</h4><blockquote><p>用于在指定目录下进行全局搜索并在另一个窗口打开;关键字<code>sample</code>;在vim命令模式下执行命令； * :<code>vim/sample/% | copen</code> 当前文件搜索 * :<code>vim/sample/* | copen</code> 当前目录搜索 * :<code>vim/sample/ ../** | copen</code> 从上一级目录开始递归搜索 * :<code>vim/sample path1/**  path2/** | copen</code> 多路径搜索 * : <code>cclose</code> 关闭当前搜索<br><img src="2.png" width="50%" height="50%" alt=""></p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;lab0&quot;&gt;Lab0&lt;/h2&gt;
&lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;从&lt;code&gt;8086汇编&lt;/code&gt;实验之后，前后两周的时间勉强通过Lab1。&lt;br&gt;
最大的感触是&lt;strong&gt;基础太差&lt;/strong&gt;:对C语言的理解，对程序编译链接本质的理解，对&lt;code&gt;GDB&lt;/code&gt;等工具的使用……当然也包括弱弱的英语理解能力。导致完成Lab1用了这么久的时间。 但是这次触动中却又多了一丝&lt;code&gt;见山不是山，见山又是山&lt;/code&gt;的感觉。主要体现在我对操作系统的理解最终都回归到代码上来。比如&lt;code&gt;printf&lt;/code&gt;函数的实现就涉及C语言中变参的特性。有理由相信这种&lt;strong&gt;血浓于水&lt;/strong&gt;的关系在后续的Lab中还会进一步加强，所以我也希望这会是我学习OS最完美的方法论——————&lt;strong&gt;Talk is cheap, show me the code.&lt;/strong&gt;。 截止到Lab1，整个学习的过程显得很清晰————就是&lt;strong&gt;通过调试&lt;code&gt;kernel&lt;/code&gt;的反汇编代码来理解运行原理并加以修改&lt;/strong&gt;。Lab0是我在实验过程中掉进的坑，查阅的资料和逐渐点亮的技能树，缩短实验前的准备时间对理解OS本身还是很有帮助的。
    
    </summary>
    
      <category term="C" scheme="http://haotianmcihael.github.io/categories/C/"/>
    
    
      <category term="MIT 6.828" scheme="http://haotianmcihael.github.io/tags/MIT-6-828/"/>
    
      <category term="OS" scheme="http://haotianmcihael.github.io/tags/OS/"/>
    
      <category term="Linux" scheme="http://haotianmcihael.github.io/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>汇编语言（王爽）ch5</title>
    <link href="http://haotianmcihael.github.io/2018/10/16/%E6%B1%87%E7%BC%96%E8%AF%AD%E8%A8%80%EF%BC%88%E7%8E%8B%E7%88%BD%EF%BC%89ch5/"/>
    <id>http://haotianmcihael.github.io/2018/10/16/汇编语言（王爽）ch5/</id>
    <published>2018-10-15T16:32:55.000Z</published>
    <updated>2020-06-26T02:29:26.340Z</updated>
    
    <content type="html"><![CDATA[<h2 id="第五部分中断ii">第五部分:中断II</h2><h3 id="abstract">Abstract</h3><blockquote><p>CPU实现I/O功能的两个问题:从何处获得外设的输入？如何解决外设输入随时可能发生的问题？首先外设芯片内部有若干寄存器，CPU将这些寄存器当做端口来访问。外设的输入输出不直接送入CPU和内存而是通过这些寄存器。第二，CPU通过外中断控制I/O的随时性。 <a id="more"></a></p></blockquote><h3 id="端口">端口</h3><blockquote><p>CPU可以直接读写三个地方的数据: * 内部寄存器 * 内存单元 * 端口 CPU通过端口地址来定位不同的端口，最多可以定位64KB个端口，则端口地址范围为<code>0~65535</code>。端口的读写指令<code>in</code>和<code>out</code>。只能使用<code>al</code>和<code>ax</code>来存放对端口进行读写的数据。</p></blockquote><h3 id="外中断">外中断</h3><blockquote><p>外中断是由相关芯片发送给CPU中的。分为<strong>可屏蔽中断</strong>和<strong>不可屏蔽中断</strong>。不可屏蔽中断指那些CPU一定需要响应的中断，这种中断很少，中断类型码固定为2。大多数外中断指的是可屏蔽中断。<br>CPU要不要响应可屏蔽中断完全取决于状态寄存器的<code>IF</code>位。为1则响应，否则不响应。8086中手动设置<code>IF</code>的指令: * <code>sti</code>:设置为1 * <code>cli</code>:设置为0</p></blockquote><h3 id="实验-编写9号中断例程">实验 编写9号中断例程</h3><h4 id="pc机键盘的处理过程">PC机键盘的处理过程</h4><blockquote><p>键盘中有一个芯片扫描每一个键的状态——按下还是松开。按下产生一个扫描码称为<strong>通码</strong>，松开也产生一个扫描码称为<strong>断码</strong>。<code>断码=通码+80h</code>。扫描码被送到<code>60h</code>端口中。<br><code>int 9h</code>是BIOS提供的不可屏蔽中断。一旦CPU收到该信号: * 读出<code>60h</code>端口的扫描码 * 如果是字符键的扫描码，将该扫描码和所对应的字符码送入内存中的BIOS键盘缓存区(16个字单元)。如果是控制键(Ctrl)和切换键(CapsLock)的扫描码，则将其转变为状态字节写进内存中存储状态字节的单元。 * 对键盘系统进行相关的控制。</p></blockquote><h4 id="实验">实验</h4><blockquote><p>在屏幕中间依次显示&quot;a&quot;~&quot;z&quot;,在显示的过程中按下Esc键后改变颜色。<br>实验有意思的地方在于:因为键盘上所有的键都会触发<code>int 9h</code>中断，<strong>需要在保证其他键无效的情况下，Esc键触发中断</strong>————就相当于出现了两个中断例程，两个中断向量。更有趣的是需要这两个中断同时有效，在一个<code>int 9h</code>中断下！<br>方法也很简单，就是使用<code>if_else</code>判断扫描码。当然汇编中没有<code>if_else</code>。</p></blockquote><h4 id="中断向量和中断例程">中断向量和中断例程</h4><blockquote><p>键盘无论是哪一个键当然只会触发<code>int 9h</code>中断这个不会变。不过得分别写例程:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">assume cs:code</span><br><span class="line">stack segment</span><br><span class="line">db 128 dup (0)</span><br><span class="line">stack ends</span><br><span class="line"></span><br><span class="line">data segment</span><br><span class="line">dw 0,0</span><br><span class="line">data ends</span><br><span class="line"></span><br><span class="line">code segment</span><br><span class="line">start:</span><br><span class="line">mov ax,stack</span><br><span class="line">mov ss,ax</span><br><span class="line">mov sp,128</span><br><span class="line"></span><br><span class="line">mov ax,data</span><br><span class="line">mov ds,ax</span><br><span class="line"></span><br><span class="line">mov ax,0</span><br><span class="line">mov es,ax</span><br><span class="line"></span><br><span class="line">push es:[9*4]</span><br><span class="line">pop ds:[0]</span><br><span class="line">push es:[9*4+2]</span><br><span class="line">pop ds:[2]</span><br><span class="line"></span><br><span class="line">mov word ptr es:[9*4],offset int 9</span><br><span class="line">mov es:[9*4+2],cs</span><br></pre></td></tr></table></figure><blockquote><p>上面的代码主要就是设置了中断向量表。可以看到讲原来的<code>int 9h</code>中断例程地址保存在ds:0,ds:2地址处，换上新的例程地址<code>cs:(offset int 9)</code>。新的例程代码: <figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">int9:push ax</span><br><span class="line">push bx</span><br><span class="line">push es</span><br><span class="line"></span><br><span class="line">in al,60h</span><br><span class="line"></span><br><span class="line">pushf</span><br><span class="line">call dword ptr ds:[0]   ;这里的实际上模拟了int 9中断例程的功能，实现子程序中的调用</span><br><span class="line"></span><br><span class="line">cmp al,1</span><br><span class="line">jne int9ret</span><br><span class="line"></span><br><span class="line">;下面是Esc例程代码</span><br><span class="line"></span><br><span class="line">mov ax,0b800h</span><br><span class="line">mov es,ax</span><br><span class="line">inc byte ptr es:[160*12+40*2+1]  ;改变属性值，改变颜色</span><br><span class="line"></span><br><span class="line">int9ret:pop es</span><br><span class="line">pop bx</span><br><span class="line">pop ax</span><br><span class="line">iret</span><br></pre></td></tr></table></figure></p></blockquote><blockquote><p>其中<code>cmp al,1</code>和<code>jne int9ret</code>就相当于<code>if_else</code>的功能。在主程序中有了栈，栈在中断例程中的主要作用就是保护现场，分析一下:<br>* 需要保护最开始的<code>int 9h</code>的中断向量号 * 因为在显示字符的主程序中，<code>ax</code>,<code>es</code>,<code>bx</code>都保存了重要的中间参数，所以需要保护 除此之外,栈并没有起到什么作用。这样看下来其实源程序也简单了不少。 <img src="25.png" width="50%" height="50%" alt=""></p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;第五部分中断ii&quot;&gt;第五部分:中断II&lt;/h2&gt;
&lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;CPU实现I/O功能的两个问题:从何处获得外设的输入？如何解决外设输入随时可能发生的问题？首先外设芯片内部有若干寄存器，CPU将这些寄存器当做端口来访问。外设的输入输出不直接送入CPU和内存而是通过这些寄存器。第二，CPU通过外中断控制I/O的随时性。
    
    </summary>
    
      <category term="Assembly" scheme="http://haotianmcihael.github.io/categories/Assembly/"/>
    
    
      <category term="i386" scheme="http://haotianmcihael.github.io/tags/i386/"/>
    
  </entry>
  
  <entry>
    <title>汇编语言（王爽）ch4</title>
    <link href="http://haotianmcihael.github.io/2018/10/12/%E6%B1%87%E7%BC%96%E8%AF%AD%E8%A8%80%EF%BC%88%E7%8E%8B%E7%88%BD%EF%BC%89ch4/"/>
    <id>http://haotianmcihael.github.io/2018/10/12/汇编语言（王爽）ch4/</id>
    <published>2018-10-11T21:11:38.000Z</published>
    <updated>2020-06-26T02:29:41.718Z</updated>
    
    <content type="html"><![CDATA[<h2 id="第四部分中断i">第四部分:中断I</h2><h3 id="abstract">Abstract</h3><blockquote><p>在之前的实验中，我们所编写的汇编程序从编译到运行出结果都很顺利。这个过程映射到CPU层面就是<code>CS:IP</code>的跳转<strong>唯一取决于</strong>汇编程序员的代码。<code>CSAPP</code>将这个过程称为<strong>程序控制流</strong>。而<strong>中断</strong>则是将这种流程打断，建立更复杂的<strong>异常控制流</strong>。中断分为<code>内中断</code>和<code>外中断</code>两种。<br><a id="more"></a></p></blockquote><h3 id="内中断">内中断</h3><h4 id="中断产生">中断产生</h4><blockquote><p>8086CPU使用<strong>中断类型码</strong>来标识中断信息的来源。中断类型码为一个字节型数据，可以表示256种<strong>中断信息的来源</strong>————具体指的是产生中断信息的事件，简称<strong>中断源</strong>。下面是8086中4种常见的中断源及其中断类型码:<br>* 除法错误: 0 * 单步执行: 1 * 执行<code>into</code>指令: 4 * 执行<code>int n</code>指令: n</p></blockquote><h4 id="中断过程">中断过程</h4><blockquote><p>CPU获得<strong>中断类型码</strong>之后，需要跳转到该<strong>中断类型码</strong>对应的<strong>中断处理程序</strong>中进行处理。如何跳转呢？使用<strong>中断向量表</strong>。而跳转通常意味着改变CPU的状态，所以需要使用栈来保存现场。所以<strong>识别</strong>，<strong>保存</strong>和<strong>跳转</strong>就是中断过程主要干的事儿————其中识别由硬件完成！<br><strong>中断向量表</strong>保存在内存<code>0000:0000</code>到<code>0000:03FF</code>的这1KB单元里。一个表项存放一个<strong>中断向量</strong>————也就是中断处理程序的地址入口。占4个字节，高地址放段地址，低地址放偏移地址。 ##### 识别 下图是一张8086的内存分布图:<br><img src="14.png" width="50%" height="50%" alt=""></p></blockquote><blockquote><p>从内存0开始，所以存储<code>N号中断源对应的中断向量的偏移地址</code>的内存地址为:<code>0000:4N</code>，存储<code>N号中断源对应的中断向量的段地址</code>的内存地址为:<code>0000：4N+2</code>。看下图: <img src="15.png" width="50%" height="50%" alt=""></p></blockquote><h5 id="跳转和保存">跳转和保存</h5><ul><li>获得中断类型码</li><li>标志寄存器的值入栈: <code>pushf</code></li><li>设置标志寄存器第8位TF和第9位IF的值位0: TF=0,IF=0</li><li>CS的内容入栈: <code>push CS</code></li><li>IP的内容入栈: <code>push IP</code></li><li>通过中断类型码获取入口地址: <code>(IP)=(N*4),(CS)=(N*4+2)</code></li><li>最后一步执行结束后，CPU开始执行由程序员编写的中断处理程序</li></ul><h4 id="中断处理">中断处理</h4><blockquote><p>所谓中断处理程序就是操作系统对各种中断的反应。 由于CPU随时可能检测到中断，所以中断处理程序必须一直存储在内存某段空间中。下面的实验就是通过来实现内中断处理程序。 ##### 0号中断 即除法溢出错误。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mov ax, 1000h</span><br><span class="line">mov bh,1</span><br><span class="line">div bh</span><br></pre></td></tr></table></figure><blockquote><p>该除法指令在执行的过程中会发生溢出错误，导致产生0号中断从而引发中断过程。</p></blockquote><h4 id="实验-编写0号中断的处理程序">实验 编写0号中断的处理程序</h4><ul><li>中断类型码:0</li><li>中断处理程序: 选择<code>0000：0200</code>到<code>0000：02ff</code>这256个Bytes内存的区域来存放</li><li>中断向量表: 0号中断的段地址<code>0000:0002</code>偏移地址<code>0000:0000</code> ##### 安装处理程序<code>do0</code> &gt; 得确保中断处理程序不会变动，所以选择一块合适的内存区域将其安装进去。</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line">assume cs:code</span><br><span class="line">code segment</span><br><span class="line">start: mov ax,cs</span><br><span class="line">   mov ds,ax</span><br><span class="line">   mov si,offset do0  ;设置ds:si指向被拷贝的程序地址</span><br><span class="line">   mov ax,0</span><br><span class="line">   mov es,ax  ;设置es:di指向拷贝的目的地址</span><br><span class="line">   mov di,200h</span><br><span class="line">   </span><br><span class="line">   mov cx,offset do0end-offset do0   ;设置需要拷贝的代码长度度</span><br><span class="line">   </span><br><span class="line">   cld  ;设置传输方向为正</span><br><span class="line">   rep movsb</span><br><span class="line">   </span><br><span class="line">   /*</span><br><span class="line">   设置中断向量表</span><br><span class="line">   */</span><br><span class="line">   </span><br><span class="line">   mov ax, 4c00h</span><br><span class="line">   int 21h</span><br><span class="line">   </span><br><span class="line">do0:   jmp short do0start</span><br><span class="line">   db &quot;overflow!&quot;</span><br><span class="line">   </span><br><span class="line">do0start:mov ax,cs</span><br><span class="line">        mov ds,ax</span><br><span class="line">        mov si,200h   ;设置ds:si指向字符串</span><br><span class="line"> </span><br><span class="line">mov ax,0b800h</span><br><span class="line">mov es,ax</span><br><span class="line">mov di,12*160+36*2 ;设置es:di指向显存空间的中间位置</span><br><span class="line"></span><br><span class="line">mov cx,9 ;字符串的长度</span><br><span class="line">s:  mov al,[si]</span><br><span class="line">mov es:[di],al</span><br><span class="line">inc si</span><br><span class="line">add di,2  </span><br><span class="line">loop s</span><br><span class="line"></span><br><span class="line">    mov ax,4c00h</span><br><span class="line">    int 21h</span><br><span class="line">   </span><br><span class="line">   do0end: nop</span><br><span class="line">   </span><br><span class="line">   code ends</span><br><span class="line">   end start</span><br></pre></td></tr></table></figure><h5 id="设置中断向量号">设置中断向量号</h5><blockquote><p>当中断发生的时候，确保0号中断的处理程序跳转地址是上一个设置好的地址。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">mov ax,0</span><br><span class="line">mov es,ax</span><br><span class="line">mov word ptr es:[0*4],200h</span><br><span class="line">mov word ptr es:[0*4+2], 0</span><br></pre></td></tr></table></figure><h5 id="验证">验证</h5><blockquote><p>注意，在跳转到中断处理程序之前，所有的CPU现场已经入栈保存好了，而跳转过程本身也是硬件过程，所以我们所做的仅仅只是设置好<strong>跳转的地址</strong>和<strong>该地址相应的处理代码</strong>。<br><strong>经过实验发现DOSBox的和传统的DOS还是有一些区别的，这里用一个小实验验证中断:</strong>首先我们直接在debug下面将<code>0号中断向量号改写成为0000:0200处</code>，然后在另一块内存<code>0200:0000</code>中写进了会引发0号中断的代码:<br><img src="16.png" width="50%" height="50%" alt=""> 然后修改好了<code>CS:IP</code>地址并开始执行:<br><img src="17.png" width="50%" height="50%" alt=""> ###### 现象 <strong>单步执行,可以看到<code>CS:IP</code>的值在执行到<code>div bh</code>的时候，立马从<code>0200:0005</code>变成了<code>0000:0002</code>!,我写的中断处理<code>mov cx,ffff</code>也被执行！然后一直执行到结束退出。</strong> 实验的整个过程实际上就是做了一个跳转，然后我们仔细对比一下跳转前后寄存器值的差距:<strong>会发现除了<code>CS:IP</code>有变化之外，<code>SS:SP</code>大小也有变化，这是<code>CPU</code>保存现场的缘故————从<code>00F7</code>到<code>00FD</code>共3个字对应<code>CS</code>，<code>IP</code>和<code>TF</code>；而且<code>状态寄存器EI也变为DI</code>,估计也和<code>TF</code>和<code>IF</code>相关;因为我们的中断处理直接退出程序所以栈中的数据没有派上用场，不过如果需要返回源程序做一些操作的时候栈就会起到恢复的作用。</strong>我还尝试过在中断产生的时候查看栈中的内容，但是每次指令刚执行就被强制退出……不知道为什么。<strong>不过这是下一个实验的内容了。</strong>另外，这里可以回过头看一下<strong>ch2</strong>中关于栈的那个猜测，基本上没有毛病。 <img src="18.png" width="50%" height="50%" alt=""></p></blockquote><h5 id="号中断">1号中断</h5><blockquote><p>在CPU保存现场的时候，对状态寄存器压栈，并且将其中两个状态位设置为零:TF=0;IF=0;主要是因为当<strong>CPU</strong>每执行完一条指令<strong>的时候，就会去查看状态寄存器的TF位是否为1，如果是则引发1号中断————单步中断</strong>。整个中断的过程和其他的没有什么区别，作者提到<code>debug -t</code>的单步执行功能，实际上就是通过debug调用1号中断对CPU进行控制。这里的控制主要体现在中断处理程序上————比如<code>debug -t</code>就是设置寄存器的值显示在屏幕上。那为什么要在处理之前又将<code>TF</code>设置为零呢？注意前面的<code>每执行完一条指令</code>的意思，CPU在执行中断处理程序的时候是不能响应其他中断的，<code>TF=0</code>是防止在过程中递归的陷入。</p></blockquote><blockquote><p>但是在有些特殊情况下中断的产生，CPU不见得马上处理。比如<code>mov ss, ax</code>和<code>MOV sp, 10</code>这两条设置堆栈的指令，在前面的实验中出现过。</p></blockquote><h3 id="int-指令">int 指令</h3><h4 id="中断例程">中断例程</h4><blockquote><p><code>int n</code>是一种很重要的内中断。<code>n</code>是中断类型码，该条指令功能就是可以引发中断过程。执行如下: * 取终端类型码n * 标志寄存器入栈，IF=0,TF=0 * <code>push cs</code>,<code>push ip</code> * (IP)=(n<em>4),(CS)=(n</em>4+2) <strong>注意一下这里的压栈顺序，后面的实验中会有用到。</strong><br>一般情况下，系统会将一些具有一定功能的子程序，以中断处理程序的方式提供给应用程序调用，通常就是使用<code>int</code>指令。将这些自己编写的中断处理程序叫做中断例程。</p></blockquote><h4 id="编写loop中断例程">编写<code>loop</code>中断例程</h4><blockquote><p>在屏幕中间显示80个“！”。 这个程序一看就知道需要使用到<code>loop</code>指令，但是该指令到底是如何实现的呢？我们知道跳转指令实际上就是改变<code>CS:IP</code>的值，而<strong>给定次数的跳转</strong>和<strong>给定目标</strong>的跳转合在一起就会比较麻烦。<br><strong>跳转指令是靠两个标号之间的相对位移而不是靠标号的地址</strong>这一点很重要。我们用<code>cx</code>存放循环次数，用<code>bx</code>存放相对位移。先看一下例程代码:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">assume cs:code</span><br><span class="line"></span><br><span class="line">code segment</span><br><span class="line">start:mov ax,0b800h   </span><br><span class="line">mov es,ax</span><br><span class="line">mov di,160*12</span><br><span class="line"></span><br><span class="line">mov bx,offset s-offset se</span><br><span class="line">mov cx,80</span><br><span class="line">s:  mov byte ptr es:[di],&apos;!&apos;</span><br><span class="line">add di,2</span><br><span class="line">/*</span><br><span class="line">int 7ch例程代码:如果（cx）!=0,跳转到标号s处</span><br><span class="line">*/</span><br><span class="line">se: nop</span><br><span class="line"></span><br><span class="line">mov ax,4c00</span><br><span class="line">int 21h</span><br><span class="line"></span><br><span class="line">code ends</span><br><span class="line">end start</span><br></pre></td></tr></table></figure><blockquote><p>至此，应该已经完全可以看懂这些汇编程序。可是核心的功能呢？如下:<br>跳转到标号s处需要知道s的段地址和偏移地址。先来分析一下在执行<code>int 7ch</code>之后发生了什么？画得不好凑活着看吧:)<br><img src="19.png" width="50%" height="50%" alt=""> x86中的栈是一个<strong>满堆栈</strong>。首先标志寄存器入栈，然后对se标号的CS:IP压栈。因为代码只有一个段，所以se的段地址和s的段地址一样。而<code>bx</code>中存储了<code>s-se</code>，所以对于s来说:<code>bx+se</code>就是它自己的偏移量。而se的偏移量就是栈中的<code>IP</code>。所以思路顺下来了。可以写代码了。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">lp: push bp</span><br><span class="line">mov bp,sp</span><br><span class="line">dec cx      ;cx--</span><br><span class="line">jcxz lpret       ;if(cx=0) (cs:ip)=lpret</span><br><span class="line">add [bp+2],bx   ;重点</span><br><span class="line">lpret: pop bp</span><br><span class="line">iret</span><br></pre></td></tr></table></figure><blockquote><p>上述代码中<code>add [bp+2],bx</code>就是前面刚顺下来的算法实现。将栈中的<code>IP</code>修改称为s的偏移地址。这里的重点是<code>iret</code>指令，这个指令和跳转指令经常组合在一起共同使用，相当于<code>pop ip</code>,<code>pop cs</code>,<code>popf</code>这三条指令的和。<strong>上述代码就是在栈中修改好<code>CS:IP</code>的值，然后使用<code>iret</code>出栈执行。</strong></p></blockquote><h5 id="结果">结果</h5><blockquote><p>本次实验的过程实际上就是分为两部分:<code>安装例程</code>和<code>执行例程</code>。 ###### 安装例程 对于<code>7ch</code>中断类型码来说，对应的中断例程地址应该在<code>0000:01f0</code>。我们<strong>首先将中断例程写进该地址，然后修改中断向量表项。</strong></p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line">assume cs:code</span><br><span class="line">code segment</span><br><span class="line">start:</span><br><span class="line">mov ax,cs</span><br><span class="line">mov ds,ax</span><br><span class="line">mov si,offset do0</span><br><span class="line">mov ax,0</span><br><span class="line">mov es,ax</span><br><span class="line">mov di,200h</span><br><span class="line"></span><br><span class="line">mov cx,offset do0end-offset do0   ;将中断例程写进对应的地址中</span><br><span class="line"></span><br><span class="line">cld</span><br><span class="line">rep movsb</span><br><span class="line"></span><br><span class="line">mov ax,0   ;这4行代码修改中断向量表</span><br><span class="line">mov es,ax</span><br><span class="line">mov word ptr es:[7ch*4],200h</span><br><span class="line">mov word ptr es:[7ch*4+2],0</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">mov ax,4c00h</span><br><span class="line">int 21h</span><br><span class="line"></span><br><span class="line">do0:    ;这是中断例程</span><br><span class="line">push bp</span><br><span class="line">mov bp,sp</span><br><span class="line">dec cx</span><br><span class="line">jcxz lpret</span><br><span class="line">add [bp+2],bx</span><br><span class="line">lpret:pop bp</span><br><span class="line">iret</span><br><span class="line"></span><br><span class="line">do0end:nop</span><br><span class="line"></span><br><span class="line">code ends</span><br><span class="line">end start</span><br></pre></td></tr></table></figure><blockquote><p>我们将上述代码保存为<code>sample.asm</code>然后编译连接。然后执行<code>sample.exe</code>:<br><img src="20.png" width="50%" height="50%" alt=""> <img src="21.png" width="50%" height="50%" alt=""> 上图分别是地址<code>0000:0200</code>处的<strong>中断例程</strong>，和<code>0000:01f0</code>处的<strong>中断向量表</strong>。可以看到成功写入内存。</p></blockquote><h6 id="执行例程">执行例程</h6><blockquote><p>执行程序就是调用<code>int 7ch</code>这个例程看是否有效。我们将执行程序保存为<code>demo.asm</code>编译连接。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">assume cs:code</span><br><span class="line"></span><br><span class="line">code segment</span><br><span class="line">start:mov ax,0b800h</span><br><span class="line">mov es,ax</span><br><span class="line">mov di,160*12 ;确定显存的地址</span><br><span class="line"></span><br><span class="line">mov bx,offset s-offset se</span><br><span class="line">mov cx,80</span><br><span class="line">s:mov byte ptr es:[di],&apos;!&apos;</span><br><span class="line">add di,2</span><br><span class="line">int 7ch    ;在这里int指令充当了loop的功能</span><br><span class="line"></span><br><span class="line">se:nop</span><br><span class="line">mov ax,4c00h</span><br><span class="line">int 21h</span><br><span class="line"></span><br><span class="line">code ends</span><br><span class="line">end start</span><br></pre></td></tr></table></figure><blockquote><p>然后执行<code>demo.exe</code>,看现象:<br><img src="22.png" width="50%" height="50%" alt=""></p></blockquote><h6 id="总结">总结</h6><blockquote><p>这是最简单的一个例程，不过我们可以因此得知<strong>跳转指令</strong>和<strong>栈</strong>的重要性!</p></blockquote><h4 id="bios和dos所提供的中断例程">BIOS和DOS所提供的中断例程</h4><blockquote><p>关于BIOS和DOS已经很熟悉了，从计算机加电开始，<code>CS:IP</code>会自动跳转到<code>ffff:0000</code>的地址执行程序，到底执行的是啥？有两大部分:<strong>自检程序</strong>和<strong>初始化程序</strong>。初始化程序将建立BIOS所支持的中断向量。BIOS中的中断例程主要有:<br>* 外部中断和内部中断的中断例程 * 用于对硬件设备进行I/O操作的中断例程 * 其他和硬件系统相关的中断例程</p></blockquote><blockquote><p>自检和初始化完成之后，调用<code>int 19h</code>进行操作系统的引导。而DOS也有自己的中断例程，这些中断例程是操作系统提供给程序员的资源。另外，DOS和硬件设备相关的中断例程一般都是调用了BIOS的中断例程。每一个中断例程都是由很多的子程序构成的。不管是BIOS还是DOS的中断例程都通过<code>ah</code>来传递内部子程序的编号。 ##### BIOS中断例程 <strong>int 10h</strong>用来设置和字符和光标。见代码:</p></blockquote><blockquote><p>看现象: <img src="23.png" width="50%" height="50%" alt=""></p></blockquote><blockquote><p>下面是代码:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">assume cs:code</span><br><span class="line">code segment</span><br><span class="line">mov ah,2    ;置光标</span><br><span class="line">mov bh,0;第0页</span><br><span class="line">mov dh,5;行号</span><br><span class="line">mov dl,12;列号</span><br><span class="line">int 10h</span><br><span class="line"></span><br><span class="line">mov ah,9   ;在光标位置显示字符</span><br><span class="line">mov al, &apos;a&apos;  ;字符</span><br><span class="line">mov bl,11001010b   ;颜色属性</span><br><span class="line">mov bh,0    ;第0页</span><br><span class="line">mov cx,5   ;字符重复个数</span><br><span class="line">int 10h</span><br><span class="line"></span><br><span class="line">mov ax,4c00h</span><br><span class="line">int 21h </span><br><span class="line"></span><br><span class="line">code ends </span><br><span class="line">end</span><br></pre></td></tr></table></figure><h5 id="dos中断例程">DOS中断例程</h5><blockquote><p>DOS中断<strong>int 21h</strong>，之前一直使用<strong>4ch00</strong>参数即程序返回功能。<br>参数<strong>ah=9</strong>表示调用第21h号中断例程的9号子程序:在光标位置上显示字符串.看现象:<br><img src="24.png" width="50%" height="50%" alt=""> 下面是代码:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">assume cs:code</span><br><span class="line">data segment</span><br><span class="line">db &apos;Dos is shit!&apos;,&apos;$&apos;</span><br><span class="line">data ends</span><br><span class="line"></span><br><span class="line">code segment</span><br><span class="line"></span><br><span class="line">start:mov ah,2</span><br><span class="line">mov bh,0</span><br><span class="line">mov dl,12</span><br><span class="line">int 10h  ;BIOS 中断例程</span><br><span class="line"></span><br><span class="line">mov ax,data</span><br><span class="line">mov ds,ax</span><br><span class="line">mov dx,0</span><br><span class="line">mov ah,9</span><br><span class="line">int 21h   ;DOS中断例程</span><br><span class="line"></span><br><span class="line">mov ax,4c00h</span><br><span class="line">int 21h</span><br><span class="line"></span><br><span class="line">code ends</span><br><span class="line">end start</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;第四部分中断i&quot;&gt;第四部分:中断I&lt;/h2&gt;
&lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;在之前的实验中，我们所编写的汇编程序从编译到运行出结果都很顺利。这个过程映射到CPU层面就是&lt;code&gt;CS:IP&lt;/code&gt;的跳转&lt;strong&gt;唯一取决于&lt;/strong&gt;汇编程序员的代码。&lt;code&gt;CSAPP&lt;/code&gt;将这个过程称为&lt;strong&gt;程序控制流&lt;/strong&gt;。而&lt;strong&gt;中断&lt;/strong&gt;则是将这种流程打断，建立更复杂的&lt;strong&gt;异常控制流&lt;/strong&gt;。中断分为&lt;code&gt;内中断&lt;/code&gt;和&lt;code&gt;外中断&lt;/code&gt;两种。&lt;br&gt;
    
    </summary>
    
      <category term="Assembly" scheme="http://haotianmcihael.github.io/categories/Assembly/"/>
    
    
      <category term="i386" scheme="http://haotianmcihael.github.io/tags/i386/"/>
    
  </entry>
  
  <entry>
    <title>汇编语言（王爽）ch3</title>
    <link href="http://haotianmcihael.github.io/2018/10/11/%E6%B1%87%E7%BC%96%E8%AF%AD%E8%A8%80%EF%BC%88%E7%8E%8B%E7%88%BD%EF%BC%89ch3/"/>
    <id>http://haotianmcihael.github.io/2018/10/11/汇编语言（王爽）ch3/</id>
    <published>2018-10-10T21:29:35.000Z</published>
    <updated>2020-06-26T02:29:54.925Z</updated>
    
    <content type="html"><![CDATA[<h2 id="第三部分指令核心">第三部分:指令核心</h2><h3 id="abstract">Abstract</h3><h4 id="内存寻址">内存寻址</h4><blockquote><p>内存是啥？计算机从诞生之初，就面临着CPU和I/O之间，与存储器之间越来越明显的速度矛盾。正是因此才诞生了内存，cache等一系列缓冲的加速设备。你可以理解为内存是CPU获取运行代码的唯一途径，<strong>注意这里的内存指的是内存地址空间</strong>。<br>CPU如何访问内存？当然是使用寄存器;那寄存器如何访问内存？这就涉及到内存寻址的知识。<br><a id="more"></a> 寻址的方式有很多种类 ，不过归结到底都是<code>段地址+偏移地址</code>。段地址存储在段寄存器中，而偏移地址的方式就有很多种。见下图:<br><img src="113.png" width="50%" height="50%" alt=""></p></blockquote><blockquote><p>需要注意的是: * 上图中的<code>idata</code>表示立即数，<code>[]</code>表示偏移地址。<code>(ax)</code>表示寄存器ax的内容。 * <code>[]</code>中的除了立即数之外，只有<code>bx</code>,<code>si</code>,<code>di</code>和<code>bp</code>四个寄存器可以进行寻址，其他的寄存器不行。 * <code>[bp]</code>的段寄存器默认是<code>ss</code>。 * 编译器masm和调试器debug两个工具对于<code>mov ax,[idata]</code>中的<code>idata</code>处理不同，编译器会将其识别为<code>idata</code>,所以在使用编译器的时候这种形式应该写成<code>mov ax,ds:[idata]</code></p></blockquote><h4 id="数据处理总结性质">数据处理(总结性质)</h4><blockquote><p>计算机处理的最终还是数据，那么就有两个最基本的问题: * 处理的数据在什么地方？ * 要处理的数据有多长？ ##### 问题一 绝大部分机器指令都是进行数据处理的指令，处理大致分为三类:读取，写入，运算。在机器指令这一层来说并不关心数据的值，而关心<strong>指令执行前一秒，将要处理的数据所在的位置</strong>。指令在执行之前，所要处理的数据可以在三个地方:CPU内部，寄存器，端口。 ##### 问题二<br>8086指令可以处理两种尺寸的数据:Byte和Word。所以在机器指令中需要指明是字操作和字节操作。<br>* 通过寄存器指明:<code>ax</code>，<code>al</code>等 * 用操作符<code>X ptr</code>指明内存单元的长度，<code>X</code>是<code>word</code>或者<code>byte</code>。比如:<code>mov byte ptr ds:[0],1</code>。 * 还有一些默认的操作比如栈操作默认为字操作。</p></blockquote><h4 id="指令详解">指令详解</h4><h5 id="伪指令补充">伪指令补充</h5><ul><li><code>db</code> 定义字节型数据</li><li><code>dw</code> 定义字型数据</li><li><code>dd</code> 定义双字节型数据</li><li><code>dup</code> 和前面三个伪指令结合使用，用来进行数据的重复。<code>db 3 dup(1,2,3)</code>表示定义的三个字节型数据分别为1,2,3。</li></ul><h5 id="loop循环指令">LOOP循环指令</h5><blockquote><p>主要进行两步操作，（cx）=(cx)-1，判断cx中的值。不为零则转至标号处执行程序，如果为零则向下执行。可见<code>cx</code>中的值影响了loop指令的执行结果。</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">mov cx,循环次数</span><br><span class="line">s:</span><br><span class="line">循环执行的程序段</span><br><span class="line">loop s</span><br></pre></td></tr></table></figure><h5 id="jmp转移指令">JMP转移指令</h5><blockquote><p>可以修改IP，或同时修改CS和IP的指令统称为转移指令。概括地讲，转移指令就是可以控制CPU执行内存中某处代码的的指令。<br>根据修改的方式，只修改IP的称为<code>段内转移</code>，修改IP和CS的称为<code>段间转移</code>。由于转移指令对IP的修改范围不同，段内转移又分为:<code>短转移（8位）</code>和<code>近转移（16位）</code>。不同的转移方式，其基本的原理确实相同的。<br>###### 1-JMP short offset 转移指令值得注意的点就是offset指的不是确定的地址，而是从标号到指令之间的位移。<br><code>(IP)=(IP)+8位位移</code>，段内短转移的位移范围位8位有符号数，用补码表示。<code>offset处的地址-JMP指令后的第一个字节的地址</code>。 ###### 1-JMP near ptr offset <code>(IP)=(IP)+16位位移</code> ,段内近转移的位移范围是16位有符号数，用补码表示。原理和前者一样。 ###### 2-JMP far ptr offset 前面的几种类型地址都是通过offset和位移，而接下来的几种是在寄存器和内存中的给定IP或者CS。<strong>内存地址单元可以用任意的寻址方式得到。</strong><br><code>（CS）=标号所在段的段地址</code>，<code>（IP）=标号所在段中的偏移地址</code> 。段间转移，会发现其跳转地址实际上是在指令中的offset的地址。和前两个还是有区别的。 ###### 2-JMP 16位reg 最基础的跳转指令，只是修改IP=16位的寄存器中内容。 ###### 2-JMP word ptr 内存单元地址 比如:<code>jmp word ptr ds:[0]</code>中<code>ptr</code>后面的就是一个地址，<code>jmp word ds:[0],0</code>后面跟两个地址。注意<strong>（CS）=(内存地址单元+2)，（IP）=（内存地址单元）</strong>。 ###### 2-jcxz 该指令为有条件转移指令，<strong>所有的有条件转移指令都是短转移</strong>。方式为:<br><code>if((cx)==0)jmp short offset</code><br>另外，所有的<code>loop</code>循环指令都是短转移。</p></blockquote><h5 id="call和ret指令">CALL和RET指令</h5><h6 id="ret近转移">ret(近转移)</h6><blockquote><p>书中说<strong>ret指令用栈中的数据修改IP中的内容</strong>。之前对栈有一个分类，这里的栈是<strong>满递减堆栈</strong>。所以由此可知堆栈指针<code>SP</code>一直指向最后一个压栈的数据。<code>ret</code>指令实际上把堆栈中的最后一个数据作为自己跳转的偏移地址。方式如下:<br><code>(IP)=((ss)*16+(sp))</code>,<code>(sp)=(sp)+2</code><br>从上面也可以看出来这两条指令等同于:<code>pop IP</code></p></blockquote><h6 id="retf远转移">retf(远转移)</h6><blockquote><p>书中说<strong>retf指令用栈中的数据修改IP和CS中的内容</strong>。类似:<br><code>(IP)=((ss)*16+(sp))</code>,<code>(sp)=(sp)+2</code><br><code>（CS）=（（ss）*16+(sp)）</code>,<code>(sp)=(sp)+2</code><br>这四条指令等同于:<code>pop IP</code>,<code>pop CS</code></p></blockquote><h6 id="call指令">call指令</h6><blockquote><p>call指令不能实现短转移，但是其转移的原理和jmp指令相同。如下：<br><code>(sp)=(sp)-2</code>,<code>((ss)*16+(sp))=(IP)</code>,<code>(IP)=(IP)+16位位移</code>这几条指令相当于:<br><code>push IP</code>和<code>jmp near ptr offset</code></p></blockquote><h6 id="call和ret指令子程序">call和ret指令(子程序)</h6><blockquote><p>先看一段代码:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">assume cs:code</span><br><span class="line">code segment</span><br><span class="line">start: mov ax,1</span><br><span class="line">   mov cx,3</span><br><span class="line">   call s</span><br><span class="line">   mov bx,ax</span><br><span class="line">   mov ax,4c00h</span><br><span class="line">   int 21h</span><br><span class="line">s: add ax,ax</span><br><span class="line">   loop s</span><br><span class="line">   ret</span><br><span class="line">code ends</span><br><span class="line">end start</span><br></pre></td></tr></table></figure><blockquote><p>程序执行到<code>call s</code>的时候，IP指向了后面的一条指令，然后CPU执行<code>call s</code>指令————<code>将当前的IP（mov bx,ax的地址）压栈，然后修改为s地址</code>。然后子程序开始执行，执行结束之后执行<code>ret</code>指令————<code>从栈中弹出一个值，将CS:IP的值指向mov bx,ax</code>。然后返回为原来的地址。这两条指令为子程序的调用提供了可能。</p></blockquote><h4 id="标志寄存器">标志寄存器</h4><blockquote><p>寄存器是汇编程序员解决问题的最有利的手段，不仅仅在于可以存储数据，还具有辅助指令执行的功能。标志寄存器就是如此，<strong>用来存储CPU执行指令的某些执行结果。</strong>存储的信息称为“程序状态字PSW”。状态寄存器的每一位都有具体的含义。下面分别介绍:<br>##### ZF标志 零标志位，相关指令执行结束如果结果为0，zf=1；如果不为0，zf=0.</p></blockquote><h5 id="pf标志">PF标志</h5><blockquote><p>奇偶标志位，相关指令执行结束如果结果的所有bit位中1的个数为偶数，pf=1,否则pf=0.</p></blockquote><h5 id="sf标志">SF标志</h5><blockquote><p>符号标志位，相关指令执行结束如果结果为负，sf=1；否则sf=0。<strong>该标志是CPU对有符号数计算结果的一种记录。</strong></p></blockquote><h5 id="cf标志">CF标志</h5><blockquote><p>进位标志位，在进行<strong>无符号</strong>数运算的时候，它记录了运算结果的最高有效位向更高位的进位值，或从更高位的借位值。</p></blockquote><h5 id="of标志">OF标志</h5><blockquote><p>溢出标志位，在进行<strong>有符号</strong>数运算的时候，记录了是否发生溢出。</p></blockquote><h5 id="标志寄存器在debug中的使用">标志寄存器在Debug中的使用</h5><blockquote><p>标志寄存器是按照有意义的各个标志位单独表示的。如下。 <img src="7.png" width="50%" height="50%" alt=""> 下图是已知的标志位的表示:<br><img src="13.png" width="50%" height="50%" alt=""> #### 其他指令（总结性质） 还有很多的指令比如<code>MOV</code>,<code>CMP</code>，<code>abc</code>等。但这里并不打算详细讲解每一条指令，就像书中所说:<strong>汇编语言只是载体，目的在于理解机器运行的原理和方式。</strong><br>至此，我们已经学习了<code>寄存器</code>，<code>内存</code>,<code>汇编指令</code>，并且掌握了一个<strong>正常的程序控制流</strong>在机器层面的运行过程。接下来的内容，就属于操作系统的层面，引入<strong>异常控制流</strong>，也就是在正常的执行过程中有外界干扰的情况，这是理解整个OS的重点，也是我写这几篇技术博客的主要学习目的。(<code>mit6.828</code>中的汇编大同小异，只是指令集换成了AT&amp;T的格式)。</p></blockquote><h3 id="实验7-寻址方式在结构化数据访问中的应用">实验7 寻址方式在结构化数据访问中的应用</h3><h3 id="实验9-根据材料编程">实验9 根据材料编程</h3><h3 id="实验10-编写子程序">实验10 编写子程序</h3>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;第三部分指令核心&quot;&gt;第三部分:指令核心&lt;/h2&gt;
&lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;h4 id=&quot;内存寻址&quot;&gt;内存寻址&lt;/h4&gt;
&lt;blockquote&gt;
&lt;p&gt;内存是啥？计算机从诞生之初，就面临着CPU和I/O之间，与存储器之间越来越明显的速度矛盾。正是因此才诞生了内存，cache等一系列缓冲的加速设备。你可以理解为内存是CPU获取运行代码的唯一途径，&lt;strong&gt;注意这里的内存指的是内存地址空间&lt;/strong&gt;。&lt;br&gt;
CPU如何访问内存？当然是使用寄存器;那寄存器如何访问内存？这就涉及到内存寻址的知识。&lt;br&gt;
    
    </summary>
    
      <category term="Assembly" scheme="http://haotianmcihael.github.io/categories/Assembly/"/>
    
    
      <category term="i386" scheme="http://haotianmcihael.github.io/tags/i386/"/>
    
  </entry>
  
  <entry>
    <title>汇编语言（王爽）ch2</title>
    <link href="http://haotianmcihael.github.io/2018/10/04/%E6%B1%87%E7%BC%96%E8%AF%AD%E8%A8%80%EF%BC%88%E7%8E%8B%E7%88%BD%EF%BC%89ch2/"/>
    <id>http://haotianmcihael.github.io/2018/10/04/汇编语言（王爽）ch2/</id>
    <published>2018-10-03T20:26:50.000Z</published>
    <updated>2020-06-26T02:30:06.641Z</updated>
    
    <content type="html"><![CDATA[<h2 id="第二部分汇编结构">第二部分:汇编结构</h2><h3 id="abstract">Abstract</h3><h4 id="汇编语言的结构">汇编语言的结构</h4><h5 id="寄存器和内存之间">寄存器和内存之间</h5><blockquote><p>汇编语言是由不同的段构成的，这些段的功能各异不过主要分为<code>指令</code>和<code>数据</code>两部分。运行汇编代码的意思就是将汇编代码加载到内存中，通过寄存器完成内存和CPU之间的交互。从这个角度看寄存器就是汇编的一种手段。但是寄存器和内存实际上是两个完全不一样的硬件设备，暂时先不考虑性能和速率差的问题。指令和数据到底是如何在不同的存储单元之间传递的。<br><a id="more"></a> <strong>字节存储和字存储</strong>就是两种解决办法，在8086中寄存器是16位的(16位为一字)，而存储器的最小存储单元是8位也就是一个字节，所以如果在一些读写中需要用到字存储而不是字节存储的话，就需要两块连续的内存单元才能完成数据的传输，而且是<strong>高地址存放字的高位字节，低地址存放字的低位字节</strong>。<br><strong>要读写一块内存单元的时候，必须先要给出这个内存单元的地址。</strong>在实验1中我们知道了CPU使用<code>段地址+偏移地址</code>的方式来进行寻址。而实现这些功能的就是一些具有特殊功能的寄存器。这里提到的是最简单的也最常使用的两种类型:<br>* <code>CS</code>+<code>IP</code>————在任意时刻CPU将CS:IP指向的内容当做指令执行。<br>* <code>DS</code>+[idata]————<code>DS</code>通常用来存放需要访问的数据的段地址。<br><strong>这里主要介绍汇编语言的结构，对应到寄存器和内存寻址的方式后面会讲。</strong> ##### 数据段 将一段内存当做数据段，是我们在编程的时候的一种安排，可以在具体操作的时候，<strong>用ds存放数据的段地址，</strong>再根据相关指令访问数据段中的具体单元。</p></blockquote><h5 id="代码段">代码段</h5><blockquote><p>从硬件角度讲，CPU只认被CS:IP指向的内存单元中的内容为指令。要让CPU执行我们放在代码段的指令，必须要将CS:IP指向所定义的代码段中的第一条指令的首地址。</p></blockquote><h5 id="栈段">栈段</h5><blockquote><p><strong>栈是一种先进后出的数据结构</strong>，这是最基本的栈的概念。这里讲到的栈指的是内存区域。从操作系统的角度讲一个线程需要分配一个栈(stack)。相对应的一个进程需要分配一个堆(heap)。栈的大小在分配的时候就已经确定好了，其作用主要是存放一些局部和确定的变量和数据。<br>关于栈也有两个寄存器:<code>SS:SP</code>。换句话讲CPU如何知道一段内存区域是不是栈？栈顶的段地址放在<code>SS</code>中，而偏移地址放在<code>SP</code>中。<strong>任意时刻，SS:SP指向栈顶元素</strong>。这是规定。<br>另外，上微机原理课的时候，老师讲到栈的分类:<br>* 向高地址生长的称为递增堆栈。 * 向地地址生长的称为递减堆栈。<br>地址延伸主要的影响就是内存中的大端寻址和小端寻址。</p></blockquote><pre><code>&lt;img src=&quot;4.png&quot; width=&quot;50%&quot; height=&quot;50%&quot; alt=&quot;&quot;&gt;</code></pre><ul><li>堆栈指针指向最后压入堆栈的有效数据项，叫做满堆栈。</li><li>堆栈指针指向下一个待压入数据的空位置，叫做空堆栈。<br><img src="5.png" width="50%" height="50%" alt=""></li></ul><h5 id="栈指令">栈指令</h5><blockquote><p>在i386的指令集中，栈操作都是以字为单位的。 ###### push执行过程 1. SP=SP-2; 2. 向<code>SP:SS</code>指向的内存字单元中送入数据; ###### pop执行过程 1. 从<code>SP:SS</code>指向的内存字单元中读取数据; 2. SP=SP+2;</p></blockquote><h5 id="段的概念">段的概念</h5><blockquote><p>之前讨论过关于<strong>段</strong>这个概念。我认为，还是因为在编程的时候可以通过一个系统化的结构来统一汇编语言的实现逻辑。使得这些指令看上去便于管理和协调合作。至于其物理实现，其实就是CPU的寻址方式决定的。书本中有一段话特别经典，我摘抄如下:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">比如我们将10000H~1001F安排为代码段，并在里面存储如下代码:  </span><br><span class="line"></span><br><span class="line">MOV ax,1000H  </span><br><span class="line">MOV ss,ax</span><br><span class="line">MOV sp,0020H  //初始化栈顶</span><br><span class="line">MOV ax,cs</span><br><span class="line">MOV ds,ax  //设置数据段段地址</span><br><span class="line">MOV ax,[0]</span><br><span class="line">add ax,[2]</span><br><span class="line">add bx,[4]</span><br><span class="line">add bx,[6]</span><br><span class="line">push ax</span><br><span class="line">push bx</span><br><span class="line">pop ax</span><br><span class="line">pop bx  </span><br><span class="line"></span><br><span class="line">设置CS=1000，IP=0.这段代码就会被执行，但是可以看到在这段代码中，我们又将  </span><br><span class="line">10000H~1001FH安排为栈段和数据段。  </span><br><span class="line">可见不管我们如何安排，CPU将内存中的某段内存当做代码，是因CS:IP指向了那里。  </span><br><span class="line">CPU将某段内存当做栈，是因为SS:SP指向了那里。我们一定要清楚，什么是我们的安排，  </span><br><span class="line">以及如何让CPU按我们的安排行事。要非常清楚CPU的工作机理，才能在控制CPU按照我们  </span><br><span class="line">的安排运行的时候做到游刃有余。  </span><br><span class="line">一段内存，可以既是代码的存储空间，又是数据的存储空间，还可以是栈空间，也可以什么都不是。  </span><br><span class="line">关键是在于CPU中寄存器的设置，机CS，IP，SS，SP，DS的指向。</span><br></pre></td></tr></table></figure><h5 id="多个段的统一">多个段的统一</h5><blockquote><p>以前写的汇编都是单个段，并且主要是代码段。如果内存中的一段汇编代码需要包含多个数据段，代码段，栈段。该如何设计才能保证程序正常运行呢？<br><strong>我们应该有一个概念，就是所谓和CPU对应的程序一定是<code>CS:IP</code>指向的内存地址。</strong>所以实际上这个问题是在问保证<code>CS:IP</code>正确指向程序的前提下，如何让数据段，栈段合理的加进来？<br>这里有个<code>伪指令</code>的概念，伪指令主要是为汇编器提供一些编译的前提信息比如从哪里开始执行代码，其他的段的起始地址在哪里。所以问题也就解决了。</p><ul><li>如何保证<code>CS:IP</code>指向代码段: <code>end</code>和<code>end start</code></li><li>如何定义一个段: <code>code segment</code>和<code>code ends</code></li><li>如何将段寄存器和段相对应: <code>assume cs:code</code></li></ul></blockquote><h4 id="汇编语言的编译连接过程及其工具的使用">汇编语言的编译、连接过程及其工具的使用</h4><blockquote><p>这部分详细见书上。</p></blockquote><h5 id="源程序组成">源程序组成</h5><blockquote><p>一个汇编程序的源程序由<code>汇编指令</code>和<code>伪指令</code>构成。汇编指令指的是编译成为机器指令最终为CPU所执行的代码。伪指令没有相对应的机器指令，主要是由编译器来执行的指令。</p></blockquote><h5 id="编译链接执行">编译、链接、执行</h5><blockquote><p>源程序经过编译成为可重定位文件，然后经过链接成为可执行文件。可执行文件是可以直接在操作系统中执行的。上述两种文件都属于ELF格式的文件，ELF是Linux下面的一种文件格式，具体的细节在后面的操作系统中会谈到，这里只是了解。<br><code>编程</code>（edit）————&gt;<code>1.asm</code>————&gt;<code>编译</code>(masm)————&gt;<code>1.obj</code>————&gt;<code>链接</code>(link)————&gt;<code>1.exe</code>————&gt;<code>加载</code>(shell)————&gt;<code>内存中的程序</code>————&gt;<code>运行</code>(CPU)</p></blockquote><p><img src="6.png" width="50%" height="50%" alt=""></p><h3 id="工具进阶">工具进阶</h3><h4 id="debug--d">debug -d</h4><blockquote><p>第一个实验中使用的debug工具，通过各种参数来对寄存器和内存进行读写。比如<code>d 段地址:偏移地址</code>指令会列出指定内存单元中的内容。但是debug也是一个程序，而能定位内存地址的方式在8086中只有段寄存器可以做到,实际上debug就是通过将地址写入段寄存器中的方式来进行内存寻址。这个原理适用于所有的参数。<br>而使用的这个段寄存器正是<code>DS</code>寄存器。<br>根据这个原理，就可以通过修改寄存器来进阶使用这些命令，比如:<code>-r ds</code>,<code>:1000</code>,<code>-d ds:0</code>这段代码就表示查看从1000：0开始的内存空间中的内容。这里主要是认识到段寄存器DS的寻址功能，为之后的寻址方式打基础。</p></blockquote><h4 id="debug--p">debug -p</h4><blockquote><p>在整个实验中，debug工具是经常要使用的。debug有两种用法，一种就是使用<code>a</code>参数直接写入程序然后调试。还有一种就是直接调试<code>exe</code>文件。<br>第一种之前一直在用，第二种方式加载的过程有一些不一样，ds段寄存器中存放着该程序的段地址，因为偏移为零所以DS:0就是所加载程序的首地址。而加载进来的程序前256字节存放的是一段通信程序，所以真正的代码是在<code>(DS+10H:0):0</code>开始的。<br>每一段汇编指令都需要以<code>mov ax,4c00H</code>，<code>int 21H</code>结尾。在调试到<code>int 21H</code>的时候需要使用<code>P</code>命令执行。出现<code>Program terminated normally</code>程序正常退出。</p></blockquote><h3 id="实验2-用机器指令和汇编指令编程">实验2 用机器指令和汇编指令编程</h3><blockquote><p>这个实验很有意思，一共有两部分: #### 第一部分 使用<code>a</code>将下面的汇编指令写入内存，然后使用<code>T</code>命令调试。如下:</p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">mov ax,2000</span><br><span class="line">mov ss,ax</span><br><span class="line">mov sp,10</span><br><span class="line">mov ax,3123</span><br><span class="line">push ax</span><br><span class="line">mov ax,3366</span><br><span class="line">push ax</span><br></pre></td></tr></table></figure><blockquote><p>会发现在单步调试的时候，在<code>mov ss,ax</code>之后的下一条指令竟然不是<code>mov sp,10</code>。但是查看寄存器的时候发现实际上这条执行已经被执行结束了。这是第一个现象。 <img src="7.png" width="50%" height="50%" alt=""></p></blockquote><h4 id="第二部分">第二部分</h4><blockquote><p>对上面的代码继续调试，如下： 会发现最开始对栈初始化之后，到执行到<code>mov ss,ax</code>指令的时候，查看原来的栈段内容竟然不再是零，且这些数字还都是和段寄存器相关的。<br><img src="8.png" width="50%" height="50%" alt=""></p></blockquote><h4 id="总结">总结</h4><blockquote><p>下图是接下来的每一步后栈段中的内存变化。<br><img src="9.png" width="50%" height="50%" alt=""> 可以看到的现象是:开始的时候栈中是没有数据的，栈桢SP指向<code>2000:0010</code>的位置。<br>在<code>mov ss,ax</code>和<code>mov sp,10</code>这两步执行结束之后，发现栈中已经有数据了，这里称为初始化。初始化的一共有6字节的数据:4个字节为<code>CS:IP</code>，还有两字节在栈底确定为<code>01A3H</code>。<br>之后的每一条栈操作的指令执行结束之后栈中的<code>CS:IP</code>值也会跟着变化，这说明每一次指令都有可能产生一次中断。<br>但是在栈底的<code>01A3H</code>一直没有变化估计应该是中断相关。在正式压栈操作的过程中发现最开始初始化过的那些数据并没有被压到栈底，<strong>这就意味着堆栈指针SP应该还在栈底，然后真正有数据项压栈的时候才会SP-=2操作。</strong> ##### 小实验 就上面的三个现象，我在想仅仅是初始化的过程就已经占据了一大半栈空间，那如果将栈大小调整成为8字节的。那会出现什么情况？ 我将<code>mov sp,10</code>改成<code>mov sp,08</code>；然后准备了8字节数据，所以栈会被压满。这时候我想看看现象。<br>这是一张初始化的图: <img src="10.png" width="50%" height="50%" alt=""></p></blockquote><blockquote><p>这是末尾压栈满了的图: <img src="11.png" width="50%" height="50%" alt=""></p></blockquote><blockquote><p>因为书上关于中断的详细讲解在第四部分，所以这里只是一个猜测。<strong>现象中随着栈不断被压满，之前初始化的那些数据都被挤出去了，具体去哪了不知道。那6个字节的数据到底有什么用也不知道。</strong><br>##### 猜测 不过可以知道的是初始化栈帧的时候会有参数压到栈里，参数有<code>CS:IP</code>和中断向量号，其中<code>CS:IP</code>还在<strong>不断变化</strong>。而随着栈中的数据越来越多，栈中的这6个字节的参数被慢慢挤出。难道这6个字节不重要吗，或者说只是在初始化的时候用了一下？但是过程中这些参数一直也在变化，所以不可能不重要.不过这些数据之所以放到栈中的原因一定不是为了存储，否则就不会被随便挤出。<br>结合第一部分实验，<code>mov sp,10</code>是紧跟着前一条指令执行的，所以我能想到的唯一一个合理的解释就是:这些参数是在指令执行过程中直接使用的，而执行过程对shell屏蔽不可见，所以在栈中数据越来越多的时候这些参数还可以随意的移动位置并且被抛弃，这些参数是过程中使用的参数，而这个过程就是中断！至于中断到底是什么，到第4章再详细学习吧。</p></blockquote><h3 id="实验3-用编程编译连接跟踪">实验3 用编程、编译、连接、跟踪</h3><blockquote><p>最后一个实验查看PSP（就是加载开始前256字节）的内容。<br>使用<code>u</code>来查看汇编好像又和中断有关系。 <img src="12.png" width="50%" height="50%" alt=""></p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;第二部分汇编结构&quot;&gt;第二部分:汇编结构&lt;/h2&gt;
&lt;h3 id=&quot;abstract&quot;&gt;Abstract&lt;/h3&gt;
&lt;h4 id=&quot;汇编语言的结构&quot;&gt;汇编语言的结构&lt;/h4&gt;
&lt;h5 id=&quot;寄存器和内存之间&quot;&gt;寄存器和内存之间&lt;/h5&gt;
&lt;blockquote&gt;
&lt;p&gt;汇编语言是由不同的段构成的，这些段的功能各异不过主要分为&lt;code&gt;指令&lt;/code&gt;和&lt;code&gt;数据&lt;/code&gt;两部分。运行汇编代码的意思就是将汇编代码加载到内存中，通过寄存器完成内存和CPU之间的交互。从这个角度看寄存器就是汇编的一种手段。但是寄存器和内存实际上是两个完全不一样的硬件设备，暂时先不考虑性能和速率差的问题。指令和数据到底是如何在不同的存储单元之间传递的。&lt;br&gt;
    
    </summary>
    
      <category term="Assembly" scheme="http://haotianmcihael.github.io/categories/Assembly/"/>
    
    
      <category term="i386" scheme="http://haotianmcihael.github.io/tags/i386/"/>
    
  </entry>
  
</feed>
